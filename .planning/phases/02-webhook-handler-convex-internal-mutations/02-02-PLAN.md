---
phase: 02-webhook-handler-convex-internal-mutations
plan: 02
type: tdd
wave: 1
depends_on: ["02-01"]
files_modified: [convex/subscriptions.ts, convex/subscriptions.test.ts]
autonomous: true

requirements: [PAY-03]

must_haves:
  truths:
    - "A checkout.session.completed-shaped call creates a subscriptions row with the correct clerkUserId (ROADMAP SC1)"
    - "Replaying the same stripeEventId produces no duplicate row (ROADMAP SC2) — the processedStripeEvents check-and-mark happens inside the same atomic mutation as the state write (D-14), and an already-processed event no-ops and returns without error (D-15)"
    - "An unresolvable clerkUserId does not throw — it returns an anomaly sentinel so the caller can still return 200 (D-12)"
    - "subscriptions.ts stays a pure data-access module — the dispatch/routing switch on event.type lives only in convex/stripeWebhooks.ts (D-04)"
  artifacts:
    - path: "convex/subscriptions.ts"
      provides: "Real upsertSubscription/deleteSubscription/markPastDue/getSubscription implementations replacing Phase 1 throw-stubs"
      exports:
        [
          "getSubscription",
          "upsertSubscription",
          "deleteSubscription",
          "markPastDue",
        ]
    - path: "convex/subscriptions.test.ts"
      provides: "convex-test coverage for idempotency, upsert/patch, delete, and by_stripeSubscriptionId lookup"
      min_lines: 80
  key_links:
    - from: "convex/subscriptions.ts upsertSubscription/deleteSubscription/markPastDue"
      to: "processedStripeEvents table"
      via: 'ctx.db.query("processedStripeEvents").withIndex("by_stripeEventId", ...) check-and-insert inside the same mutation'
      pattern: "by_stripeEventId"
    - from: "convex/subscriptions.ts markPastDue"
      to: "convex/schema.ts subscriptions.by_stripeSubscriptionId"
      via: 'ctx.db.query("subscriptions").withIndex("by_stripeSubscriptionId", ...)'
      pattern: "by_stripeSubscriptionId"
---

<objective>
Replace the Phase 1 throw-stubs in `convex/subscriptions.ts` with real, atomic, idempotent implementations of `upsertSubscription`, `deleteSubscription`, `markPastDue`, and `getSubscription` — the sole writers of the `subscriptions` table for this milestone (D-01).

Purpose: Fulfills ROADMAP SC1 (correct row creation) and SC2 (idempotent replay). The idempotency check-and-mark must live inside these mutations, not in the dispatcher action (Convex actions are not transactional — a `ctx.runQuery` + `ctx.runMutation` split from the action would reopen the exact race D-14 exists to close).
Output: `convex/subscriptions.ts` with 4 fully implemented exports; `convex/subscriptions.test.ts` green via `convex-test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-CONTEXT.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-RESEARCH.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-PATTERNS.md
@.planning/phases/01-schema-infrastructure-foundation/01-PATTERNS.md
</context>

<interfaces>
Current `convex/subscriptions.ts` (Phase 1 stub, to be replaced — read this file directly before editing, do not assume this excerpt is still accurate):

```typescript
import { query, internalMutation } from "./_generated/server";
import { v } from "convex/values";

export const getSubscription = query({
  args: { clerkUserId: v.string() },
  handler: async () => {
    throw new Error("Not implemented — Phase 2");
  },
});

export const upsertSubscription = internalMutation({
  args: {
    clerkUserId: v.string(),
    stripeCustomerId: v.string(),
    stripeSubscriptionId: v.string(),
    status: v.union(
      v.literal("active"),
      v.literal("canceled"),
      v.literal("past_due"),
    ),
    currentPeriodEnd: v.number(),
    cancelAtPeriodEnd: v.boolean(),
  },
  handler: async () => {
    throw new Error("Not implemented — Phase 2");
  },
});

export const deleteSubscription = internalMutation({
  args: { clerkUserId: v.string() },
  handler: async () => {
    throw new Error("Not implemented — Phase 2");
  },
});
```

`convex/schema.ts` subscriptions table (post-Plan-02-01) has both `by_clerkUserId` and `by_stripeSubscriptionId` indexes. `processedStripeEvents` table has `by_stripeEventId` index (unchanged from Phase 1).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: upsertSubscription + getSubscription — creation, patch, idempotency, anomaly handling</name>
  <read_first>
    convex/subscriptions.ts (current Phase 1 stub — full file)
    convex/schema.ts (subscriptions + processedStripeEvents table shapes)
    convex/users.ts (lines ~17-23, the withIndex-then-patch-or-insert pattern to translate)
    .planning/phases/02-webhook-handler-convex-internal-mutations/02-PATTERNS.md (upsertSubscription target shape section)
  </read_first>
  <files>convex/subscriptions.ts, convex/subscriptions.test.ts</files>
  <behavior>
    - Test: `upsertSubscription` called with `{ stripeEventId: "evt_1", eventType: "checkout.session.completed", clerkUserId: "user_1", stripeCustomerId: "cus_1", stripeSubscriptionId: "sub_1", status: "active", currentPeriodEnd: 1234567890, cancelAtPeriodEnd: false }` on an empty database inserts one row into `subscriptions`; a subsequent `getSubscription({ clerkUserId: "user_1" })` call returns a row with `clerkUserId: "user_1"`, `stripeSubscriptionId: "sub_1"`, `status: "active"`.
    - Test: calling `upsertSubscription` again with the SAME `stripeEventId: "evt_1"` but a different `status: "past_due"` returns `{ alreadyProcessed: true }` and leaves the existing row's `status` as `"active"` (unchanged) — proves D-14/D-15 idempotency.
    - Test: calling `upsertSubscription` with a NEW `stripeEventId: "evt_2"`, the same `clerkUserId: "user_1"`, and `status: "past_due"` PATCHES the existing row (still exactly one row in the table) rather than inserting a second row — simulates a `customer.subscription.updated` follow-up call.
    - Test: calling `upsertSubscription` with `clerkUserId: undefined` (all other fields present) returns a sentinel object containing an `anomaly` key (do NOT throw) and the `subscriptions` table row count stays at 0 for that call.
    - Test: `getSubscription({ clerkUserId: "user_nonexistent" })` returns `null`.
  </behavior>
  <action>
    Implement `getSubscription` as a `query` doing `ctx.db.query("subscriptions").withIndex("by_clerkUserId", q => q.eq("clerkUserId", args.clerkUserId)).unique()`, returning the row or `null`.
    Implement `upsertSubscription` as an `internalMutation` whose `args` adds `stripeEventId: v.string()` and `eventType: v.string()` to the existing Phase 1 signature (already has clerkUserId/stripeCustomerId/stripeSubscriptionId/status/currentPeriodEnd/cancelAtPeriodEnd) and makes `clerkUserId` optional (`v.optional(v.string())`) since the anomaly path needs to accept a missing value without a validator rejection. Handler body, in order: (1) query `processedStripeEvents` by `by_stripeEventId` on `args.stripeEventId`; if found, `return { alreadyProcessed: true }` immediately (D-15) — no further writes. (2) if `!args.clerkUserId`, `console.error` a descriptive anomaly message including `args.stripeEventId`, then `return { anomaly: "missing clerkUserId" }` (D-12) — do NOT throw. (3) query `subscriptions` by `by_clerkUserId` on `args.clerkUserId`; if found, `ctx.db.patch` the existing doc's `_id` with `stripeCustomerId`, `stripeSubscriptionId`, `status`, `currentPeriodEnd`, `cancelAtPeriodEnd`; else `ctx.db.insert("subscriptions", {...})` with all 6 fields including `clerkUserId`. (4) `ctx.db.insert("processedStripeEvents", { stripeEventId: args.stripeEventId, eventType: args.eventType, processedAt: Date.now() })`. Return a plain success value (e.g. `{ success: true }`).
    Keep the bare-throw convention (no try/catch) for any genuine error path (e.g. Convex internal errors) — only the D-12/D-15 sentinel-return cases deviate from bare-throw, per 02-PATTERNS.md's explicit carve-out.
  </action>
  <verify>
    <automated>npx vitest run convex/subscriptions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/subscriptions.test.ts -t "creates"` passes
    - `npx vitest run convex/subscriptions.test.ts -t "idempotent"` passes and asserts the table row count is exactly 1 after 2 calls with the same stripeEventId
    - `npx vitest run convex/subscriptions.test.ts -t "anomaly"` passes and asserts no throw occurred (test does not wrap the call in `expect(...).rejects`)
    - `grep -c "throw new Error(\"Not implemented" convex/subscriptions.ts` returns 0 for `upsertSubscription`/`getSubscription` (stub text fully removed for these two)
  </acceptance_criteria>
  <done>upsertSubscription and getSubscription are fully implemented and covered by passing convex-test tests; idempotency and anomaly paths return sentinels, never throw.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deleteSubscription + markPastDue — cancellation and D-08 stripeSubscriptionId lookup</name>
  <read_first>
    convex/subscriptions.ts (as modified by Task 1 — re-read after Task 1 completes)
    .planning/phases/02-webhook-handler-convex-internal-mutations/02-CONTEXT.md (D-07 for deleteSubscription's clerkUserId resolution, D-08 for markPastDue's stripeSubscriptionId lookup)
  </read_first>
  <files>convex/subscriptions.ts, convex/subscriptions.test.ts</files>
  <behavior>
    - Test: given an existing `subscriptions` row for `clerkUserId: "user_2"`, calling `deleteSubscription({ stripeEventId: "evt_3", eventType: "customer.subscription.deleted", clerkUserId: "user_2" })` removes that row (`getSubscription({ clerkUserId: "user_2" })` returns `null` afterward).
    - Test: calling `deleteSubscription` a second time with the SAME `stripeEventId: "evt_3"` returns `{ alreadyProcessed: true }` and does not error even though the row no longer exists.
    - Test: calling `deleteSubscription` with `clerkUserId: undefined` returns an anomaly sentinel, does not throw.
    - Test: given an existing `subscriptions` row with `stripeSubscriptionId: "sub_9"` and `status: "active"`, calling `markPastDue({ stripeEventId: "evt_4", eventType: "invoice.payment_failed", stripeSubscriptionId: "sub_9" })` patches that row's `status` to `"past_due"` without needing a `clerkUserId` argument at all (D-08 — resolves entirely via the `by_stripeSubscriptionId` index).
    - Test: calling `markPastDue` with a `stripeSubscriptionId` that matches no existing row returns an anomaly sentinel, does not throw, and inserts no `subscriptions` row.
    - Test: calling `markPastDue` twice with the same `stripeEventId` is idempotent (second call returns `{ alreadyProcessed: true }`, status is not reverted or re-patched).
  </behavior>
  <action>
    Extend `deleteSubscription`'s `args` to add `stripeEventId: v.string()` and `eventType: v.string()` (Phase 1 stub only had `clerkUserId`), keep `clerkUserId: v.optional(v.string())` for the same anomaly-acceptance reason as Task 1. Handler: (1) idempotency check by `stripeEventId` identical to Task 1's step 1. (2) if `!args.clerkUserId`, return anomaly sentinel, no throw. (3) query `subscriptions` by `by_clerkUserId`; if found, `ctx.db.delete(existing._id)`; if not found, still proceed to step 4 (nothing to delete is not itself an error — D-12 anomaly is reserved for unresolvable clerkUserId, not "already gone"). (4) insert the `processedStripeEvents` row.
    Add a new `internalMutation` named `markPastDue` with `args: { stripeEventId: v.string(), eventType: v.string(), stripeSubscriptionId: v.string() }` (no `clerkUserId` param — this function resolves entirely via the subscription row itself per D-08, avoiding the extra Stripe API round-trip D-05 rejects). Handler: (1) idempotency check by `stripeEventId`. (2) query `subscriptions` by `by_stripeSubscriptionId` on `args.stripeSubscriptionId`; if not found, `console.error` an anomaly message and `return { anomaly: "no subscription for stripeSubscriptionId" }` (D-12) — do NOT throw. (3) `ctx.db.patch(existing._id, { status: "past_due" })`. (4) insert the `processedStripeEvents` row.
  </action>
  <verify>
    <automated>npx vitest run convex/subscriptions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/subscriptions.test.ts` — full file passes, 0 failures
    - `grep -c "export const markPastDue" convex/subscriptions.ts` returns exactly 1
    - `grep -c "by_stripeSubscriptionId" convex/subscriptions.ts` returns at least 1 (markPastDue uses the new index, not `.filter()`)
    - `grep -c "\.filter(" convex/subscriptions.ts` returns 0 (no non-indexed lookups introduced)
  </acceptance_criteria>
  <done>deleteSubscription and markPastDue are fully implemented, idempotent, and covered by passing tests; markPastDue resolves exclusively via the by_stripeSubscriptionId index with no Stripe API calls.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                                               |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Convex action -> these internalMutations | Only reachable via `ctx.runMutation(internal.subscriptions.*)` from `convex/stripeWebhooks.ts` (Plan 02-04) — not exposed to any public client SDK (D-01) |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                                                                     |
| --------- | ---------------------- | --------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-02   | Elevation of Privilege | `subscriptions` table writes      | mitigate    | All 4 exports stay `internalMutation`/`query`, never converted to public `mutation` — enforced by import from `./_generated/server`'s `internalMutation` builder, matching Phase 1's D-01/D-02 lock |
| T-02-04   | Tampering (replay)     | Duplicate Stripe webhook delivery | mitigate    | `processedStripeEvents` check-and-mark happens inside the same mutation transaction as the state write (D-14), verified by Task 1/2's idempotency tests                                             |

</threat_model>

<verification>
`npx vitest run convex/subscriptions.test.ts` passes with 0 failures covering: row creation, idempotent replay (no duplicate row), patch-on-second-event, anomaly sentinel (no throw) for missing clerkUserId, delete, and markPastDue's by_stripeSubscriptionId resolution.
</verification>

<success_criteria>

- `convex/subscriptions.ts` has zero remaining "Not implemented — Phase 2" throw-stubs
- All idempotency and anomaly paths return sentinel objects, never throw
- ROADMAP SC1 (correct clerkUserId on row creation) and SC2 (no duplicate on replay) are automatically verified
  </success_criteria>

<output>
Create `.planning/phases/02-webhook-handler-convex-internal-mutations/02-02-SUMMARY.md` when done
</output>
