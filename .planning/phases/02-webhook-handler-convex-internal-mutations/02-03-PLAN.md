---
phase: 02-webhook-handler-convex-internal-mutations
plan: 03
type: tdd
wave: 1
depends_on: ["02-01"]
files_modified: [convex/aiCredits.ts, convex/aiCredits.test.ts]
autonomous: true

requirements: [CRED-01]

must_haves:
  truths:
    - "invoice.paid with billing_reason subscription_cycle resets the aiCredits balance to 100 (ROADMAP SC3)"
    - "Replaying the same stripeEventId does not double-reset or double-insert a creditTransactions row"
    - "An invoice event whose stripeSubscriptionId matches no subscriptions row does not throw — it returns an anomaly sentinel (D-12)"
  artifacts:
    - path: "convex/aiCredits.ts"
      provides: "Real resetCredits/getCredits implementations; deductCredit/addCredits remain Phase 1 stubs (out of Phase 2 scope — CRED-04/PAY-05 belong to Phase 5)"
      exports: ["getCredits", "resetCredits"]
    - path: "convex/aiCredits.test.ts"
      provides: "convex-test coverage for cross-table stripeSubscriptionId resolution, balance reset, and idempotency"
      min_lines: 60
  key_links:
    - from: "convex/aiCredits.ts resetCredits"
      to: "convex/schema.ts subscriptions.by_stripeSubscriptionId"
      via: 'ctx.db.query("subscriptions").withIndex("by_stripeSubscriptionId", ...) to resolve clerkUserId (D-08/D-09)'
      pattern: "by_stripeSubscriptionId"
    - from: "convex/aiCredits.ts resetCredits"
      to: "creditTransactions table"
      via: 'ctx.db.insert("creditTransactions", { type: "reset", amount: 100, ... })'
      pattern: "creditTransactions"
---

<objective>
Implement `resetCredits` in `convex/aiCredits.ts` — the sole function that fulfills CRED-01 (monthly credit quota reset on billing date). Unlike `subscriptions.ts`'s functions, `resetCredits` does NOT receive `clerkUserId` directly: `invoice.paid` events carry no user metadata (D-08), so `resetCredits` must resolve `clerkUserId` itself by looking up the existing `subscriptions` row via `stripeSubscriptionId`, entirely inside its own atomic mutation transaction (D-09/D-14).

Purpose: Fulfills ROADMAP SC3 exactly as worded — a subscription_cycle invoice.paid event resets balance to 100. `deductCredit`/`addCredits` stay untouched Phase 1 stubs; they belong to Phase 5 (CRED-04, PAY-05) per REQUIREMENTS.md traceability and are out of this phase's scope — do not implement them here.
Output: `convex/aiCredits.ts` with `getCredits`/`resetCredits` fully implemented; `convex/aiCredits.test.ts` green via `convex-test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-CONTEXT.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-RESEARCH.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-PATTERNS.md
</context>

<interfaces>
Current `convex/aiCredits.ts` (Phase 1 stub, `getCredits`/`resetCredits` to be replaced; `deductCredit`/`addCredits` left untouched):

```typescript
import { query, internalMutation } from "./_generated/server";
import { v } from "convex/values";

export const getCredits = query({
  args: { clerkUserId: v.string() },
  handler: async () => {
    throw new Error("Not implemented — Phase 2");
  },
});

export const deductCredit = internalMutation({
  args: { clerkUserId: v.string(), amount: v.number() },
  handler: async () => {
    throw new Error("Not implemented — Phase 2");
  },
}); // LEAVE UNCHANGED — Phase 5 (CRED-04)

export const resetCredits = internalMutation({
  args: { clerkUserId: v.string() },
  handler: async () => {
    throw new Error("Not implemented — Phase 2");
  },
});

export const addCredits = internalMutation({
  args: { clerkUserId: v.string(), amount: v.number() },
  handler: async () => {
    throw new Error("Not implemented — Phase 2");
  },
}); // LEAVE UNCHANGED — Phase 5 (PAY-05)
```

`convex/schema.ts` `subscriptions` table (post-Plan-02-01) has `by_stripeSubscriptionId` index — this is what `resetCredits` queries to resolve `clerkUserId`. `aiCredits` and `creditTransactions` tables keep their existing single `by_clerkUserId` index (D-09 — no new index needed on either).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resetCredits + getCredits — cross-table clerkUserId resolution, balance reset, idempotency</name>
  <read_first>
    convex/aiCredits.ts (current Phase 1 stub — full file)
    convex/schema.ts (subscriptions, aiCredits, creditTransactions, processedStripeEvents table shapes)
    convex/subscriptions.ts (as implemented by Plan 02-02 — confirms the exact shape of a subscriptions row this mutation will query against)
  </read_first>
  <files>convex/aiCredits.ts, convex/aiCredits.test.ts</files>
  <behavior>
    - Test setup: seed a `subscriptions` row directly via `t.run(ctx => ctx.db.insert("subscriptions", { clerkUserId: "user_3", stripeCustomerId: "cus_3", stripeSubscriptionId: "sub_3", status: "active", currentPeriodEnd: 111, cancelAtPeriodEnd: false }))` — resetCredits must resolve clerkUserId from this seeded row, not from a direct argument.
    - Test: calling `resetCredits({ stripeEventId: "evt_5", eventType: "invoice.paid", stripeSubscriptionId: "sub_3" })` on a database with NO existing `aiCredits` row for `user_3` inserts a new `aiCredits` row with `balance: 100` and a `lastResetAt` timestamp; `getCredits({ clerkUserId: "user_3" })` confirms `balance === 100`.
    - Test: seed an existing `aiCredits` row for `user_3` with `balance: 42`; calling `resetCredits` with a new `stripeEventId` patches `balance` back to `100` (patch path, not insert).
    - Test: calling `resetCredits` twice with the SAME `stripeEventId: "evt_5"` — the second call returns `{ alreadyProcessed: true }`, the `aiCredits` row is not modified further, and exactly one `creditTransactions` row of `type: "reset"` exists (no duplicate).
    - Test: calling `resetCredits({ stripeEventId: "evt_6", eventType: "invoice.paid", stripeSubscriptionId: "sub_nonexistent" })` (no matching `subscriptions` row) returns an anomaly sentinel, does NOT throw, and inserts no `aiCredits`/`creditTransactions` row.
    - Test: after a successful reset, exactly one `creditTransactions` row exists with `clerkUserId: "user_3"`, `type: "reset"`, `amount: 100`.
    - Test: `getCredits({ clerkUserId: "user_nonexistent" })` returns `null`.
  </behavior>
  <action>
    Define a module-level constant `const MONTHLY_CREDIT_QUOTA = 100;` at the top of `convex/aiCredits.ts` (RESEARCH.md Open Question 2 recommendation — named constant, no arg-shape change from the Phase 1 stub's intent).
    Implement `getCredits` as a `query` doing `ctx.db.query("aiCredits").withIndex("by_clerkUserId", q => q.eq("clerkUserId", args.clerkUserId)).unique()`, returning the row or `null`. Leave its `args: { clerkUserId: v.string() }` signature unchanged from Phase 1.
    Replace `resetCredits`'s `args` with `{ stripeEventId: v.string(), eventType: v.string(), stripeSubscriptionId: v.string() }` (drops the Phase 1 stub's `clerkUserId` param entirely — this function resolves it internally, unlike `subscriptions.ts`'s functions which receive it as an argument). Handler body, in order: (1) query `processedStripeEvents` by `by_stripeEventId` on `args.stripeEventId`; if found, `return { alreadyProcessed: true }` (D-15). (2) query `subscriptions` by `by_stripeSubscriptionId` on `args.stripeSubscriptionId`; if not found, `console.error` an anomaly message including `args.stripeEventId` and `args.stripeSubscriptionId`, then `return { anomaly: "no subscription for stripeSubscriptionId" }` (D-12) — do NOT throw. (3) extract `clerkUserId` from the found subscription doc. (4) query `aiCredits` by `by_clerkUserId` on that `clerkUserId`; if found, `ctx.db.patch(existing._id, { balance: MONTHLY_CREDIT_QUOTA, lastResetAt: Date.now() })`; else `ctx.db.insert("aiCredits", { clerkUserId, balance: MONTHLY_CREDIT_QUOTA, lastResetAt: Date.now() })`. (5) `ctx.db.insert("creditTransactions", { clerkUserId, type: "reset", amount: MONTHLY_CREDIT_QUOTA, createdAt: Date.now() })`. (6) `ctx.db.insert("processedStripeEvents", { stripeEventId: args.stripeEventId, eventType: args.eventType, processedAt: Date.now() })`.
    Do NOT modify `deductCredit` or `addCredits` — leave their Phase 1 stub bodies (`throw new Error("Not implemented — Phase 2")`) exactly as-is; they are Phase 5's responsibility (CRED-04, PAY-05).
  </action>
  <verify>
    <automated>npx vitest run convex/aiCredits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/aiCredits.test.ts` passes with 0 failures
    - `grep -c "MONTHLY_CREDIT_QUOTA = 100" convex/aiCredits.ts` returns exactly 1
    - `grep -c "throw new Error(\"Not implemented" convex/aiCredits.ts` returns exactly 2 (deductCredit and addCredits remain untouched stubs)
    - `grep -c "by_stripeSubscriptionId" convex/aiCredits.ts` returns at least 1
    - `grep -c "\.filter(" convex/aiCredits.ts` returns 0
  </acceptance_criteria>
  <done>resetCredits resolves clerkUserId via the subscriptions table's by_stripeSubscriptionId index, sets balance to exactly 100, is idempotent by stripeEventId, and never throws on an unresolvable stripeSubscriptionId; deductCredit/addCredits remain untouched Phase 1 stubs.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                                                               |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Convex action -> resetCredits | Only reachable via `ctx.runMutation(internal.aiCredits.resetCredits)` from `convex/stripeWebhooks.ts` (Plan 02-04), never a public client SDK call (D-01) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                  | Disposition | Mitigation Plan                                                                                                                                   |
| --------- | ---------------------- | ---------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-02   | Elevation of Privilege | `aiCredits`/`creditTransactions` table writes              | mitigate    | `resetCredits` stays `internalMutation`; `deductCredit`/`addCredits` (also `internalMutation`) are left untouched, preserving Phase 1's D-01 lock |
| T-02-04   | Tampering (replay)     | Duplicate `invoice.paid` delivery double-resetting credits | mitigate    | Idempotency check-and-mark inside the same mutation as the balance write (D-14), verified by this plan's replay test                              |

</threat_model>

<verification>
`npx vitest run convex/aiCredits.test.ts` passes with 0 failures covering: balance reset to 100 (insert and patch paths), idempotent replay, anomaly sentinel on unresolvable stripeSubscriptionId (no throw), and a single creditTransactions row per successful reset.
</verification>

<success_criteria>

- `resetCredits` resolves clerkUserId exclusively via the by_stripeSubscriptionId index, never via a direct argument
- ROADMAP SC3 (subscription_cycle invoice resets balance to 100) is automatically verified
- deductCredit/addCredits remain unimplemented, correctly deferred to Phase 5
  </success_criteria>

<output>
Create `.planning/phases/02-webhook-handler-convex-internal-mutations/02-03-SUMMARY.md` when done
</output>
