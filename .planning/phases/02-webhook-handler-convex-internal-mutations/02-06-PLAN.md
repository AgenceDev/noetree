---
phase: 02-webhook-handler-convex-internal-mutations
plan: 06
type: execute
wave: 4
depends_on: ["02-05"]
files_modified: [.env.local]
autonomous: false

requirements: [PAY-03, CRED-01]

user_setup:
  - service: vercel
    why: "Propagate INTERNAL_WEBHOOK_SECRET (D-02) to Development/Preview/Production scopes — no Vercel CLI/API token available in this environment (confirmed absent during Phase 1 Plan 01-04)"
    env_vars:
      - name: INTERNAL_WEBHOOK_SECRET
        source: "Same value Claude generates and sets locally in Task 1 of this plan — copy it into Vercel's 3 scopes"
    dashboard_config:
      - task: "Add INTERNAL_WEBHOOK_SECRET to Development, Preview, and Production environment scopes, marked Sensitive"
        location: "Vercel Project Settings -> Environment Variables"
  - service: convex
    why: "Set INTERNAL_WEBHOOK_SECRET identically on the staging/production Convex deployments (D-02 requires the Next.js and Convex values to match exactly per environment)"
    env_vars:
      - name: INTERNAL_WEBHOOK_SECRET
        source: "Same value as above"
    dashboard_config:
      - task: "Run `npx convex env set INTERNAL_WEBHOOK_SECRET <value> --prod` (or the staging-deployment equivalent) once staging/production Convex deployment targets are known"
        location: "Convex CLI, run against each non-local deployment"

must_haves:
  truths:
    - "INTERNAL_WEBHOOK_SECRET is set identically in local .env.local and the local Convex dev deployment, so a local stripe listen session can exercise the full webhook flow end-to-end"
    - "A live stripe trigger for checkout.session.completed creates a subscriptions row with correct clerkUserId in the real (non-test-harness) Convex dev deployment (ROADMAP SC1)"
    - "Replaying the same event via stripe trigger --skip-webhook-signing-secret-verification (or a second trigger with the same idempotency behavior) produces no duplicate row (ROADMAP SC2)"
    - "All 5 ROADMAP Phase 2 success criteria are explicitly confirmed by the user before the phase is considered complete"
  artifacts:
    - path: ".env.local"
      provides: "Local INTERNAL_WEBHOOK_SECRET value, matching the one set on the local Convex dev deployment"
      contains: "INTERNAL_WEBHOOK_SECRET="
  key_links: []
---

<objective>
Generate and propagate `INTERNAL_WEBHOOK_SECRET` (D-02) to the local environment (fully automatable — Convex CLI and Stripe CLI are both present in this environment) and to staging/production (requires human dashboard access, no Vercel CLI available), then run the ROADMAP Phase 2 end-to-end manual verification pass and obtain explicit user sign-off on all 5 success criteria.

Purpose: D-02 explicitly requires 3-environment propagation of this new secret, mirroring Phase 1's Wave 2 pattern. ROADMAP SC1/SC2/SC5 are explicitly manual-verification criteria per 02-VALIDATION.md's Manual-Only Verifications table (live `stripe listen`/`stripe trigger` against a running dev server, and a dev-server-restart-dependent wrong-secret test) — these cannot be replaced by the convex-test-based automated suite from Plans 02-01 through 02-05.
Output: `.env.local` has a real `INTERNAL_WEBHOOK_SECRET`; local Convex dev deployment has the matching value; user has explicitly signed off on all 5 ROADMAP Phase 2 success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-CONTEXT.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-VALIDATION.md
@.planning/phases/01-schema-infrastructure-foundation/01-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and set INTERNAL_WEBHOOK_SECRET locally</name>
  <read_first>
    .env.local (current file — existing STRIPE_* keys already present from Phase 1; do not overwrite any of them)
    .env.example (documents the INTERNAL_WEBHOOK_SECRET convention added in Plan 02-01)
  </read_first>
  <files>.env.local</files>
  <action>
    Generate a random 64-character hex value via `openssl rand -hex 32` (or Node's `crypto.randomBytes(32).toString("hex")` if `openssl` is unavailable). Append a new line `INTERNAL_WEBHOOK_SECRET=&lt;generated-value&gt;` to `.env.local` (do not modify any existing `STRIPE_*`/`CONVEX_*`/`NEXT_PUBLIC_*` lines already present). Run `npx convex env set INTERNAL_WEBHOOK_SECRET &lt;same-generated-value&gt;` to set the identical value on the local Convex dev deployment (this deployment target is already configured via `CONVEX_DEPLOYMENT` in `.env.local` from Phase 1).
  </action>
  <verify>
    <automated>npx convex env get INTERNAL_WEBHOOK_SECRET</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^INTERNAL_WEBHOOK_SECRET=" .env.local` returns exactly 1, with a non-empty value after the `=`
    - `npx convex env get INTERNAL_WEBHOOK_SECRET` prints the same value written to `.env.local`
    - All pre-existing keys in `.env.local` (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRO_PRICE_ID`, `STRIPE_TOPUP_PRICE_ID`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, `CONVEX_DEPLOYMENT`, `NEXT_PUBLIC_CONVEX_URL`, `NEXT_PUBLIC_CONVEX_SITE_URL`) are unchanged
  </acceptance_criteria>
  <done>.env.local and the local Convex dev deployment both have the identical INTERNAL_WEBHOOK_SECRET value; no pre-existing env vars were modified.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Propagate INTERNAL_WEBHOOK_SECRET to Vercel and staging/production Convex deployments</name>
  <read_first>.planning/phases/01-schema-infrastructure-foundation/01-04-PLAN.md (Task 1 — the identical pattern used for Phase 1's env var propagation, since no Vercel CLI/API token is available in this environment)</read_first>
  <files>(none — external dashboard configuration, no repo files modified)</files>
  <action>
    Claude has no Vercel CLI/API token in this environment (confirmed absent during Phase 1's equivalent task) and no way to target staging/production Convex deployments without their deployment identifiers. Present the how-to-verify steps below using the exact `INTERNAL_WEBHOOK_SECRET` value generated in Task 1, and wait for confirmation.
  </action>
  <what-built>Task 1 already generated and set `INTERNAL_WEBHOOK_SECRET` locally (in `.env.local` and the local Convex dev deployment). This step propagates the same value to staging and production.</what-built>
  <how-to-verify>
    1. In Vercel Project Settings -> Environment Variables, add `INTERNAL_WEBHOOK_SECRET` with the value generated in Task 1 to all 3 scopes (Development, Preview, Production), marked "Sensitive".
    2. For each of the staging and production Convex deployments, run `npx convex env set INTERNAL_WEBHOOK_SECRET &lt;same-value&gt; --prod` (or the equivalent flag/deployment selector for staging, per your Convex project's deployment naming) so the value matches exactly what Vercel now serves to the corresponding Next.js deployment (D-02 requires byte-identical values per environment).
  </how-to-verify>
  <resume-signal>Reply "done" once INTERNAL_WEBHOOK_SECRET is confirmed present in Vercel's 3 scopes and set on both staging and production Convex deployments, or describe any step you couldn't complete.</resume-signal>
  <verify>
    <manual>User has replied "done", confirming INTERNAL_WEBHOOK_SECRET is present across Vercel's 3 scopes and both non-local Convex deployments.</manual>
  </verify>
  <acceptance_criteria>
    - User has replied "done" confirming INTERNAL_WEBHOOK_SECRET exists in Vercel's Development/Preview/Production scopes
    - User has confirmed `npx convex env set` was run against both staging and production Convex deployments with the same value
  </acceptance_criteria>
  <done>INTERNAL_WEBHOOK_SECRET is present and identical across local, staging, and production for both the Next.js and Convex sides.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual end-to-end Stripe CLI verification and ROADMAP Phase 2 sign-off</name>
  <read_first>.planning/ROADMAP.md (Phase 2 — 5 success criteria, exact wording)</read_first>
  <files>(none — verification-only checkpoint, no files modified)</files>
  <action>
    Claude will attempt to automate as much of this as the sandboxed environment allows: start the dev server (`npm run dev` in the background), run `stripe listen --forward-to localhost:3000/api/webhooks/stripe` in the background, capture its printed signing secret and confirm it matches (or update) the local `STRIPE_WEBHOOK_SECRET`, then run `stripe trigger checkout.session.completed`, `stripe trigger customer.subscription.updated`, `stripe trigger customer.subscription.deleted`, `stripe trigger invoice.payment_failed`, and `stripe trigger invoice.paid --override invoice:billing_reason=subscription_cycle` (or the closest available override), checking the Convex dashboard/CLI for the expected row changes after each. The SC5 "wrong secret" scenario requires a dev-server restart with a deliberately corrupted `STRIPE_WEBHOOK_SECRET` — this specific sub-step is manual (per 02-VALIDATION.md's explicit "not unit-testable, requires restarting the dev server" classification) and Claude should present it as a step for the user to run if Claude's own sandboxed process management cannot restart the dev server with a modified env var mid-session.
  </action>
  <what-built>Across Plans 02-01 through 02-06: Vitest + convex-test toolchain, `by_stripeSubscriptionId` schema index, fully implemented `subscriptions.ts`/`aiCredits.ts` internalMutations (idempotent, atomic), the `stripeWebhooks.ts` dispatcher action (shared-secret gated), the Next.js webhook route (signature-verified, status-code-mapped), and `INTERNAL_WEBHOOK_SECRET` propagated to all 3 environments.</what-built>
  <how-to-verify>
    Confirm each of ROADMAP.md's 5 Phase 2 Success Criteria:
    1. `stripe listen --forward-to localhost:3000/api/webhooks/stripe` + `stripe trigger checkout.session.completed` creates a `subscriptions` row in the Convex dashboard with the correct `clerkUserId`.
    2. Triggering the same event a second time (or re-sending via the Stripe Dashboard's "Resend" button) produces no duplicate row.
    3. `stripe trigger invoice.paid --override invoice:billing_reason=subscription_cycle` resets the corresponding user's `aiCredits.balance` to 100.
    4. All 5 event types return 200 (visible in `stripe listen`'s terminal output showing `[200]` for each), and a deliberately malformed signature (e.g. `curl` with a bogus `Stripe-Signature` header) returns 400.
    5. Temporarily set `STRIPE_WEBHOOK_SECRET` to an incorrect value, restart the dev server, send a real Stripe test event, confirm the response is 400 (not 500), then restore the correct value and restart again.
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 ROADMAP Phase 2 success criteria are confirmed true, or list which are still failing.</resume-signal>
  <verify>
    <manual>User has typed "approved", confirming all 5 ROADMAP Phase 2 success criteria pass against a live stripe listen/stripe trigger session.</manual>
  </verify>
  <acceptance_criteria>
    - User has typed "approved" in this session
    - All 5 ROADMAP Phase 2 success criteria are explicitly confirmed, not inferred
  </acceptance_criteria>
  <done>User has typed "approved", confirming all 5 ROADMAP Phase 2 success criteria are true.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                              |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| Vercel/Convex dashboards -> deployed runtime | `INTERNAL_WEBHOOK_SECRET` set here becomes `process.env.INTERNAL_WEBHOOK_SECRET` in every corresponding deployment scope |
| Local `stripe listen` -> local dev server    | Live network path exercising the full real-code (non-mocked) flow for final sign-off                                     |

## STRIDE Threat Register

| Threat ID                       | Category               | Component                                             | Disposition | Mitigation Plan                                                                                                                                                                                 |
| ------------------------------- | ---------------------- | ----------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-05                         | Information Disclosure | `INTERNAL_WEBHOOK_SECRET` in Vercel/Convex dashboards | mitigate    | Task 2 instructs marking the Vercel entry "Sensitive"; value is never logged by Task 1's automated generation step                                                                              |
| T-02-01/T-02-02/T-02-03/T-02-04 | (full register)        | —                                                     | —           | All mitigated in Plans 02-04/02-05; this plan only performs live end-to-end confirmation that those mitigations work against a real Stripe CLI session, not a re-derivation of the threat model |

</threat_model>

<verification>
User has typed "approved" in Task 3, confirming all 5 ROADMAP Phase 2 success criteria pass against a live `stripe listen`/`stripe trigger` session.
</verification>

<success_criteria>

- INTERNAL_WEBHOOK_SECRET present and identical across local/staging/production
- All 5 ROADMAP Phase 2 success criteria explicitly confirmed by the user
- Phase 2 fully complete
  </success_criteria>

<output>
Create `.planning/phases/02-webhook-handler-convex-internal-mutations/02-06-SUMMARY.md` when done
</output>
