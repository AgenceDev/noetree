---
phase: 02-webhook-handler-convex-internal-mutations
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [package.json, vitest.config.ts, convex/schema.ts, .env.example]
autonomous: false

requirements: [PAY-03, CRED-01]

must_haves:
  truths:
    - "A Vitest-based test runner exists and can execute Convex-side unit tests before any Phase 2 mutation/action code is written"
    - "The subscriptions table has an index that lets invoice.paid/invoice.payment_failed handlers resolve a row by stripeSubscriptionId without a full table scan (D-08)"
    - "INTERNAL_WEBHOOK_SECRET is documented as a required env var alongside the existing Stripe secrets (D-02)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with edge-runtime environment required by convex-test"
      contains: 'environment: "edge-runtime"'
    - path: "convex/schema.ts"
      provides: "by_stripeSubscriptionId index on subscriptions table"
      contains: "by_stripeSubscriptionId"
    - path: ".env.example"
      provides: "INTERNAL_WEBHOOK_SECRET documented as a new required secret"
      contains: "INTERNAL_WEBHOOK_SECRET="
  key_links:
    - from: "convex/aiCredits.ts (Plan 02-03, later wave)"
      to: "convex/schema.ts subscriptions.by_stripeSubscriptionId"
      via: 'ctx.db.query("subscriptions").withIndex("by_stripeSubscriptionId", ...)'
      pattern: "by_stripeSubscriptionId"
---

<objective>
Install the Vitest + convex-test + @edge-runtime/vm test toolchain (the repo currently has zero working test runner — Jest is installed but unconfigured and unused), and land the two small schema/config prerequisites every later wave in this phase depends on: the new `by_stripeSubscriptionId` index (D-08) and the `INTERNAL_WEBHOOK_SECRET` env var documentation (D-02).

Purpose: Per 02-VALIDATION.md, this is the Wave 0 gap-closure the Nyquist rule requires — no Convex mutation/action logic in this phase can be automatically verified until this toolchain exists. The schema index and env var are cross-cutting prerequisites referenced by every subsequent plan's `<read_first>`.
Output: `npx vitest run` executes cleanly (zero test files yet, exits 0). `convex/schema.ts` has `by_stripeSubscriptionId` on `subscriptions`. `.env.example` documents `INTERNAL_WEBHOOK_SECRET`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-CONTEXT.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-RESEARCH.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-PATTERNS.md
@.planning/phases/02-webhook-handler-convex-internal-mutations/02-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify `vitest` package legitimacy before install (slopcheck SUS flag)</name>
  <read_first>.planning/phases/02-webhook-handler-convex-internal-mutations/02-RESEARCH.md (Package Legitimacy Audit table)</read_first>
  <files>(none — verification-only checkpoint, no files modified)</files>
  <action>
    02-RESEARCH.md's Package Legitimacy Audit flagged `vitest` `[SUS]` by slopcheck as "suspiciously close to 'vite' — could be a typosquat." Research already verified `vitest@4.1.10` against the npm registry and `github.com/vitest-dev/vitest` and concluded this is a name-similarity false positive, not a real typosquat. Per the Package Legitimacy Gate protocol, this checkpoint is mandatory before Task 2 installs the package and is never auto-approvable regardless of `workflow.auto_advance`.
  </action>
  <what-built>Nothing yet — this is a pre-install verification gate for a package flagged `[SUS]` by the automated legitimacy scanner during research.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/vitest and confirm the publisher is the `vitest-dev` org, current version is 4.x, and weekly download count is in the millions.
    2. Visit https://github.com/vitest-dev/vitest and confirm it is the official Vite-ecosystem test runner repo (not a fork/clone with a similar name).
    3. Confirm the two other packages in this task set (`convex-test`, `@edge-runtime/vm`) are unflagged (`[OK]`) per 02-RESEARCH.md's audit table — no separate check needed for these.
  </how-to-verify>
  <resume-signal>Reply "approved" once you've confirmed `vitest` on npmjs.com/github is the legitimate package, or "block" to halt installation.</resume-signal>
  <verify>
    <manual>User has replied "approved" in this session, confirming vitest's npm/GitHub identity matches the official vitest-dev project.</manual>
  </verify>
  <acceptance_criteria>
    - User has explicitly replied "approved" in this session before Task 2 runs `npm install --save-dev vitest`
  </acceptance_criteria>
  <done>User has typed "approved", confirming `vitest`'s npm/GitHub identity matches the official vitest-dev project.</done>
</task>

<task type="auto">
  <name>Task 2: Install Vitest test toolchain and wire config</name>
  <read_first>
    package.json (current scripts/devDependencies block, especially the existing "test" script which is a lint/format wrapper — do not overwrite it)
    .planning/phases/02-webhook-handler-convex-internal-mutations/02-RESEARCH.md (Validation Architecture -> Test Framework, Code Examples -> vitest.config.ts template)
  </read_first>
  <files>package.json, vitest.config.ts</files>
  <action>
    Run `npm install --save-dev vitest convex-test @edge-runtime/vm` (installs versions `vitest@4.1.10`, `convex-test@0.0.54`, `@edge-runtime/vm@5.0.0` per 02-RESEARCH.md's live npm-registry verification). Create `vitest.config.ts` at the repo root using `defineConfig` from `"vitest/config"` with `test.environment` set to the literal string `"edge-runtime"` and `test.server.deps.inline` set to an array containing the literal string `"convex-test"` (required by `convex-test`'s official setup docs — this is the `ActionCtx`/`MutationCtx` mock backend's required Vitest environment). Add a new npm script key `"test:unit": "vitest run"` to package.json's existing `scripts` block — do NOT rename, remove, or overwrite the existing `"test"` script (`"prettier --write . && eslint app/ --fix "`), which is unrelated tooling already wired into husky/lint-staged.
  </action>
  <verify>
    <automated>npx vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies contains `"vitest"`, `"convex-test"`, and `"@edge-runtime/vm"` keys
    - `package.json` scripts contains a `"test:unit"` key with value `"vitest run"`, and the pre-existing `"test"` key's value is unchanged (still starts with `"prettier --write"`)
    - `vitest.config.ts` exists at repo root and contains the literal string `edge-runtime`
    - `npx vitest run --passWithNoTests` exits with code 0
  </acceptance_criteria>
  <done>Vitest, convex-test, and @edge-runtime/vm are installed devDependencies; vitest.config.ts exists with the edge-runtime environment; `npm run test:unit` is a valid script; `npx vitest run --passWithNoTests` exits 0 with no test files present yet.</done>
</task>

<task type="auto">
  <name>Task 3: Add by_stripeSubscriptionId schema index (D-08) and INTERNAL_WEBHOOK_SECRET env var (D-02)</name>
  <read_first>
    convex/schema.ts (current subscriptions table definition, lines 40-51 — has only `.index("by_clerkUserId", ["clerkUserId"])`)
    .env.example (current file — existing STRIPE_WEBHOOK_SECRET block structure to match, lines 5-8)
    .planning/phases/02-webhook-handler-convex-internal-mutations/02-PATTERNS.md (schema.ts and .env.example patch sections)
  </read_first>
  <files>convex/schema.ts, .env.example</files>
  <action>
    In `convex/schema.ts`, change the `subscriptions` table's single chained `.index("by_clerkUserId", ["clerkUserId"])` call into two chained `.index(...)` calls on the same `defineTable({...})` object: keep `.index("by_clerkUserId", ["clerkUserId"])` first, then add `.index("by_stripeSubscriptionId", ["stripeSubscriptionId"])` — this exact multi-index chaining style already exists elsewhere in this same file (the `users` table chains `by_token`/`by_email`; the `shares` table chains 5 indexes). Do not touch `aiCredits`, `creditTransactions`, or `processedStripeEvents` — none of them need new indexes this phase (D-09).
    In `.env.example`, insert a new 5-line block immediately after the existing `STRIPE_WEBHOOK_SECRET=whsec_xxx` line (line 8) and before the `STRIPE_PRO_PRICE_ID` block (line 10): a comment header explaining this is a server-only secret shared identically between the Next.js environment and the Convex deployment (set via `npx convex env set INTERNAL_WEBHOOK_SECRET <value>`), a suggestion to generate it with `openssl rand -hex 32`, followed by the bare key `INTERNAL_WEBHOOK_SECRET=` with no placeholder value (matches the file's existing convention of secret keys having illustrative placeholder values like `whsec_xxx`/`sk_test_xxx` — for this internal-only secret, leave the value blank since there is no Stripe-issued format to illustrate).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `convex/schema.ts` subscriptions table has both `.index("by_clerkUserId", ["clerkUserId"])` and `.index("by_stripeSubscriptionId", ["stripeSubscriptionId"])` chained on the same `defineTable` call
    - `grep -c "by_stripeSubscriptionId" convex/schema.ts` returns exactly 1
    - `grep -c "^INTERNAL_WEBHOOK_SECRET=" .env.example` returns exactly 1
    - `npx tsc --noEmit` exits with code 0 (schema still type-checks)
    - `npx convex dev --once` deploys the updated schema without error
  </acceptance_criteria>
  <done>convex/schema.ts compiles and deploys with the new by_stripeSubscriptionId index; .env.example documents INTERNAL_WEBHOOK_SECRET immediately after STRIPE_WEBHOOK_SECRET.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                                                                                                |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Stripe -> Next.js route               | Untrusted internet-facing input arrives as a raw HTTP POST body + `Stripe-Signature` header; must be authenticated before any state change (implemented in Plan 02-05)                     |
| Next.js route -> Convex public action | The action is reachable by ANY client holding `NEXT_PUBLIC_CONVEX_URL`, not only the Next.js route — a second authentication layer (shared secret) is required (implemented in Plan 02-04) |
| npm registry -> local devDependencies | Installing `vitest`/`convex-test`/`@edge-runtime/vm` pulls third-party code into the build/test toolchain                                                                                  |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                            | Disposition        | Mitigation Plan                                                                                                                                                                                                                                    |
| --------- | ---------------------- | -------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-SC   | Tampering              | npm install of `vitest`/`convex-test`/`@edge-runtime/vm`             | mitigate           | Task 1 blocking-human checkpoint verifies `vitest`'s slopcheck `[SUS]` flag is a name-similarity false positive against npmjs.com/GitHub before Task 2 installs it; `convex-test`/`@edge-runtime/vm` are unflagged `[OK]` per 02-RESEARCH.md audit |
| T-02-05   | Information Disclosure | `INTERNAL_WEBHOOK_SECRET` value once set in Vercel/Convex dashboards | accept (this plan) | `.env.example` documents the var as SECRET/server-only with no client (`NEXT_PUBLIC_`) prefix; actual dashboard "Sensitive" marking happens in Plan 02-06                                                                                          |

</threat_model>

<verification>
`npx vitest run --passWithNoTests` exits 0; `npx tsc --noEmit` exits 0; `npx convex dev --once` deploys cleanly with the new index; `.env.example` documents `INTERNAL_WEBHOOK_SECRET`.
</verification>

<success_criteria>

- Vitest + convex-test + @edge-runtime/vm installed and configured, ready for Wave 1 TDD plans to add real test files against
- `convex/schema.ts` subscriptions table has `by_stripeSubscriptionId` index
- `.env.example` documents `INTERNAL_WEBHOOK_SECRET`
  </success_criteria>

<output>
Create `.planning/phases/02-webhook-handler-convex-internal-mutations/02-01-SUMMARY.md` when done
</output>
