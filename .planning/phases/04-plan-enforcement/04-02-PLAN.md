---
phase: 04-plan-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - providers/UpgradeModalProvider.tsx
  - components/UpgradeModal.tsx
  - app/[locale]/(app)/layout.tsx
  - messages/en.json
  - messages/fr.json
autonomous: true
requirements: [PLAN-04]
must_haves:
  truths:
    - "A global UpgradeModal can be opened from anywhere under the (app) route group via useUpgradeModal().open()"
    - "The modal shows the Free-limit copy and an Upgrade to Pro CTA that links to /pricing"
    - "The modal has a non-navigating Maybe later dismiss action"
  artifacts:
    - path: "providers/UpgradeModalProvider.tsx"
      provides: "cross-subtree open/close context + useUpgradeModal hook"
      contains: "useUpgradeModal"
    - path: "components/UpgradeModal.tsx"
      provides: "the Dialog with CTA to /pricing"
      contains: "/pricing"
    - path: "app/[locale]/(app)/layout.tsx"
      provides: "mounts UpgradeModalProvider around all (app) routes"
      contains: "UpgradeModalProvider"
    - path: "messages/en.json"
      provides: "PlanEnforcement i18n namespace"
      contains: "PlanEnforcement"
    - path: "messages/fr.json"
      provides: "PlanEnforcement i18n namespace (fr)"
      contains: "PlanEnforcement"
  key_links:
    - from: "components/UpgradeModal.tsx CTA"
      to: "/pricing"
      via: "next-intl Link href"
      pattern: 'href="/pricing"'
    - from: "app/[locale]/(app)/layout.tsx"
      to: "providers/UpgradeModalProvider.tsx"
      via: "wrap children"
      pattern: "UpgradeModalProvider"
---

<objective>
Build the shared upgrade-modal infrastructure: a global context provider (`UpgradeModalProvider` + `useUpgradeModal()`), the `UpgradeModal` Dialog component, mount the provider in the `(app)` layout, and add the `PlanEnforcement` i18n copy — so plan 03 can trigger one shared modal instance from two unrelated note-creation call sites.

Purpose: The two `createNote` call sites (`page.tsx` and `TreeProvider`/`useNoteMutations`) live in separate component subtrees with no shared parent state. A `HeaderProvider`-style global context is the established pattern for cross-subtree UI state (D-06/D-07/D-08 — reuse existing Dialog primitives, no toast, no new dependency).

Output: Two new files, an updated layout, and the `PlanEnforcement` namespace in both message catalogs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-plan-enforcement/04-CONTEXT.md
@.planning/phases/04-plan-enforcement/04-PATTERNS.md
@.planning/phases/04-plan-enforcement/04-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly, no exploration. -->

Global-context analog — providers/HeaderProvider.tsx: two-piece shape — `createContext<T | null>(null)`, a `Provider` component holding `useState` + `useMemo` value, and a `useX()` accessor hook that throws "must be used within a ..." when context is null. Copy this exact shape.

Layout mount point — app/[locale]/(app)/layout.tsx (full file, 22 lines): currently wraps children in `<HeaderProvider><SidebarProvider>...<main>{children}</main>...</SidebarProvider></HeaderProvider>`. This layout wraps BOTH notes/page.tsx and notes/[id]/page.tsx (which mounts TreeProvider), so a provider added here is visible to both plan-03 call sites.

Standalone Dialog analog — components/ShareDialog.tsx: `"use client"`, imports Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription from `@/components/ui/dialog`, Button from `@/components/ui/button`, controlled via `open`/`onOpenChange` props, `useTranslations("ShareDialog")` for copy.

Locale-aware Link — import from `@/i18n/routing`: `import { Link } from "@/i18n/routing";` (used at app/[locale]/(app)/notes/page.tsx line 34). Use `<Link href="/pricing">`.

Dialog footer/button composition analog — app/[locale]/(app)/notes/page.tsx lines 742-746: `DialogFooter` with `DialogClose asChild` wrapping a secondary `<Button>` for dismiss, plus a primary `<Button>`.

i18n format — messages/en.json is a flat top-level namespace map (Pricing, CheckoutSuccess, Notes, ShareDialog, ...). Pricing already contains `"upgradeCta": "Upgrade to Pro"`. Add a NEW top-level `PlanEnforcement` key. messages/fr.json mirrors the same keys in French.
</interfaces>

<copy_contract>
From 04-UI-SPEC.md — PlanEnforcement namespace (en):

- title: "You've reached the Free plan limit"
- body: "Free plans include up to 20 notes. Upgrade to Pro for unlimited notes and 100 AI credits a month."
- upgradeCta: "Upgrade to Pro"
- dismiss: "Maybe later"
  Provide French equivalents in messages/fr.json (same four keys).
  </copy_contract>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add PlanEnforcement i18n namespace to both catalogs</name>
  <files>messages/en.json, messages/fr.json</files>
  <read_first>
    - messages/en.json (flat namespace structure; Pricing namespace for tone/format)
    - messages/fr.json (French equivalents for tone/format)
    - .planning/phases/04-plan-enforcement/04-UI-SPEC.md (Copywriting Contract — exact en copy)
  </read_first>
  <action>
    Add a new top-level `PlanEnforcement` namespace object to messages/en.json with exactly these four keys and values: `title` = "You've reached the Free plan limit", `body` = "Free plans include up to 20 notes. Upgrade to Pro for unlimited notes and 100 AI credits a month.", `upgradeCta` = "Upgrade to Pro", `dismiss` = "Maybe later". Add the same `PlanEnforcement` namespace with the same four keys to messages/fr.json translated to French (e.g. title "Vous avez atteint la limite du plan Gratuit", body "Les plans Gratuits incluent jusqu'à 20 notes. Passez à Pro pour des notes illimitées et 100 crédits IA par mois.", upgradeCta "Passer à Pro", dismiss "Plus tard"). Keep both files valid JSON (do not break existing namespaces; match existing indentation).
  </action>
  <verify>
    <automated>node -e "const en=require('./messages/en.json'); const fr=require('./messages/fr.json'); const k=['title','body','upgradeCta','dismiss']; if(!en.PlanEnforcement||!fr.PlanEnforcement) throw new Error('missing PlanEnforcement'); for(const key of k){ if(!en.PlanEnforcement[key]) throw new Error('en missing '+key); if(!fr.PlanEnforcement[key]) throw new Error('fr missing '+key);} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - messages/en.json has a top-level `PlanEnforcement` object with keys title, body, upgradeCta, dismiss
    - en `PlanEnforcement.title` === "You've reached the Free plan limit"
    - en `PlanEnforcement.upgradeCta` === "Upgrade to Pro"
    - messages/fr.json has a `PlanEnforcement` object with the same four keys populated in French
    - Both files parse as valid JSON (the node verify command exits 0 printing `ok`)
  </acceptance_criteria>
  <done>Both catalogs contain a complete PlanEnforcement namespace and remain valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Create UpgradeModal Dialog component</name>
  <files>components/UpgradeModal.tsx</files>
  <read_first>
    - components/ShareDialog.tsx (standalone controlled-Dialog shape, useTranslations, imports)
    - components/ui/dialog.tsx (Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription/DialogFooter/DialogClose primitives)
    - app/[locale]/(app)/notes/page.tsx line 34 (Link import) and lines 742-746 (DialogFooter/DialogClose/Button composition)
    - .planning/phases/04-plan-enforcement/04-UI-SPEC.md (Color/Typography/Copywriting — primary CTA, secondary dismiss)
  </read_first>
  <action>
    Create components/UpgradeModal.tsx as a `"use client"` component. Props: `{ open: boolean; onOpenChange: (open: boolean) => void }` (controlled, same shape as ShareDialog — do NOT hold its own open state; the provider owns it). Import Dialog primitives from `@/components/ui/dialog`, `Button` from `@/components/ui/button`, `Link` from `@/i18n/routing`, and `useTranslations` from `next-intl` scoped to `"PlanEnforcement"`. Render `<Dialog open={open} onOpenChange={onOpenChange}>` → `<DialogContent>` → `<DialogHeader>` with `<DialogTitle>{t("title")}</DialogTitle>` and `<DialogDescription>{t("body")}</DialogDescription>` → `<DialogFooter>` containing: a secondary dismiss button `<DialogClose asChild><Button variant="secondary">{t("dismiss")}</Button></DialogClose>` (closes, no navigation), and a primary CTA `<Button asChild><Link href="/pricing">{t("upgradeCta")}</Link></Button>` (routes to the Phase 3 pricing page). Do not override DialogTitle/DialogDescription font classes (UI-SPEC: use their built-in defaults). Default export the component.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - components/UpgradeModal.tsx starts with `"use client"` and default-exports a component taking `{ open, onOpenChange }`
    - Imports Link from `@/i18n/routing` and uses `<Link href="/pricing">` inside a `<Button asChild>`
    - Uses `useTranslations("PlanEnforcement")` and renders t("title"), t("body"), t("upgradeCta"), t("dismiss")
    - Dismiss button is `variant="secondary"` wrapped in `DialogClose asChild` (no navigation)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>UpgradeModal renders the Free-limit copy with a working /pricing CTA and a non-navigating dismiss, driven entirely by controlled props.</done>
</task>

<task type="auto">
  <name>Task 3: Create UpgradeModalProvider and mount it in the (app) layout</name>
  <files>providers/UpgradeModalProvider.tsx, app/[locale]/(app)/layout.tsx</files>
  <read_first>
    - providers/HeaderProvider.tsx (createContext + provider + accessor-hook-that-throws shape to copy)
    - components/UpgradeModal.tsx (the modal this provider renders — created in Task 2)
    - app/[locale]/(app)/layout.tsx (current HeaderProvider/SidebarProvider nesting to extend)
  </read_first>
  <action>
    Create providers/UpgradeModalProvider.tsx as `"use client"`, mirroring HeaderProvider's two-piece shape. Define `interface UpgradeModalContextType { isOpen: boolean; open: () => void; close: () => void }`, `const UpgradeModalContext = createContext<UpgradeModalContextType | null>(null)`. Export `function UpgradeModalProvider({ children }: { children: ReactNode })` that holds `const [isOpen, setIsOpen] = useState(false)`, memoizes `{ isOpen, open: () => setIsOpen(true), close: () => setIsOpen(false) }` with `useMemo` keyed on `[isOpen]`, and renders `<UpgradeModalContext.Provider value={value}>{children}<UpgradeModal open={isOpen} onOpenChange={setIsOpen} /></UpgradeModalContext.Provider>` (import UpgradeModal from `@/components/UpgradeModal`). Export `function useUpgradeModal()` that reads the context and throws `new Error("useUpgradeModal must be used within an UpgradeModalProvider")` when null, else returns it. Then edit app/[locale]/(app)/layout.tsx: import `UpgradeModalProvider` from `@/providers/UpgradeModalProvider` and wrap the existing `<HeaderProvider>...</HeaderProvider>` subtree with `<UpgradeModalProvider>` as an additional outer wrapper (order relative to HeaderProvider does not matter — they are independent). Leave SidebarProvider/AppSidebar/Header/main untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - providers/UpgradeModalProvider.tsx exports both `UpgradeModalProvider` and `useUpgradeModal`
    - useUpgradeModal throws `"useUpgradeModal must be used within an UpgradeModalProvider"` when context is null
    - The provider renders `<UpgradeModal open={isOpen} onOpenChange={setIsOpen} />` inside its context provider
    - app/[locale]/(app)/layout.tsx imports and wraps children with `UpgradeModalProvider`
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>useUpgradeModal().open() is callable from any (app) route and opens the single shared UpgradeModal instance.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                                                                                                                                             |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| n/a (client-only UI) | This plan adds presentational UI + i18n only. No new trust boundary is crossed — the modal displays static copy and a same-app navigation link. The security-critical enforcement is entirely in plan 01 (server-side). |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                                             |
| --------- | ---------------------- | -------------------- | ----------- | ----------------------------------------------------------------------------------------------------------- |
| T-04-04   | Information Disclosure | UpgradeModal content | accept      | Modal shows only public plan copy and a /pricing link — no PII, no secrets, no user-specific data rendered. |

</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` — new provider/modal/layout type-check.
- `node -e` JSON assertion — PlanEnforcement present in both catalogs.
- Manual grep: `useUpgradeModal` exported from provider; `href="/pricing"` present in UpgradeModal.tsx; `UpgradeModalProvider` present in layout.tsx.
</verification>

<success_criteria>

- A shared UpgradeModal is mountable and openable from anywhere under `(app)` (D-07 mechanism).
- Modal copy and CTA match the UI-SPEC copy contract and link to /pricing (D-06).
- No new dependency, no toast, reuses existing Dialog primitives (D-08).
  </success_criteria>

<output>
Create `.planning/phases/04-plan-enforcement/04-02-SUMMARY.md` when done, recording the exact hook name (`useUpgradeModal`) and its `.open()` method so plan 03 wires against the identical API.
</output>
