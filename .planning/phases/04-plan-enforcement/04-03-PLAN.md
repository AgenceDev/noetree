---
phase: 04-plan-enforcement
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - hooks/useNoteMutations.ts
  - app/[locale]/(app)/notes/page.tsx
autonomous: true
requirements: [PLAN-04]
must_haves:
  truths:
    - "When createNote fails with NOTE_LIMIT_REACHED at the child-note call site, the upgrade modal opens"
    - "When createNote fails with NOTE_LIMIT_REACHED at the root New note call site, the upgrade modal opens"
    - "Any other createNote error keeps the existing silent optimistic-revert behavior"
  artifacts:
    - path: "hooks/useNoteMutations.ts"
      provides: "child-note createNote onError opens upgrade modal on limit error"
      contains: "NOTE_LIMIT_REACHED"
    - path: "app/[locale]/(app)/notes/page.tsx"
      provides: "root createNote onError opens upgrade modal on limit error"
      contains: "NOTE_LIMIT_REACHED"
  key_links:
    - from: "hooks/useNoteMutations.ts createNote onError"
      to: "useUpgradeModal().open()"
      via: "err.message === NOTE_LIMIT_REACHED guard"
      pattern: "useUpgradeModal"
    - from: "app/[locale]/(app)/notes/page.tsx createNote onError"
      to: "useUpgradeModal().open()"
      via: "err.message === NOTE_LIMIT_REACHED guard"
      pattern: "useUpgradeModal"
---

<objective>
Wire the two `createNote` `onError` handlers so that a `NOTE_LIMIT_REACHED` error (thrown by plan 01's server gate) opens the shared upgrade modal (plan 02's `useUpgradeModal()`), while preserving the existing silent optimistic-revert behavior for every other error.

Purpose: This connects the server enforcement to the user-facing prompt across BOTH note-creation call sites (D-07). Per the pattern-mapper's correction, the child-note mutation lives in `hooks/useNoteMutations.ts` (invoked via `providers/TreeProvider.tsx`), NOT in `components/NotesTree.tsx` — so `NotesTree.tsx` needs no changes.

Output: Both `onError` handlers gain a limit-error branch that calls `useUpgradeModal().open()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-plan-enforcement/04-CONTEXT.md
@.planning/phases/04-plan-enforcement/04-PATTERNS.md
@.planning/phases/04-plan-enforcement/04-01-SUMMARY.md
@.planning/phases/04-plan-enforcement/04-02-SUMMARY.md

<interfaces>
<!-- Contracts from plans 01 and 02. Use directly. -->

Error contract (plan 01) — convex/notes.ts createNote throws `new Error("NOTE_LIMIT_REACHED")` when a Free user is at the 20-note cap. Client detects via `err.message === "NOTE_LIMIT_REACHED"`. (Confirm exact string in 04-01-SUMMARY.md before wiring.)

Modal API (plan 02) — providers/UpgradeModalProvider.tsx exports `useUpgradeModal()` returning `{ isOpen, open, close }`. Call `useUpgradeModal().open()` to show the modal. The provider is mounted in app/[locale]/(app)/layout.tsx, so both call sites below are within its subtree.

Child-note call site — hooks/useNoteMutations.ts, the `createNote` useMutation (lines 52-85). Its `onError` (lines 75-79) currently only reverts optimistic tree state. This hook is called once, from providers/TreeProvider.tsx line 137 (`useNoteMutations(noteId, setSelectedNoteId)`), which is inside the (app) layout subtree.

Root call site — app/[locale]/(app)/notes/page.tsx, the `createNote` useMutation (lines 637-671). Its `onError` (lines 662-670) currently only reverts optimistic trees state.

onError signature at both sites: `(err, variables, context) => { if (context?.previousX) queryClient.setQueryData(queryKey, context.previousX); }` — `err` is typed `Error`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open upgrade modal from the child-note createNote onError</name>
  <files>hooks/useNoteMutations.ts</files>
  <read_first>
    - hooks/useNoteMutations.ts lines 1-85 (imports + createNote mutation + its onError at 75-79)
    - providers/UpgradeModalProvider.tsx (useUpgradeModal API — from plan 02)
    - .planning/phases/04-plan-enforcement/04-01-SUMMARY.md (confirm exact NOTE_LIMIT_REACHED string)
    - .planning/phases/04-plan-enforcement/04-CONTEXT.md (D-07)
  </read_first>
  <action>
    Import `useUpgradeModal` from `@/providers/UpgradeModalProvider` at the top of hooks/useNoteMutations.ts. Inside the `useNoteMutations` hook body (before the mutation definitions, following React hook rules), call `const upgradeModal = useUpgradeModal();`. In the `createNote` mutation's `onError` handler (currently lines 75-79), PRESERVE the existing optimistic-revert block unchanged (`if (context?.previousTree) queryClient.setQueryData(queryKey, context.previousTree);`) and ADD, after the revert, a guarded branch: `if (err.message === "NOTE_LIMIT_REACHED") { upgradeModal.open(); }`. Do not modify the other mutations' onError handlers (updateNoteTitle, deleteNote, etc.) — only `createNote`. Because `useNoteMutations` is only ever called from TreeProvider (inside the (app) layout that mounts UpgradeModalProvider per plan 02), `useUpgradeModal()` will resolve without throwing.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useNoteMutations.ts imports `useUpgradeModal` from `@/providers/UpgradeModalProvider`
    - The hook body calls `useUpgradeModal()` once and stores the result
    - The `createNote` onError still contains the `context?.previousTree` revert AND adds `if (err.message === "NOTE_LIMIT_REACHED")` opening the modal
    - No other mutation's onError in the file is modified
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Child-note creation hitting the server limit opens the upgrade modal; other errors still silently revert.</done>
</task>

<task type="auto">
  <name>Task 2: Open upgrade modal from the root createNote onError</name>
  <files>app/[locale]/(app)/notes/page.tsx</files>
  <read_first>
    - app/[locale]/(app)/notes/page.tsx lines 637-671 (createNote mutation + onError at 662-670) and the component's existing imports/hook-call region near the top
    - providers/UpgradeModalProvider.tsx (useUpgradeModal API — from plan 02)
    - .planning/phases/04-plan-enforcement/04-01-SUMMARY.md (confirm exact NOTE_LIMIT_REACHED string)
    - .planning/phases/04-plan-enforcement/04-CONTEXT.md (D-07)
  </read_first>
  <action>
    Import `useUpgradeModal` from `@/providers/UpgradeModalProvider` in app/[locale]/(app)/notes/page.tsx. Inside the page component body, alongside the other hook calls, add `const upgradeModal = useUpgradeModal();`. In the root `createNote` mutation's `onError` handler (currently lines 662-670), PRESERVE the existing optimistic-revert block unchanged (`if (context?.previousTrees) queryClient.setQueryData(queryKey, context.previousTrees);`) and ADD, after the revert, a guarded branch: `if (err.message === "NOTE_LIMIT_REACHED") { upgradeModal.open(); }`. Do not change the other mutations (updateNoteTitle, deleteNote) or the onMutate/onSuccess logic. The page renders under the (app) layout that mounts UpgradeModalProvider (plan 02), so the hook resolves without throwing.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - app/[locale]/(app)/notes/page.tsx imports `useUpgradeModal` from `@/providers/UpgradeModalProvider`
    - The component body calls `useUpgradeModal()` once
    - The root `createNote` onError still contains the `context?.previousTrees` revert AND adds `if (err.message === "NOTE_LIMIT_REACHED")` opening the modal
    - No other mutation handler in the file is modified
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Root New note creation hitting the server limit opens the upgrade modal; other errors still silently revert.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                                                                                                                                                                                                           |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| server error → client onError | The client trusts the error message string to decide whether to show the upgrade modal. This is a UX signal only — the actual note-creation block already happened server-side (plan 01), so a spoofed/absent message cannot let a Free user exceed the cap; it can at worst fail to show the prompt. |

## STRIDE Threat Register

| Threat ID | Category  | Component                   | Disposition | Mitigation Plan                                                                                                                                                                                                         |
| --------- | --------- | --------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-05   | Tampering | client error-message branch | accept      | Keying the modal off `err.message === "NOTE_LIMIT_REACHED"` is UX-only; the security boundary is the server gate (T-04-01). No client tampering can bypass the note cap — it is enforced in the mutation before insert. |

</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` — both wired files type-check.
- Manual grep: `useUpgradeModal` and `NOTE_LIMIT_REACHED` present in both hooks/useNoteMutations.ts and app/[locale]/(app)/notes/page.tsx.
- Existing revert lines (`context?.previousTree` / `context?.previousTrees`) still present in each onError.
</verification>

<success_criteria>

- Both note-creation call sites open the shared upgrade modal on NOTE_LIMIT_REACHED (D-07).
- Non-limit errors retain the pre-existing silent-revert behavior (no regression).
- components/NotesTree.tsx unchanged (per pattern-mapper correction).
  </success_criteria>

<output>
Create `.planning/phases/04-plan-enforcement/04-03-SUMMARY.md` when done.
</output>
