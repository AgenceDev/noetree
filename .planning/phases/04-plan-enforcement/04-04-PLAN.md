---
phase: 04-plan-enforcement
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified: []
autonomous: false
requirements: [PLAN-02, PLAN-03, PLAN-04]
must_haves:
  truths:
    - "A Free user creating their 21st note sees the upgrade modal with the Upgrade to Pro CTA"
    - "The CTA navigates to /pricing"
    - "The 21st note is not created (list does not gain a 21st note)"
    - "A Pro user can create beyond 20 notes without the modal appearing"
  artifacts: []
  key_links:
    - from: "Free user 21st createNote"
      to: "upgrade modal visible + /pricing CTA"
      via: "end-to-end UI behavior"
      pattern: "manual"
---

<objective>
End-to-end human verification of the full phase behavior against the running app: the Free-tier cap blocks the 21st note server-side, the upgrade modal appears with the correct copy and a working `/pricing` CTA, and a Pro user is unaffected.

Purpose: Plans 01-03 are individually verified by tsc/vitest, but ROADMAP SC4 ("an upgrade prompt appears in the UI with a clear CTA") and the UI-SPEC checker sign-off require a human to observe the actual rendered behavior across both note-creation call sites. This is the phase's UI-hint acceptance gate.

Output: Human confirmation (or a punch list of issues) covering SC1-SC4 in the live app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-plan-enforcement/04-UI-SPEC.md
@.planning/phases/04-plan-enforcement/04-01-SUMMARY.md
@.planning/phases/04-plan-enforcement/04-02-SUMMARY.md
@.planning/phases/04-plan-enforcement/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gate</name>
  <files></files>
  <read_first>
    - .planning/phases/04-plan-enforcement/04-01-SUMMARY.md
    - .planning/phases/04-plan-enforcement/04-02-SUMMARY.md
    - .planning/phases/04-plan-enforcement/04-03-SUMMARY.md
  </read_first>
  <action>
    Before requesting human verification, run the full automated suite to confirm nothing regressed and the enforcement tests pass: run the convex enforcement tests and a project type-check. This gate ensures the human is verifying a build that already passes machine checks — do not proceed to the checkpoint if either command fails; instead surface the failure for a gap-closure pass.
  </action>
  <verify>
    <automated>npx vitest run convex/notes.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/notes.test.ts` exits 0 (all enforcement tests pass)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Automated suite is green; ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Server-side Free-tier 20-note enforcement in Convex `createNote`, plus a shared upgrade modal that opens from both note-creation call sites (root "New note" dialog and in-tree child-note creation) when the limit is hit, with an "Upgrade to Pro" CTA linking to /pricing.
  </what-built>
  <how-to-verify>
    1. Start the app: `npm run dev` (Next dev on https://localhost:3000 + convex dev), sign in as a FREE test user (no active subscription).
    2. Ensure the account is at the cap: create notes until the user owns 20 total notes (root + children count together). Tip: you can also seed via the Convex dashboard.
    3. Root call site: click "New note", submit a title. Expected: the note is NOT added to the list, and the upgrade modal appears showing title "You've reached the Free plan limit", the body copy about 20 notes / 100 AI credits, an "Upgrade to Pro" primary button, and a "Maybe later" dismiss button.
    4. Click "Upgrade to Pro". Expected: navigates to /pricing (the Phase 3 pricing page).
    5. Reopen a note, go into the tree, and add a CHILD note via the in-tree "+"/add-child affordance. Expected: the SAME upgrade modal appears (child-note call site also gated), and the child is not created.
    6. Click "Maybe later". Expected: modal closes with no navigation and no note created.
    7. Pro path: sign in as (or switch the test user to) a user whose subscription status is "active". Create notes 21, 22, 50. Expected: all succeed, NO modal appears.
    8. Bypass check (optional but recommended for SC1): with the Free user at 20 notes, call `api.notes.createNote` directly from the Convex dashboard function runner. Expected: it throws NOTE_LIMIT_REACHED and no row is inserted.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe which step(s) failed (with what you saw) so a gap-closure plan can be created.</resume-signal>
</task>

</tasks>

<verification>
- Automated: enforcement tests + tsc green (Task 1).
- Human: SC1 (21st blocked), SC2 (Pro unlimited), SC3 (no-row user blocked at 21), SC4 (modal + CTA) confirmed live across both call sites.
</verification>

<success_criteria>

- All four ROADMAP Phase 4 success criteria observed in the running app.
- UI-SPEC copy (title, body, CTA, dismiss) matches what renders.
- /pricing CTA navigates correctly; dismiss closes without side effects.
  </success_criteria>

<output>
Create `.planning/phases/04-plan-enforcement/04-04-SUMMARY.md` when done, recording the human verification result (approved or issue list) and the UI-SPEC checker dimensions confirmed.
</output>
