---
phase: 01-schema-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [package.json, package-lock.json, middleware.ts]
autonomous: false

requirements: [PLAN-05]

must_haves:
  truths:
    - "`stripe` and `@stripe/stripe-js` packages are installed and importable (ROADMAP Phase 1 SC-3)"
    - "Clerk middleware excludes /api/webhooks/** from authentication protection while /dashboard remains protected (ROADMAP Phase 1 SC-4)"
  artifacts:
    - path: "package.json"
      provides: "stripe@22.3.0, @stripe/stripe-js@9.9.0 dependencies; @clerk/nextjs bumped to >=6.39.2"
      contains: "\"stripe\":"
    - path: "middleware.ts"
      provides: "isPublicRoute matcher excluding /api/webhooks/(.*) from auth.protect()"
      contains: "isPublicRoute"
  key_links:
    - from: "middleware.ts"
      to: "clerkMiddleware auth.protect() call"
      via: "conditional gate: !isPublicRoute(req) && isProtectedRoute(req)"
      pattern: "isPublicRoute\\(req\\)"
---

<objective>
Install the Stripe SDKs (`stripe`, `@stripe/stripe-js`), patch `middleware.ts` to exclude `/api/webhooks/**` from Clerk authentication per D-06, and bump `@clerk/nextjs` to a patched version to close a disclosed critical middleware-bypass CVE while this phase is already the one touching `middleware.ts`.

Purpose: Phase 2's webhook handler needs the `stripe` package for signature verification; Phase 3's checkout page needs `@stripe/stripe-js` for the client-side redirect; and the webhook route must be reachable without a Clerk session (Stripe cannot present one) while `/dashboard` stays protected.
Output: `stripe`/`@stripe/stripe-js` installed and importable; `@clerk/nextjs` patched to `^6.39.2`; `middleware.ts` excludes webhook routes from auth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-schema-infrastructure-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump @clerk/nextjs (CVE fix) and install Stripe packages</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    package.json (current — dependencies block, alphabetical ordering convention)
  </read_first>
  <action>
    Edit the `dependencies` block in package.json: change the existing `"@clerk/nextjs": "^6.12.4"` line to `"@clerk/nextjs": "^6.39.2"` — this patches CVE-2026-41248 / GHSA-vqx2-fgx2-5wq9 (CVSS 9.1, critical middleware route-gating bypass in `createRouteMatcher`), same major version, non-breaking. Run `npm install` to apply the bump and regenerate package-lock.json.

    Then run `npm install stripe @stripe/stripe-js` to add `stripe@22.3.0` (server-side SDK) and `@stripe/stripe-js@9.9.0` (browser Stripe.js loader) as new dependency entries — insert alphabetically per the existing convention (`@stripe/stripe-js` slots after the `@radix-ui/*` block, before `@tiptap/*`; `stripe` slots before `tailwind-merge`). Both packages are pre-approved in RESEARCH.md's Package Legitimacy Audit (`[OK]` disposition, 13.87M/8.75M weekly downloads, official `stripe` org repos, no `postinstall` scripts) — no legitimacy checkpoint is required for these installs.
  </action>
  <verify>
    <automated>npm ls @clerk/nextjs && node -e "require('stripe')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @clerk/nextjs` reports a version `>=6.39.2` (not `6.12.4`)
    - package.json `dependencies` block contains `"stripe"` and `"@stripe/stripe-js"` keys
    - `node -e "require('stripe')"` exits 0 (CJS import smoke test, SC-3)
    - `npx tsc --noEmit` passes with no new type errors introduced
  </acceptance_criteria>
  <done>@clerk/nextjs is patched to >=6.39.2, stripe and @stripe/stripe-js are installed and importable, npm install completes cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Patch middleware.ts to exclude /api/webhooks/** from Clerk auth</name>
  <files>middleware.ts</files>
  <read_first>
    middleware.ts (current, full file — 14 lines)
  </read_first>
  <action>
    Add a second `createRouteMatcher` call named `isPublicRoute` matching `["/api/webhooks/(.*)"]`, declared directly below the existing `isProtectedRoute` declaration. Change the `clerkMiddleware` callback body from `if (isProtectedRoute(req)) await auth.protect();` to only call `auth.protect()` when `!isPublicRoute(req) && isProtectedRoute(req)` is true, per D-06 (verbatim pattern locked in CONTEXT.md). Leave `export const config` (the `matcher` array) byte-for-byte unchanged — it already covers `/api` routes broadly. This is additive: `/dashboard` protection behavior must remain identical for every non-webhook path.

    After patching, verify the behavior with a local dev server: start `npm run dev` (or `next dev`) in the background on its default port, `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/dashboard` should return a redirect status (`307` or `302`, unauthenticated), `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/webhooks/probe` should return `404` (no matching route handler exists yet, but critically NOT a `307`/`302` redirect to sign-in — confirming `auth.protect()` was skipped for the excluded path). Stop the background dev server once both checks complete.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isPublicRoute" middleware.ts` returns at least 2 (declaration + usage in the condition)
    - `grep -c "/api/webhooks/(.\\*)" middleware.ts` returns 1 (exact route matcher string)
    - `export const config` block is unchanged (diff shows no modified lines in that block)
    - `npx tsc --noEmit` passes for middleware.ts
    - Background dev-server curl check: `/dashboard` unauthenticated returns `307`/`302`; `/api/webhooks/probe` unauthenticated returns `404` (not `307`/`302`)
  </acceptance_criteria>
  <done>middleware.ts is patched per D-06; /dashboard remains protected; /api/webhooks/** bypasses Clerk auth (confirmed via curl status codes); config matcher untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm CVE fix and middleware behavior</name>
  <files>middleware.ts, package.json (review only — no changes in this task)</files>
  <action>
    Present the Task 1/2 automated verification results (clerk version bump, curl redirect/404 checks) to the user for confirmation, given RESEARCH.md Pitfall 1 flags this as a security-sensitive dependency bump warranting human sign-off. No code changes in this task.
  </action>
  <what-built>
    @clerk/nextjs bumped from 6.12.4 to >=6.39.2, fixing CVE-2026-41248 (critical middleware route-gating bypass, CVSS 9.1). middleware.ts patched to exclude /api/webhooks/** from Clerk auth while preserving /dashboard protection. Automated curl checks in Task 2 confirmed /dashboard redirects unauthenticated requests (307/302) and /api/webhooks/probe does not (404).
  </what-built>
  <how-to-verify>
    1. Run `npm ls @clerk/nextjs` and confirm the resolved version is 6.39.2 or higher.
    2. Run `npm run dev` locally, open an incognito/private window, visit `/dashboard`, and confirm you're redirected to Clerk sign-in.
    3. In the same session, visit any `/api/webhooks/*` path and confirm you get a 404 rather than a sign-in redirect.
    4. Review the `middleware.ts` diff to confirm no other behavior changed (config matcher untouched).
  </how-to-verify>
  <verify>
    <manual>Human walks through the 4 how-to-verify steps above and confirms each passes.</manual>
  </verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
  <done>User has typed "approved", confirming the CVE fix is verified and middleware behavior matches the automated checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → Next.js middleware | Every incoming request is evaluated by `clerkMiddleware` before reaching a route handler |
| npm registry → local node_modules | New third-party code (`stripe`, `@stripe/stripe-js`) executed in the build/runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | `middleware.ts` (`createRouteMatcher` gating) | mitigate | Bump `@clerk/nextjs` to `^6.39.2` in package.json (patched build) — CVE-2026-41248 is a library-level bypass, not fixable by application code alone; verified via `npm ls @clerk/nextjs` in Task 1's acceptance criteria |
| T-01-SC | Tampering | `npm install stripe @stripe/stripe-js` | mitigate | RESEARCH.md Package Legitimacy Audit already confirms slopcheck `[OK]` for both packages (official `stripe` org repos, 13.87M/8.75M weekly downloads, no `postinstall` scripts) — no `[ASSUMED]`/`[SUS]` verdict, so no blocking-human install checkpoint is required per the legitimacy gate rules |
| T-01-05 | Spoofing | `/api/webhooks/**` bypassing Clerk auth entirely | accept | Intentional per D-06 — Stripe cannot present a Clerk session, so exclusion is correct. The actual authenticity control is Stripe signature verification (`stripe.webhooks.constructEvent`) added in Phase 2's webhook handler — out of this phase's scope, forward-pointer only |

</threat_model>

<verification>
`npm ls @clerk/nextjs` reports `>=6.39.2`. `node -e "require('stripe')"` exits 0. `npx tsc --noEmit` passes. Curl checks against a local dev server confirm `/dashboard` redirects unauthenticated traffic while `/api/webhooks/*` does not. Human checkpoint confirms the security-sensitive dependency bump behaves as expected.
</verification>

<success_criteria>
- `stripe` and `@stripe/stripe-js` installed and importable (ROADMAP SC-3)
- `@clerk/nextjs` patched to `>=6.39.2`, closing CVE-2026-41248
- `/api/webhooks/**` bypasses Clerk auth while `/dashboard` remains protected (ROADMAP SC-4)
</success_criteria>

<output>
Create `.planning/phases/01-schema-infrastructure-foundation/01-02-SUMMARY.md` when done
</output>
