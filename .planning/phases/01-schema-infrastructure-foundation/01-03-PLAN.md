---
phase: 01-schema-infrastructure-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore, .env.example, .env.local]
autonomous: false

requirements: [PLAN-05]

user_setup:
  - service: stripe
    why: "Test-mode API keys and product/price creation for the Free/Pro monetization model (D-03, D-05)"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys (test mode)"
      - name: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard -> Developers -> API keys (test mode)"
    dashboard_config:
      - task: "Create a Stripe account (if none exists) and switch to Test mode"
        location: "https://dashboard.stripe.com/register"

must_haves:
  truths:
    - "A .env.example documenting all 5 required vars exists and is trackable in git (D-04)"
    - "Real Stripe test-mode Price IDs exist for the Pro subscription and Credits Top-up products (D-03)"
  artifacts:
    - path: ".env.example"
      provides: "Documented template for all 5 required env vars with secret-vs-public annotations"
      contains: "STRIPE_SECRET_KEY"
    - path: ".gitignore"
      provides: "Negation of the blanket .env* rule so .env.example is trackable"
      contains: "!.env.example"
    - path: ".env.local"
      provides: "Real captured STRIPE_SECRET_KEY, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY, STRIPE_PRO_PRICE_ID, STRIPE_TOPUP_PRICE_ID for local dev"
      contains: "STRIPE_PRO_PRICE_ID"
  key_links: []
---

<objective>
Document all 5 required Stripe/Clerk-adjacent environment variables in a committable `.env.example` (fixing the `.gitignore` blanket rule that would otherwise silently swallow it), then use the Stripe API to create the Pro and Top-up test-mode products/prices and capture their real Price IDs into `.env.local`.

Purpose: D-04/D-05 require a documented, committed env var contract before any later phase writes checkout/webhook code against these vars. D-03 requires real `price_xxx` values (not placeholders) before Phase 1 is complete — Phase 3's checkout flow references `STRIPE_PRO_PRICE_ID`/`STRIPE_TOPUP_PRICE_ID` directly.
Output: `.gitignore` patched, `.env.example` created and committable, 2 Stripe test-mode products + prices created via API, real Price IDs captured in `.env.local`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-schema-infrastructure-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch .gitignore and create .env.example</name>
  <files>.gitignore, .env.example</files>
  <read_first>
    .gitignore (current — lines 33-34, the "# env files" block with the blanket `.env*` rule)
  </read_first>
  <action>
    Insert a new line `!.env.example` immediately after the existing `.env*` line (currently line 34, inside the "# env files (can opt-in for committing if needed)" block) — without this negation, `git add .env.example` will silently succeed while the file remains untracked (RESEARCH.md Pitfall 2).

    Create `.env.example` at the repo root with exactly these 5 variables and inline comments, placeholder values only (never real captured secrets):
    - `STRIPE_SECRET_KEY=sk_test_xxx` — comment: secret, server-only, one value per environment, from Stripe Dashboard -> Developers -> API keys (test mode).
    - `STRIPE_WEBHOOK_SECRET=whsec_xxx` — comment: secret, server-only, DISTINCT value per environment (local via `stripe listen --forward-to localhost:3000/api/webhooks/stripe`; staging/production via Stripe Dashboard -> Webhooks -> [endpoint] -> Signing secret).
    - `STRIPE_PRO_PRICE_ID=price_xxx` and `STRIPE_TOPUP_PRICE_ID=price_xxx` — comment: created once in test mode, can be shared across environments.
    - `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx` — comment: safe to expose in the client bundle, must keep the `NEXT_PUBLIC_` prefix (Next.js inlines this at build time).
  </action>
  <verify>
    <automated>git check-ignore -v .env.example; git status --short | grep .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains a line `!.env.example` immediately following the `.env*` line
    - `git check-ignore -v .env.example` exits with code 1 (no output — confirms the file is no longer ignored)
    - `git status --short` shows `.env.example` as `??` (untracked, not absent/ignored)
    - `grep -c -E "^(STRIPE_SECRET_KEY|STRIPE_WEBHOOK_SECRET|STRIPE_PRO_PRICE_ID|STRIPE_TOPUP_PRICE_ID|NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY)=" .env.example` equals 5
    - `grep -c "^NEXT_PUBLIC_" .env.example` equals 1 (confirms no `NEXT_PUBLIC_` prefix leaked onto `STRIPE_SECRET_KEY` or `STRIPE_WEBHOOK_SECRET`, per D-05)
  </acceptance_criteria>
  <done>.gitignore negates .env.example, .env.example exists with all 5 vars documented and correctly annotated secret-vs-public, no real secret values present.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Retrieve Stripe test-mode API keys</name>
  <files>.env.local</files>
  <action>
    Claude has no Stripe account access and no Stripe CLI installed in this environment (confirmed: `stripe` binary not found on PATH) — retrieving live API key values requires the user's Stripe Dashboard login. Present the how-to-verify steps below to the user and wait for confirmation or the pasted values before proceeding to Task 3.
  </action>
  <what-built>
    Claude has no Stripe account access and no Stripe CLI installed in this environment (confirmed: `stripe` binary not found on PATH) — retrieving live API key values requires your Stripe Dashboard login. Phase 1 needs a test-mode STRIPE_SECRET_KEY and NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY before Task 3 can programmatically create the Pro and Top-up products (D-03).
  </what-built>
  <how-to-verify>
    1. If you don't already have a Stripe account, create one at https://dashboard.stripe.com/register.
    2. Ensure you're in Test mode (toggle in the top-right of the dashboard).
    3. Go to Developers -> API keys.
    4. Copy the "Secret key" (starts with `sk_test_`) and the "Publishable key" (starts with `pk_test_`).
    5. Add both to `.env.local` at the repo root as `STRIPE_SECRET_KEY=sk_test_...` and `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...` (create the lines if they don't exist — `.env.local` is already git-ignored, safe for real secrets).
  </how-to-verify>
  <verify>
    <manual>grep -c -E "^(STRIPE_SECRET_KEY=sk_test_|NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_)" .env.local equals 2</manual>
  </verify>
  <resume-signal>Reply "done" once both values are saved in .env.local, or paste the two values directly and Claude will add them for you.</resume-signal>
  <done>.env.local contains real STRIPE_SECRET_KEY (sk_test_...) and NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY (pk_test_...) values.</done>
</task>

<task type="auto">
  <name>Task 3: Create Stripe test-mode products/prices via API, capture real Price IDs</name>
  <files>.env.local</files>
  <read_first>
    .env.local (post-Task-2 state, to read the STRIPE_SECRET_KEY value)
  </read_first>
  <action>
    Read `STRIPE_SECRET_KEY` from `.env.local` (set in Task 2). Using `curl` against the Stripe API with HTTP Basic auth (`-u "$STRIPE_SECRET_KEY:"`, secret key as username, empty password — Stripe's standard API auth convention):

    1. `POST https://api.stripe.com/v1/products` with `name="Noetree Pro"` and `description="Unlimited notes + 100 AI credits/month"` — capture the returned `id` (`prod_xxx`).
    2. `POST https://api.stripe.com/v1/prices` with `product=<pro prod_xxx>`, `unit_amount=900`, `currency=eur`, `recurring[interval]=month` — creates the recurring 9€/month Pro price. Capture the returned `id` as `STRIPE_PRO_PRICE_ID`.
    3. `POST https://api.stripe.com/v1/products` with `name="Noetree AI Credits Top-up"` and `description="One-time purchase of 50 AI credits"` — capture the returned `id` (`prod_yyy`).
    4. `POST https://api.stripe.com/v1/prices` with `product=<topup prod_yyy>`, `unit_amount=200`, `currency=eur` (no `recurring` field — this is a one-time price) — creates the 2€ Top-up price. Capture the returned `id` as `STRIPE_TOPUP_PRICE_ID`.

    Append `STRIPE_PRO_PRICE_ID=price_xxx` and `STRIPE_TOPUP_PRICE_ID=price_xxx` lines to `.env.local` (create if missing, overwrite if placeholder values already exist).
  </action>
  <verify>
    <automated>grep -c -E "^STRIPE_(PRO|TOPUP)_PRICE_ID=price_" .env.local</automated>
  </verify>
  <acceptance_criteria>
    - Both product-creation curl responses contain `"object": "product"` and a non-null `"id"` starting with `prod_`
    - The Pro price-creation response contains `"object": "price"`, an `"id"` starting with `price_`, and `"recurring":{"interval":"month",...}`
    - The Top-up price-creation response contains `"object": "price"`, an `"id"` starting with `price_`, and `"recurring": null`
    - `grep -c -E "^STRIPE_(PRO|TOPUP)_PRICE_ID=price_" .env.local` equals 2
    - Neither captured value equals the literal placeholder string `price_xxx` from .env.example
  </acceptance_criteria>
  <done>2 Stripe products + 2 prices exist in test mode (verifiable in Stripe Dashboard -> Product catalog), and real price IDs are captured in .env.local, satisfying D-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Repo (git-tracked) ↔ local secrets (.env.local, git-ignored) | `.env.example` must never cross into containing real secret values |
| Local machine → Stripe API | `STRIPE_SECRET_KEY` (test mode) is transmitted over HTTPS via curl Basic auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | `.env.example` | mitigate | Inline comments explicitly mark secret vs. public vars per D-05; Task 1 acceptance criteria greps confirm no `NEXT_PUBLIC_` prefix on `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` |
| T-01-07 | Information Disclosure | `.env.example` committed to git | mitigate | Only placeholder values (`sk_test_xxx`, `whsec_xxx`, `price_xxx`, `pk_test_xxx`) are ever written to `.env.example` — real captured values live exclusively in `.env.local`, which remains git-ignored and untouched by the `!.env.example` negation (scoped to that single filename) |
| T-01-08 | Information Disclosure | `.gitignore`'s blanket `.env*` rule | mitigate | Explicit `!.env.example` negation added in Task 1, verified via `git check-ignore -v .env.example` exiting 1 before any commit is attempted |
| T-01-09 | Information Disclosure | STRIPE_SECRET_KEY transmitted to Stripe API | accept | Standard TLS-protected Stripe API call using the officially documented Basic-auth convention — no alternative exists for programmatic product/price creation |

</threat_model>

<verification>
`git check-ignore -v .env.example` exits 1. `.env.example` contains exactly the 5 documented vars with correct secret/public framing. `.env.local` contains real (non-placeholder) `STRIPE_PRO_PRICE_ID`/`STRIPE_TOPUP_PRICE_ID` values confirmed via Stripe API response inspection.
</verification>

<success_criteria>
- `.env.example` exists, is trackable, and documents all 5 required vars (D-04)
- 2 real Stripe test-mode products + prices exist, captured in `.env.local` (D-03)
</success_criteria>

<output>
Create `.planning/phases/01-schema-infrastructure-foundation/01-03-SUMMARY.md` when done
</output>
