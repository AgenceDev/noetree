---
phase: 01-schema-infrastructure-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-03"]
files_modified: [.env.local]
autonomous: false

requirements: [PLAN-05]

user_setup:
  - service: stripe
    why: "Webhook endpoints for staging/production + local CLI signing secret (D-05)"
    env_vars:
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard -> Developers -> Webhooks (per-environment signing secret), or `stripe listen --print-secret` for local"
    dashboard_config:
      - task: "Create 2 webhook endpoints (staging URL + production URL) enabled for checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.paid, invoice.payment_failed"
        location: "Stripe Dashboard -> Developers -> Webhooks"
  - service: vercel
    why: "Propagate all 5 required env vars across Development/Preview/Production scopes (ROADMAP Phase 1 SC-2)"
    env_vars:
      - name: "STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRO_PRICE_ID, STRIPE_TOPUP_PRICE_ID, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY"
        source: ".env.local (captured in Plan 01-03) + Stripe Dashboard webhook signing secrets"
    dashboard_config:
      - task: "Add all 5 vars to Development, Preview, and Production environment scopes; mark STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET as Sensitive"
        location: "Vercel Project Settings -> Environment Variables"

must_haves:
  truths:
    - "All 5 required env vars are present in local, staging, and production environments with no NEXT_PUBLIC_ prefix on secrets (ROADMAP Phase 1 SC-2)"
  artifacts:
    - path: ".env.local"
      provides: "Local STRIPE_WEBHOOK_SECRET captured from `stripe listen`"
      contains: "STRIPE_WEBHOOK_SECRET"
  key_links: []
---

<objective>
Propagate all 5 required environment variables into Vercel's Development, Preview (staging), and Production scopes, create distinct Stripe webhook signing secrets for staging and production, and capture the local `stripe listen` signing secret — completing the last unmet piece of ROADMAP Phase 1 Success Criterion 2.

Purpose: SC-2 requires all 5 vars present in local/staging/production with correct secret/public framing. Claude has no Vercel CLI/API token and no Stripe CLI installed in this sandboxed environment (confirmed: `vercel`/`stripe` binaries absent from PATH, no `.vercel`/`vercel.json` in repo — deploys run via a Vercel Deploy Hook per `.github/workflows/release-dev.yml`, not local CLI), and creating webhook endpoints for staging/production domains requires Stripe/Vercel dashboard access this environment does not have (RESEARCH.md Pitfalls 3-4, Open Questions 1-2). This is a genuine human-only step.
Output: All 5 env vars present in Vercel's 3 scopes; 2 distinct staging/production `STRIPE_WEBHOOK_SECRET` values created; local `STRIPE_WEBHOOK_SECRET` captured in `.env.local`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-schema-infrastructure-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Propagate env vars to Vercel and create staging/production/local webhook secrets</name>
  <files>.env.local</files>
  <action>
    Claude has confirmed no Vercel CLI, Vercel API token, or Stripe CLI are available in this environment, and does not know the staging/production domain names — none of these steps can be automated here. Present the how-to-verify steps below to the user, wait for confirmation, and once the user reports the local `stripe listen` signing secret, write it to `.env.local` as `STRIPE_WEBHOOK_SECRET`.
  </action>
  <what-built>
    Claude has confirmed no Vercel CLI, Vercel API token, or Stripe CLI are available in this environment, and does not know your staging/production domain names — none of these can be automated here. .env.local already holds STRIPE_SECRET_KEY, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY, STRIPE_PRO_PRICE_ID, and STRIPE_TOPUP_PRICE_ID from Plan 01-03.
  </what-built>
  <how-to-verify>
    1. In Vercel project settings (Settings -> Environment Variables), add all 5 vars for each of the 3 scopes (Development, Preview [staging/dev branch], Production): `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRO_PRICE_ID`, `STRIPE_TOPUP_PRICE_ID`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`. Use the values already in `.env.local` for `STRIPE_SECRET_KEY`/`NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`/`STRIPE_PRO_PRICE_ID`/`STRIPE_TOPUP_PRICE_ID` across all 3 scopes (test-mode values can be shared per D-05). Mark `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` as "Sensitive" in Vercel (hides the value from build logs/UI after saving).
    2. In Stripe Dashboard (test mode) -> Developers -> Webhooks, create 2 webhook endpoints: one for your staging URL (`https://<staging-domain>/api/webhooks/stripe`) and one for your production URL (`https://<production-domain>/api/webhooks/stripe`), each enabled for events `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.paid`, `invoice.payment_failed` (the 5 event types ROADMAP Phase 2 will handle). Copy each endpoint's "Signing secret" (`whsec_...`) — set the staging one as `STRIPE_WEBHOOK_SECRET` in Vercel's Preview scope, and the production one as `STRIPE_WEBHOOK_SECRET` in Vercel's Production scope. These must be 2 DISTINCT values, never the same secret reused across environments (D-05).
    3. For local development, install the Stripe CLI if not already installed (https://stripe.com/docs/stripe-cli), run `stripe login`, then run `stripe listen --forward-to localhost:3000/api/webhooks/stripe --print-secret` to obtain the local webhook signing secret, and add it to `.env.local` as `STRIPE_WEBHOOK_SECRET=whsec_...`.
  </how-to-verify>
  <verify>
    <manual>grep -c "^STRIPE_WEBHOOK_SECRET=whsec_" .env.local equals 1; user confirms Vercel's 3 scopes and the 2 Stripe webhook endpoints are configured per the how-to-verify steps.</manual>
  </verify>
  <resume-signal>Reply "done" once all 5 vars are confirmed present in Vercel's Development/Preview/Production scopes and .env.local has a local STRIPE_WEBHOOK_SECRET, or describe any step you couldn't complete.</resume-signal>
  <done>.env.local contains a real local STRIPE_WEBHOOK_SECRET; user confirms all 5 vars are set across Vercel's Development/Preview/Production scopes with 2 distinct staging/production webhook secrets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final Phase 1 success-criteria sign-off</name>
  <files>(none — verification-only checkpoint, no files modified)</files>
  <action>
    Present a consolidated summary of Plans 01-01 through 01-04's automated verification results against ROADMAP.md's 5 Phase 1 Success Criteria and wait for explicit user sign-off before the phase is considered complete.
  </action>
  <what-built>
    Across Plans 01-01 through 01-04: 4 new Convex tables + indexes deployed, 7 typed stub functions created, `stripe`/`@stripe/stripe-js` installed, `@clerk/nextjs` CVE-patched, middleware excludes webhook routes, `.env.example` documented and committable, real Stripe test-mode Price IDs captured, and all 5 env vars propagated to Vercel's 3 scopes plus 3 distinct webhook secrets (local/staging/production).
  </what-built>
  <how-to-verify>
    Confirm each of ROADMAP.md's 5 Phase 1 Success Criteria:
    1. `npx convex dev --once` compiles cleanly.
    2. All 5 env vars (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRO_PRICE_ID`, `STRIPE_TOPUP_PRICE_ID`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`) exist in local `.env.local` and in Vercel's Development/Preview/Production scopes, with no `NEXT_PUBLIC_` prefix on `STRIPE_SECRET_KEY` or `STRIPE_WEBHOOK_SECRET` (grep `.env.local` locally; check the Vercel dashboard for the other two scopes).
    3. `node -e "require('stripe')"` succeeds.
    4. `middleware.ts` excludes `/api/webhooks/**` while `/dashboard` stays protected.
    5. `npx convex dev --once` shows `convex/subscriptions.ts` and `convex/aiCredits.ts` deployed without errors.
  </how-to-verify>
  <verify>
    <manual>Human walks through the 5 ROADMAP success criteria above and confirms each passes.</manual>
  </verify>
  <resume-signal>Type "approved" once all 5 criteria are confirmed true, or list which are still failing.</resume-signal>
  <done>User has typed "approved", confirming all 5 ROADMAP Phase 1 success criteria are true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Vercel dashboard ↔ deployed Next.js runtime | Env vars set here become `process.env.*` in every Vercel deployment scope |
| Stripe Dashboard ↔ webhook endpoints | Each endpoint's signing secret is the sole authenticity proof for inbound webhook calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Information Disclosure | `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` in Vercel dashboard | mitigate | Checklist instructs marking both as "Sensitive" in Vercel environment variable settings for all 3 scopes, hiding values from build logs and lower-permission collaborators |
| T-01-11 | Spoofing | Reused webhook secret across environments | mitigate | D-05 requires 3 distinct `STRIPE_WEBHOOK_SECRET` values (local CLI / staging / production); checklist requires each Stripe webhook endpoint object to be created separately per environment URL, each yielding its own signing secret |
| T-01-12 | — (topology uncertainty) | Convex "staging" deployment mapping (RESEARCH.md Open Question 1) | accept | Does not block Phase 1's 5 stated success criteria — none of the required secrets need a Convex-side mirror (Pitfall 3, all 5 vars are consumed exclusively by Next.js/Vercel-side code). Flagged forward for Phase 2 planning if it becomes relevant. |

</threat_model>

<verification>
Human confirms via the Task 2 checklist that all 5 vars exist in local/staging/production with correct secret/public framing, distinct webhook secrets per environment, and the prior 3 plans' automated checks (schema compile, package import, middleware behavior) all still pass.
</verification>

<success_criteria>
- All 5 required env vars present in local, staging, and production with no `NEXT_PUBLIC_` prefix on secrets (ROADMAP SC-2 — the final unmet Phase 1 criterion)
- Phase 1 fully complete: all 5 ROADMAP success criteria confirmed true
</success_criteria>

<output>
Create `.planning/phases/01-schema-infrastructure-foundation/01-04-SUMMARY.md` when done
</output>
