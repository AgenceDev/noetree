---
phase: 06-settings-page
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/[locale]/(app)/settings/actions.ts
  - app/[locale]/(app)/settings/actions.test.ts
autonomous: true
requirements: [SET-04, PAY-04]

must_haves:
  truths:
    - "cancelSubscription re-verifies status === 'active' server-side before calling Stripe"
    - "cancelSubscription calls stripe.subscriptions.update with cancel_at_period_end: true"
    - "resumeSubscription re-verifies cancelAtPeriodEnd === true before calling Stripe"
    - "resumeSubscription calls stripe.subscriptions.update with cancel_at_period_end: false"
    - "Neither action accepts a subscription id argument; the id is derived from the caller's own getSubscription"
    - "Neither action writes Convex state directly or calls redirect()"
  artifacts:
    - path: "app/[locale]/(app)/settings/actions.ts"
      provides: "cancelSubscription + resumeSubscription Server Actions"
      exports: ["cancelSubscription", "resumeSubscription"]
    - path: "app/[locale]/(app)/settings/actions.test.ts"
      provides: "guard + Stripe-call coverage for both actions"
      contains: "cancelSubscription"
  key_links:
    - from: "app/[locale]/(app)/settings/actions.ts"
      to: "stripe.subscriptions.update"
      via: "single guarded Stripe call using existing.stripeSubscriptionId"
      pattern: "subscriptions\\.update"
    - from: "app/[locale]/(app)/settings/actions.ts"
      to: "api.subscriptions.getSubscription"
      via: "identity-forwarded Convex query re-verification"
      pattern: "getSubscription"
---

<objective>
Create the two Server Actions the Settings page binds to — `cancelSubscription` (SET-04) and `resumeSubscription` (PAY-04, D-02) — in a new `app/[locale]/(app)/settings/actions.ts`, mirroring the exact shape of the existing `notes/actions.ts`/`pricing/actions.ts` Server Actions. Each authenticates via Clerk, re-derives the caller's subscription server-side (never trusting UI conditionals), server-verifies the correct precondition, then makes exactly one `stripe.subscriptions.update()` call. Convex state is written back only by the existing webhook — these actions do NOT write Convex directly and do NOT redirect.

Purpose: Provides the cancel/resume mutation surface the Settings Plan Card triggers.
Output: New `actions.ts` (two exports) + new `actions.test.ts` covering the guards and Stripe calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-page/06-CONTEXT.md
@.planning/phases/06-settings-page/06-RESEARCH.md
@.planning/phases/06-settings-page/06-PATTERNS.md

<interfaces>
<!-- Server Action template (app/[locale]/(app)/notes/actions.ts createTopupCheckoutSession) — mirror TWICE -->
<!-- Shape: "use server"; const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);
     export async function X(): Promise<void> {
       const { userId, getToken } = await auth(); if (!userId) throw new Error("UNAUTHENTICATED");
       const convex = new ConvexHttpClient(process.env.NEXT_PUBLIC_CONVEX_URL!);
       try { const t = await getToken({ template: "convex" }); if (t) convex.setAuth(t);
             existing = await convex.query(api.subscriptions.getSubscription, {}); }
       catch (err) { console.error(...); throw new Error("subscriptionLookupFailed"); }
       <server-side guard>  try { await stripe.subscriptions.update(existing.stripeSubscriptionId, { cancel_at_period_end: <bool> }); }
       catch (err) { console.error(...); throw new Error("<name>Failed"); }
     } -->

<!-- getSubscription return fields (convex/subscriptions.ts): { stripeSubscriptionId: string; status: "active"|...; currentPeriodEnd: number; cancelAtPeriodEnd: boolean } or null -->

<!-- Test mock shape (app/[locale]/(marketing)/pricing/actions.test.ts) — reuse EXACTLY except stripe surface -->
<!-- mock "stripe" → { subscriptions: { update: mockSubscriptionsUpdate } } (NOT checkout.sessions.create)
     mock "convex/browser" → ConvexHttpClient { query: mockConvexQuery, setAuth: mockSetAuth }
     mock "@clerk/nextjs/server" → { auth: mockAuth }
     OMIT the next/navigation mock — these actions do not call redirect() -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Create app/[locale]/(app)/settings/actions.test.ts</name>
  <files>app/[locale]/(app)/settings/actions.test.ts</files>
  <read_first>
    - app/[locale]/(marketing)/pricing/actions.test.ts (full file — copy its `vi.mock` blocks for `stripe`, `convex/browser`, `@clerk/nextjs/server`; copy the `beforeAll` env-var setup + dynamic `await import("./actions")`; copy the `beforeEach` mock-reset shape)
    - app/[locale]/(app)/notes/actions.ts (the guard the test asserts against — the `TOPUP_REQUIRES_PRO`-style server-side re-verification)
    - .planning/phases/06-settings-page/06-PATTERNS.md ("settings/actions.test.ts" section — mock-shape substitutions)
  </read_first>
  <behavior>
    - cancelSubscription, authenticated + active subscription: calls `stripe.subscriptions.update` with the caller's `stripeSubscriptionId` and `{ cancel_at_period_end: true }` exactly once
    - cancelSubscription, caller whose getSubscription returns null / status !== "active": throws `CANCEL_REQUIRES_ACTIVE_SUBSCRIPTION` and `mockSubscriptionsUpdate` is NEVER called
    - cancelSubscription, unauthenticated (auth returns userId: null): throws `UNAUTHENTICATED`, no Stripe call
    - resumeSubscription, authenticated + cancelAtPeriodEnd === true: calls `stripe.subscriptions.update` with `{ cancel_at_period_end: false }` exactly once
    - resumeSubscription, caller with cancelAtPeriodEnd !== true: throws `RESUME_REQUIRES_PENDING_CANCELLATION` (or the exact guard error name chosen in Task 2) and no Stripe call
  </behavior>
  <action>Create `app/[locale]/(app)/settings/actions.test.ts`. Reuse `pricing/actions.test.ts`'s mock scaffolding but change the stripe mock to expose `subscriptions: { update: mockSubscriptionsUpdate }` (rename the fn from `mockSessionsCreate`), and OMIT the `next/navigation`/`permanentRedirect` mock entirely (these actions never redirect). In `beforeAll`, set `process.env.STRIPE_SECRET_KEY` and `process.env.NEXT_PUBLIC_CONVEX_URL`, then `({ cancelSubscription, resumeSubscription } = await import("./actions"))`. Drive `mockAuth` to return `{ userId, getToken }` and `mockConvexQuery` to return the fixture subscription object per case. Assert the five behaviors above using `await expect(fn()).rejects.toThrow("...")` for guard/unauth cases and `expect(mockSubscriptionsUpdate).toHaveBeenCalledWith(<id>, { cancel_at_period_end: <bool> })` for happy paths. Tests MUST fail initially (actions.ts does not exist).</action>
  <verify>
    <automated>npx vitest run "app/[locale]/(app)/settings/actions.test.ts" 2>&1 | grep -Eiq "Cannot find module|Failed to load|cancelSubscription|resumeSubscription" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - File exists with mocks for `stripe`, `convex/browser`, `@clerk/nextjs/server` and NO `next/navigation` mock
    - Contains assertions for both `cancelSubscription` and `resumeSubscription` covering happy-path + guard-rejection + (for cancel) unauthenticated
    - Happy-path assertions check the exact `{ cancel_at_period_end: true }` / `{ cancel_at_period_end: false }` argument to `subscriptions.update`
    - Guard-rejection assertions verify `mockSubscriptionsUpdate` was NOT called
    - Running the file fails (RED) because `./actions` cannot be imported yet
  </acceptance_criteria>
  <done>The actions test file exists and fails because actions.ts is not yet written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement app/[locale]/(app)/settings/actions.ts</name>
  <files>app/[locale]/(app)/settings/actions.ts</files>
  <read_first>
    - app/[locale]/(app)/notes/actions.ts (full file — mirror its "use server" header, `new Stripe(process.env.STRIPE_SECRET_KEY!)` module const, `auth()` → `ConvexHttpClient` + `setAuth(convexToken)` → `getSubscription` handshake, and the narrow try/catch around the single Stripe call)
    - .planning/phases/06-settings-page/06-RESEARCH.md (Pattern 1 full template, Pitfall 4 — NO redirect(); Security Domain — server-side re-verification is the T-06-01 mitigation)
    - .planning/phases/06-settings-page/06-PATTERNS.md ("settings/actions.ts" — the boolean/guard differences between cancel and resume)
  </read_first>
  <action>Create `app/[locale]/(app)/settings/actions.ts` with `"use server"` at the top and a module-level `const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!)`. Export two async functions returning `Promise<void>`, each taking ZERO arguments (the subscription id is never a parameter — T-06-01/T-06-04 invariant):
  - `cancelSubscription`: `auth()` → throw `UNAUTHENTICATED` if no userId; build `ConvexHttpClient(process.env.NEXT_PUBLIC_CONVEX_URL!)`, `setAuth` with `getToken({ template: "convex" })`, query `api.subscriptions.getSubscription` inside a try/catch that rethrows `subscriptionLookupFailed`; then guard `if (existing?.status !== "active" || !existing.stripeSubscriptionId) throw new Error("CANCEL_REQUIRES_ACTIVE_SUBSCRIPTION")`; then in a narrow try/catch call `stripe.subscriptions.update(existing.stripeSubscriptionId, { cancel_at_period_end: true })`, rethrowing `cancelFailed` on error.
  - `resumeSubscription`: identical shape EXCEPT the guard is `if (existing?.cancelAtPeriodEnd !== true || !existing.stripeSubscriptionId) throw new Error("RESUME_REQUIRES_PENDING_CANCELLATION")` and the Stripe call passes `{ cancel_at_period_end: false }`, rethrowing `resumeFailed` on error.
  Do NOT call `redirect()` anywhere (Pitfall 4). Do NOT call any `internal.subscriptions.*` mutation or write Convex state directly (anti-pattern — the existing `customer.subscription.updated` webhook is the sole writer). Keep every `catch` a `console.error(...)` + rethrow of a stable error string, matching notes/actions.ts. Use the exact guard error-string names above so they match the Task 1 test assertions.</action>
  <verify>
    <automated>npx vitest run "app/[locale]/(app)/settings/actions.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File begins with `"use server"` and exports `cancelSubscription` and `resumeSubscription`, both zero-argument
    - `grep -Eq "cancel_at_period_end: true" "app/[locale]/(app)/settings/actions.ts"` and `grep -Eq "cancel_at_period_end: false" "app/[locale]/(app)/settings/actions.ts"` both succeed
    - `grep -q "getSubscription" "app/[locale]/(app)/settings/actions.ts"` succeeds (server-side re-verification present)
    - `grep -Eq "redirect|internal\\.subscriptions" "app/[locale]/(app)/settings/actions.ts"` returns NO match (no redirect, no direct Convex write)
    - Neither function signature takes a subscription-id / clerkUserId parameter
    - `npx vitest run "app/[locale]/(app)/settings/actions.test.ts"` passes all cases (GREEN)
  </acceptance_criteria>
  <done>Both Server Actions are implemented; all actions.test.ts cases pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                            |
| -------------------------- | ------------------------------------------------------------------------------------------------------ |
| browser → Server Action    | A crafted form POST can invoke these actions directly, bypassing the UI's conditional button rendering |
| Server Action → Stripe API | Outbound state mutation on a subscription                                                              |

## STRIDE Threat Register

| Threat ID | Category                      | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                                                                                                         |
| --------- | ----------------------------- | ------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-01   | Tampering                     | `cancelSubscription` / `resumeSubscription` | mitigate    | Server-side re-verification of `status === "active"` (cancel) / `cancelAtPeriodEnd === true` (resume) via an identity-scoped `getSubscription` fetched INSIDE the action — UI conditional rendering is not a security boundary. Proven by Task 1 guard-rejection tests. |
| T-06-02   | Elevation of Privilege (IDOR) | Server Action arguments                     | mitigate    | Actions take ZERO arguments; the `stripeSubscriptionId` acted on is derived from the caller's own identity-scoped `getSubscription`, never a client-supplied id.                                                                                                        |
| T-06-03   | Tampering                     | webhook replay desync after cancel/resume   | accept      | Already mitigated in Phase 2 — `processedStripeEvents` idempotency table rejects replayed events. Zero changes this phase.                                                                                                                                              |
| T-06-SC   | Tampering                     | npm installs                                | accept      | No new packages (`stripe` already installed). No install task in this plan.                                                                                                                                                                                             |

</threat_model>

<verification>
- `npx vitest run "app/[locale]/(app)/settings/actions.test.ts"` is green.
- `grep -Eq "redirect|internal\\.subscriptions" "app/[locale]/(app)/settings/actions.ts"` returns no match.
</verification>

<success_criteria>

- Both actions exist, are zero-argument, re-verify server-side, call Stripe once with the correct boolean, never redirect, never write Convex directly.
- All tests pass.
  </success_criteria>

<output>
Create `.planning/phases/06-settings-page/06-02-SUMMARY.md` when done.
</output>
