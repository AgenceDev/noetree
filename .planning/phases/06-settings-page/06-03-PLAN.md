---
phase: 06-settings-page
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - messages/fr.json
  - components/nav-user.tsx
autonomous: true
requirements: [SET-01]

must_haves:
  truths:
    - "A 'Settings' item appears in the NavUser dropdown, distinct from the existing 'Account' item"
    - "Clicking 'Settings' navigates to /settings"
    - "A Settings i18n namespace exists in both en.json and fr.json with all copy the Settings page needs"
    - "D-04: Settings route lives at app/[locale]/(app)/settings/page.tsx, parallel to the notes route"
    - "D-05: Settings NavUser item is a separate entry from Account, which still opens Clerk's profile modal"
  artifacts:
    - path: "components/nav-user.tsx"
      provides: "Settings dropdown navigation entry"
      contains: "settings"
    - path: "messages/en.json"
      provides: "Settings namespace + NavUser.settings key"
      contains: '"Settings"'
    - path: "messages/fr.json"
      provides: "Settings namespace + NavUser.settings key (French)"
      contains: '"Settings"'
  key_links:
    - from: "components/nav-user.tsx Settings item"
      to: "/settings route"
      via: "router.push('/settings')"
      pattern: "settings"
---

<objective>
Provide the navigation entry point and i18n copy the Settings page (Plan 04) depends on: add a distinct "Settings" item to the `NavUser` dropdown (D-05 — separate from the existing "Account" item that opens Clerk's profile modal), and add a complete `Settings` namespace + `NavUser.settings` key to both `messages/en.json` and `messages/fr.json`.

Purpose: Users can reach the new Settings page; Plan 04's `t(...)` calls resolve to real strings.
Output: One new dropdown item, one new i18n namespace mirrored across two locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-page/06-CONTEXT.md
@.planning/phases/06-settings-page/06-UI-SPEC.md
@.planning/phases/06-settings-page/06-PATTERNS.md

<interfaces>
<!-- nav-user.tsx: existing "Account" DropdownMenuItem (lines ~103-108) calls openUserProfile();
     router + usePathname already imported from "@/i18n/routing"; lucide icons imported at top (lines 3-11).
     Insert a sibling DropdownMenuItem using router.push("/settings") and a Settings lucide icon. -->
<!-- messages/en.json: "NavUser" namespace at line 12 already has "account": "Account".
     Existing analog namespaces (Pricing / AiCredits / PlanEnforcement) are flat key-value. -->
</interfaces>

<copywriting_contract>
All page-facing copy is locked in 06-UI-SPEC.md "Copywriting Contract". The Settings namespace MUST cover every string the Plan 04 page/cards render. Required keys (English values shown; provide the French mirror in fr.json):

- title: "Settings"
- planCardTitle: "Plan"
- creditsCardTitle: "Credits"
- freePlanName: "Free plan"
- proPlanName: "Pro"
- renewsOn: "Renews on {date}"
- cancelsOn: "Cancels on {date}" (LOCKED wording, D-03 — must read exactly "Cancels on")
- upgradeCta: "Upgrade to Pro" (may reuse the existing Pricing.upgradeCta value verbatim)
- cancelCta: "Cancel subscription"
- resumeCta: "Resume subscription"
- cancelConfirmTitle: "Cancel your subscription?"
- cancelConfirmDesc: "You'll keep Pro access until {date}. You can resume anytime before then."
- cancelConfirmKeep: "Keep subscription"
- cancelConfirmConfirm: "Cancel subscription"
- creditsBalance: "{count} credits"
- topupHistoryTitle: "Top-up history"
- topupHistoryEmptyTitle: "No top-ups yet"
- topupHistoryEmptyBody: "Your AI credit purchases will appear here once you buy a top-up."
- actionError: "Something went wrong updating your subscription. Please try again, or contact support if this keeps happening."
  Use ICU `{date}` / `{count}` placeholders (next-intl syntax) exactly as the existing namespaces do.
  </copywriting_contract>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add Settings i18n namespace + NavUser.settings to en.json and fr.json</name>
  <files>messages/en.json, messages/fr.json</files>
  <read_first>
    - messages/en.json (read the `NavUser` namespace at line ~12 and an existing flat namespace such as `Pricing` / `AiCredits` for key-format and interpolation conventions)
    - messages/fr.json (read the matching `NavUser` and `Pricing`/`AiCredits` namespaces to mirror structure and French register)
    - .planning/phases/06-settings-page/06-UI-SPEC.md (Copywriting Contract — the locked source of every string)
  </read_first>
  <action>Add a top-level `"Settings"` namespace to BOTH `messages/en.json` and `messages/fr.json` containing every key listed in the plan's copywriting_contract block, with English values in en.json and faithful French translations in fr.json. Add a `"settings"` key to the existing `"NavUser"` namespace in BOTH files (English "Settings"; French use the project's register — check how "account" is translated in fr.json and match it, e.g. "Paramètres"). Preserve valid JSON with the file's existing 2-space indentation and no trailing commas. Use ICU `{date}`/`{count}` placeholders exactly as existing namespaces do. The `cancelsOn` value MUST read "Cancels on {date}" in en.json (D-03 locked literal). Both files MUST end up with identical key sets under `Settings` and identical `NavUser` keys (only values differ by language).</action>
  <verify>
    <automated>node -e "const en=require('./messages/en.json'),fr=require('./messages/fr.json');const ek=Object.keys(en.Settings).sort(),fk=Object.keys(fr.Settings).sort();if(JSON.stringify(ek)!==JSON.stringify(fk))throw new Error('Settings key mismatch');if(!en.NavUser.settings||!fr.NavUser.settings)throw new Error('NavUser.settings missing');if(en.Settings.cancelsOn!=='Cancels on {date}')throw new Error('cancelsOn wrong: '+en.Settings.cancelsOn);console.log('OK '+ek.length+' keys')"</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse as valid JSON (the node command above runs without throwing)
    - `en.Settings` and `fr.Settings` have identical, sorted key sets covering all keys in the copywriting contract
    - `en.Settings.cancelsOn === "Cancels on {date}"` exactly (D-03)
    - `en.NavUser.settings` and `fr.NavUser.settings` both exist and are non-empty
    - No existing key in either file is removed or renamed
  </acceptance_criteria>
  <done>Both locale files carry a complete, mirrored Settings namespace and a NavUser.settings key.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Settings dropdown item to components/nav-user.tsx</name>
  <files>components/nav-user.tsx</files>
  <read_first>
    - components/nav-user.tsx (full file — the existing "Account" `DropdownMenuItem` at lines ~103-108, the `router`/`usePathname` import from `@/i18n/routing` at line ~34, the `lucide-react` import block at lines ~3-11, and the `useTranslations("NavUser")` usage)
    - .planning/phases/06-settings-page/06-PATTERNS.md ("components/nav-user.tsx" section — the exact insertion idiom)
  </read_first>
  <action>Add a new `DropdownMenuItem` to `components/nav-user.tsx` that navigates to the Settings page via `router.push("/settings")` (reuse the already-imported `router` from `@/i18n/routing` — do not import Next's raw router), labelled with `t("settings")` from the existing `NavUser` translations, and prefixed with the `Settings` icon from `lucide-react` (add `Settings` to the existing lucide import block). Insert it as its OWN item adjacent to — NOT merged with — the existing "Account" item, which must keep calling `openUserProfile()` (D-05: identity/security vs. billing/credits are distinct concerns). Match the surrounding `DropdownMenuItem` markup (icon + label) already used by the "Account" item.</action>
  <verify>
    <automated>grep -Eq 'router\.push\("/settings"\)' components/nav-user.tsx && grep -q "openUserProfile" components/nav-user.tsx && npx tsc --noEmit -p . 2>&1 | grep -i "nav-user" && echo "TSC-ERROR" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq 'router\.push\("/settings"\)' components/nav-user.tsx` succeeds (new item navigates to /settings)
    - `Settings` is imported from `lucide-react` and rendered inside the new `DropdownMenuItem`
    - The existing "Account" item is unchanged and still calls `openUserProfile()`
    - The new item uses `t("settings")` for its label (no hardcoded string)
    - `npx tsc --noEmit` reports no error referencing `nav-user`
  </acceptance_criteria>
  <done>A distinct "Settings" dropdown item navigates to /settings; "Account" is untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                                             |
| --------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| browser → client route navigation | Clicking the dropdown item performs an in-app client-side navigation; no untrusted input crosses a server boundary here |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                                                                                                                   |
| --------- | ---------------------- | ------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-05   | Elevation of Privilege | `/settings` route access | accept      | Route lives under the existing `(app)` auth boundary (Clerk middleware protects all `(app)` routes); the dropdown item only renders for signed-in users. No new access-control surface added by nav/i18n changes. |
| T-06-SC   | Tampering              | npm installs             | accept      | No new packages (`lucide-react`, `next-intl` already installed). No install task in this plan.                                                                                                                    |

</threat_model>

<verification>
- `node` JSON key-parity check (Task 1) passes.
- `npx tsc --noEmit` reports no `nav-user` error.
</verification>

<success_criteria>

- Settings namespace present and mirrored across both locales; `NavUser.settings` present in both.
- A distinct "Settings" dropdown item navigates to `/settings`; "Account" still opens the Clerk profile modal.
  </success_criteria>

<output>
Create `.planning/phases/06-settings-page/06-03-SUMMARY.md` when done.
</output>
