---
phase: 06-settings-page
plan: 05
type: execute
wave: 3
depends_on: [06-04]
files_modified: []
autonomous: false
requirements: [SET-01, SET-02, SET-03, SET-04, SET-05, PAY-04]

must_haves:
  truths:
    - "The full test suite is green before human sign-off"
    - "No new public query or Server Action accepts a client-supplied clerkUserId/subscription id"
    - "A human has verified the five ROADMAP success criteria against the running app + Stripe test mode"
  artifacts: []
  key_links:
    - from: "automated security grep"
      to: "identity-derived invariant"
      via: "grep confirms zero client-supplied id arguments in new code"
      pattern: "getUserIdentity|getSubscription"
---

<objective>
Close Phase 6 with an automated security + suite gate followed by a human UAT of the five ROADMAP success criteria that cannot be unit-tested (they require live Stripe test-mode webhook delivery). This verifies the cancel → "Cancels on [date]" → resume round-trip, the Free upgrade path, the credits/top-up history, and the period-end downgrade — none of which are mockable in the edge-runtime unit suite.

Purpose: Prove the phase's user-facing success criteria hold end-to-end before marking Phase 6 complete.
Output: A verification record (SUMMARY) capturing suite result, security-grep result, and the human's SC1-SC5 verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-page/06-CONTEXT.md
@.planning/phases/06-settings-page/06-VALIDATION.md
@.planning/phases/06-settings-page/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated security + full-suite verification</name>
  <files>convex/aiCredits.ts, app/[locale]/(app)/settings/actions.ts</files>
  <read_first>
    - convex/aiCredits.ts (confirm listMyTopups is identity-derived — args: {} and getUserIdentity, no clerkUserId argument)
    - app/[locale]/(app)/settings/actions.ts (confirm cancel/resume take zero arguments and re-verify via getSubscription before Stripe)
    - .planning/phases/06-settings-page/06-VALIDATION.md (Per-Task Verification Map — the security invariants to confirm)
  </read_first>
  <action>Run the full test suite and the IDOR/identity-derivation grep gates that prove the phase's security invariants (T-06-01, T-06-04). Confirm: (1) the new listMyTopups query uses ctx.auth.getUserIdentity() and takes no clerkUserId argument; (2) the cancel/resume Server Actions re-derive the subscription via api.subscriptions.getSubscription server-side and take no subscription-id argument; (3) the full vitest suite passes. If any gate fails, stop and report — do not proceed to the human checkpoint with a red suite.</action>
  <verify>
    <automated>npx vitest run && grep -q "getUserIdentity" convex/aiCredits.ts && grep -q "getSubscription" "app/[locale]/(app)/settings/actions.ts" && grep -Eq "args: \{\}" convex/aiCredits.ts && echo SECURITY-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run exits green (all Convex + Server Action tests pass)
    - grep confirms listMyTopups is identity-derived (getUserIdentity present, no clerkUserId argument in the new query)
    - grep confirms the cancel/resume actions re-verify via getSubscription and expose no id parameter
    - The command prints SECURITY-GATE-OK
  </acceptance_criteria>
  <done>Full suite is green and the identity-derived security invariants are confirmed by grep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT of ROADMAP SC1-SC5</name>
  <files>app/[locale]/(app)/settings/page.tsx</files>
  <action>Pause for the human to verify the five ROADMAP success criteria (plus the resume round-trip) against the running app + Stripe test mode, following the steps in how-to-verify below. This is a verification-only checkpoint — no code is written. Capture each verdict; any failure becomes a specific gap for gap-closure planning.</action>
  <what-built>The Settings page (/settings), reachable from the NavUser dropdown "Settings" item, showing a Plan Card (plan/renewal/cancel/resume/upgrade) and a Credits Card (balance + top-up history), backed by the new listMyTopups query and the cancel/resume Server Actions. Cancel/resume drive Stripe, and the existing webhook writes state back to Convex reactively.</what-built>
  <how-to-verify>
    Prerequisites: run the app with `stripe listen --forward-to https://localhost:3000/api/webhooks/stripe --skip-verify` active and STRIPE_WEBHOOK_SECRET synced (per STATE.md's Phase 3 gap-closure note about HTTPS-only local dev).

    1. SC1 (plan/status display): Sign in as a Pro user, open the NavUser dropdown, click "Settings". Confirm the Plan Card shows "Pro" and a status line "Renews on [date]" where [date] is a REAL future date (NOT January 1970 — this is the seconds-vs-ms pitfall check).
    2. SC2 (Free upgrade CTA): Sign in as a Free user, open /settings. Confirm the Plan Card shows "Free plan" and an "Upgrade to Pro" button. Click it → confirm it navigates to /pricing (the existing Phase 3 checkout entry).
    3. SC3 (cancel with confirm, no immediate access loss): As the Pro user, click "Cancel subscription" → confirm the AlertDialog appears ("Cancel your subscription?"). Confirm cancellation. After the customer.subscription.updated webhook fires, confirm the status line flips to "Cancels on [date]" and a "Resume subscription" button appears — and that Pro access is NOT lost immediately.
    4. PAY-04 / resume: Click "Resume subscription". After the webhook fires, confirm the status flips back to "Renews on [date]" and the Cancel button returns.
    5. SC4 (period-end downgrade): Use `stripe trigger customer.subscription.deleted` (or advance the test clock) for the cancelling subscription. Confirm the Convex subscriptions row is removed and /settings reactively renders "Free plan".
    6. SC5 (credits + top-up history): Confirm the Credits Card shows the current balance. If the user has prior top-ups, confirm they appear newest-first with date + amount; if none, confirm the "No top-ups yet" empty state. (Optionally buy a 50-credit top-up and confirm it appears after the webhook.)

  </how-to-verify>
  <verify>
    <human-check>Human confirms each of SC1-SC5 plus the resume round-trip against the running app + Stripe test mode; rendered dates are real future dates (not 1970).</human-check>
  </verify>
  <acceptance_criteria>
    - Each of SC1-SC5 (plus the resume round-trip) is confirmed working, OR any failure is captured as a specific gap for gap-closure planning
    - Rendered dates are real future dates, never 1970 (Pitfall 1 regression check)
  </acceptance_criteria>
  <resume-signal>Type "approved" if all six checks pass, or describe each failing check for gap-closure.</resume-signal>
  <done>All six checks pass (or failures are captured as gaps); Phase 6 is verified against live Stripe test mode.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                                         |
| ----------------- | --------------------------------------------------------------------------------------------------- |
| verification only | This plan runs checks and a human UAT; it writes no application code and adds no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Disposition | Mitigation Plan                                                                                            |
| --------- | ---------------------- | ----------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-06-01   | Tampering              | cancel/resume server-side re-verification | mitigate    | Task 1 grep gate confirms getSubscription re-verification is present; Task 2 exercises the live round-trip |
| T-06-04   | Information Disclosure | listMyTopups identity derivation          | mitigate    | Task 1 grep gate confirms getUserIdentity + zero-argument shape                                            |
| T-06-03   | Tampering              | webhook replay                            | accept      | Already mitigated by Phase 2 processedStripeEvents idempotency — unchanged this phase                      |
| T-06-SC   | Tampering              | npm installs                              | accept      | No packages installed this phase                                                                           |

</threat_model>

<verification>
- npx vitest run green + security grep gates pass (Task 1).
- Human confirms SC1-SC5 + resume round-trip (Task 2).
</verification>

<success_criteria>

- Full suite green, identity-derived invariants confirmed, and all five ROADMAP success criteria verified against the running app + Stripe test mode.
  </success_criteria>

<output>
Create `.planning/phases/06-settings-page/06-05-SUMMARY.md` when done, recording the suite result, grep-gate result, and per-SC human verdicts.
</output>
