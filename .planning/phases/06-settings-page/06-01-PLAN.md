---
phase: 06-settings-page
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - convex/aiCredits.ts
  - convex/aiCredits.test.ts
autonomous: true
requirements: [SET-05]

must_haves:
  truths:
    - "listMyTopups returns only type:'topup' rows for the authenticated caller"
    - "listMyTopups returns [] (not null) for an unauthenticated caller"
    - "listMyTopups never returns another user's creditTransactions rows"
    - "Rows are returned newest-first"
  artifacts:
    - path: "convex/aiCredits.ts"
      provides: "listMyTopups public identity-derived query"
      contains: "export const listMyTopups"
    - path: "convex/aiCredits.test.ts"
      provides: "listMyTopups test coverage (auth, type-filter, isolation)"
      contains: "listMyTopups"
  key_links:
    - from: "convex/aiCredits.ts listMyTopups"
      to: "creditTransactions.by_clerkUserId index"
      via: "withIndex scoped to identity.subject"
      pattern: "withIndex\\(\"by_clerkUserId\""
---

<objective>
Add the `listMyTopups` public Convex query to `convex/aiCredits.ts` — the only new backend read this phase needs — so the Settings page can render a user's top-up purchase history (SET-05). The query is identity-derived (IDOR-safe), returns only `type: "topup"` rows, newest-first, and returns `[]` when unauthenticated.

Purpose: Supplies the data source for the Settings page's Credits Card top-up history list.
Output: New `listMyTopups` query + appended test coverage in the existing `convex/aiCredits.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-page/06-CONTEXT.md
@.planning/phases/06-settings-page/06-RESEARCH.md
@.planning/phases/06-settings-page/06-PATTERNS.md

<convex_guidelines>
Read `convex/_generated/ai/guidelines.md` before writing any Convex code — its rules override training-data assumptions. Directives that apply here:

- ALWAYS include an argument validator — this query takes `args: {}` (zero args).
- Never accept a `userId`/identifier as an argument for authorization — derive via `ctx.auth.getUserIdentity()`.
- Prefer `.take()`/`.paginate()` over `.collect()` UNLESS the caller explicitly wants the full set — D-09 is that explicit instruction, so `.collect()` is correct here.
  </convex_guidelines>

<interfaces>
<!-- Existing analog to mirror EXACTLY — same file, getMyCredits at convex/aiCredits.ts line ~126 -->
getMyCredits: `query({ args: {}, handler: async ctx => { const identity = await ctx.auth.getUserIdentity(); if (!identity) return null; return await ctx.db.query("aiCredits").withIndex("by_clerkUserId", q => q.eq("clerkUserId", identity.subject)).unique(); } })`

<!-- creditTransactions schema (convex/schema.ts lines 61-72) — NO schema change needed -->

creditTransactions: { clerkUserId: v.string(); type: v.union("deduction"|"topup"|"reset"|"refund"); amount: v.number(); createdAt: v.number(); stripePaymentIntentId: v.optional(v.string()) } .index("by_clerkUserId", ["clerkUserId"])

<!-- Test harness (convex/aiCredits.test.ts): convexTest(schema, modules); IDENTITY const { subject, tokenIdentifier } at line ~299; t.withIdentity(IDENTITY).query(...) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Append listMyTopups tests to convex/aiCredits.test.ts</name>
  <files>convex/aiCredits.test.ts</files>
  <read_first>
    - convex/aiCredits.test.ts (the whole file — reuse its existing `convexTest(schema, modules)` setup, the `IDENTITY` const at line ~299, and the `withIdentity(...).query(...)` shape used by the `getMyCredits`/`runAiAction` describe blocks)
    - convex/schema.ts lines 61-72 (creditTransactions field shape to build fixtures)
    - .planning/phases/06-settings-page/06-PATTERNS.md ("convex/aiCredits.test.ts — appended tests" section)
  </read_first>
  <behavior>
    - Test 1: unauthenticated caller (no `withIdentity`) → `listMyTopups` returns `[]` (empty array, NOT null — differs from getMyCredits which returns null)
    - Test 2: caller with a mixed set of creditTransactions rows (one each of type deduction/topup/reset/refund, all with clerkUserId = IDENTITY.subject) → returns ONLY the topup row(s)
    - Test 3: cross-user isolation — seed a topup row for IDENTITY.subject AND a topup row for a different clerkUserId (e.g. "user_other") → the IDENTITY caller's result contains only its own row, never "user_other"'s
    - Test 4: ordering — seed two topup rows for the caller with different createdAt/_creationTime → result is newest-first (index 0 is the most recently inserted)
  </behavior>
  <action>Add a new `describe("aiCredits.listMyTopups")` block at the end of `convex/aiCredits.test.ts`. Reuse the existing top-of-file imports (`convexTest`, `describe/expect/it`, `api`, `schema`, `modules`) and the existing `IDENTITY` const — do NOT redeclare them. Seed fixtures via `t.run(async ctx => ctx.db.insert("creditTransactions", {...}))` with the four `type` literals and `createdAt: Date.now()`. Call the query via `t.withIdentity(IDENTITY).query(api.aiCredits.listMyTopups, {})` for authenticated cases and `t.query(api.aiCredits.listMyTopups, {})` for the unauthenticated case. Assert the four behaviors above. These tests MUST fail initially because `listMyTopups` does not exist yet (import/type error is the expected RED state).</action>
  <verify>
    <automated>npx vitest run convex/aiCredits.test.ts 2>&1 | grep -Eiq "listMyTopups|is not a function|does not exist|Cannot read" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `convex/aiCredits.test.ts` contains a `describe("aiCredits.listMyTopups"` block with at least 4 `it(...)` cases
    - The block references all four transaction `type` literals ("deduction", "topup", "reset", "refund") in its fixtures
    - Running the file fails (RED) solely because `api.aiCredits.listMyTopups` is not yet implemented — no other test in the file regresses
    - No duplicate declaration of `IDENTITY`, `modules`, or the top-of-file imports
  </acceptance_criteria>
  <done>The four listMyTopups tests are appended and fail because the query is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement listMyTopups query in convex/aiCredits.ts</name>
  <files>convex/aiCredits.ts</files>
  <read_first>
    - convex/aiCredits.ts (read `getMyCredits` at line ~126 to copy its exact identity-derivation shape and the `query` import already present)
    - convex/_generated/ai/guidelines.md (argument-validator + identity-derivation + collect-vs-paginate rules)
    - .planning/phases/06-settings-page/06-RESEARCH.md (Pattern 3 + Pitfall 3 — the in-handler `.filter()` on `type` is the accepted, D-08-locked approach; do NOT add a `by_clerkUserId_and_type` compound index this phase)
  </read_first>
  <action>Add `export const listMyTopups = query({ args: {}, handler: ... })` to `convex/aiCredits.ts`, placed near `getMyCredits` (reuse the existing `query` import — do not add a new import). In the handler: call `await ctx.auth.getUserIdentity()`; if null, `return []` (empty array — matches the unauthenticated test expectation, distinct from getMyCredits' null). Otherwise query `creditTransactions` with `.withIndex("by_clerkUserId", q => q.eq("clerkUserId", identity.subject))`, `.order("desc")`, `.collect()`, then `return rows.filter(r => r.type === "topup")`. Do NOT accept any `clerkUserId`/`userId` argument (IDOR invariant — mitigates T-06-04). Do NOT add pagination (D-09). Do NOT modify the schema.</action>
  <verify>
    <automated>npx vitest run convex/aiCredits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/aiCredits.ts` contains `export const listMyTopups = query(` with `args: {}`
    - Handler derives identity via `ctx.auth.getUserIdentity()` and returns `[]` when identity is null (source assertion: the handler returns an empty array on the unauthenticated branch)
    - Handler uses `.withIndex("by_clerkUserId"` scoped to `identity.subject` — no client-supplied id argument exists in the file's new query
    - Handler filters to `type === "topup"` and does not call `.paginate()`
    - `npx vitest run convex/aiCredits.test.ts` passes all listMyTopups cases (GREEN) with no regression to existing describe blocks
    - `convex/schema.ts` is unchanged (no new index added this phase)
  </acceptance_criteria>
  <done>listMyTopups is implemented; all four new tests pass and no existing aiCredits test regresses.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                                    |
| --------------------- | -------------------------------------------------------------------------------------------------------------- |
| client → Convex query | A signed-in browser client calls `listMyTopups`; the caller's identity is the only trusted authorization input |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                                                                                                                                               |
| --------- | ---------------------- | ------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-04   | Information Disclosure | `convex/aiCredits.ts` `listMyTopups` | mitigate    | Zero-argument query; `clerkUserId` derived exclusively from `ctx.auth.getUserIdentity().subject`, never a client argument — mirrors `getMyCredits`/`getSubscription` (03-REVIEW.md CR-01). Cross-user isolation is proven by Task 1's Test 3. |
| T-06-SC   | Tampering              | npm/pip/cargo installs               | accept      | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A — `convex` already installed). No install task exists in this plan.                                                                                                        |

</threat_model>

<verification>
- `npx vitest run convex/aiCredits.test.ts` is green.
- `grep -n "clerkUserId" convex/aiCredits.ts` shows the new query never reads a client-supplied id (identity-derived only).
</verification>

<success_criteria>

- `listMyTopups` exists, is identity-derived, returns only topups newest-first, and returns `[]` when unauthenticated.
- All new tests pass; no existing test regresses; schema unchanged.
  </success_criteria>

<output>
Create `.planning/phases/06-settings-page/06-01-SUMMARY.md` when done.
</output>
