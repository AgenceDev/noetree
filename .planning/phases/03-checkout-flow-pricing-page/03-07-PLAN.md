---
phase: 03-checkout-flow-pricing-page
plan: 07
type: execute
wave: 2
depends_on: ["03-06"]
files_modified: []
autonomous: false
requirements: [PAY-01, PAY-02]
gap_closure: true
user_setup:
  - service: stripe
    why: "Re-running the failed UAT tests requires a real Stripe test-mode payment and a local webhook forwarder — neither is automatable in CI"
    env_vars:
      - name: STRIPE_WEBHOOK_SECRET
        source: "The `whsec_...` value printed by `stripe listen` at startup — must be copied into .env.local and the dev server restarted"
    dashboard_config:
      - task: "Confirm checkout.session.completed shows a successful (200) delivery"
        location: "Stripe Dashboard (test mode) → Developers → Events / Webhooks (or the `stripe listen` terminal output)"
      - task: "Confirm only ONE Customer object exists for the test user's email (no duplicate on repeat checkout)"
        location: "Stripe Dashboard (test mode) → Customers"

must_haves:
  truths:
    - "With `stripe listen --forward-to localhost:3000/api/webhooks/stripe` running and its `whsec_...` secret synced to .env.local, a real test-mode payment causes `/en/checkout/success` to transition from the loading state to 'Subscription active' (closes Test 2)."
    - "As the now-active user, clicking 'Upgrade to Pro' again short-circuits straight to the success page with no new Stripe Checkout Session, and only one Stripe Customer exists for the email (closes Test 3, D-06/D-08)."
  artifacts: []
  key_links:
    - from: "Stripe hosted checkout"
      to: "Convex subscriptions row"
      via: "Phase 2 webhook (checkout.session.completed) forwarded by a running `stripe listen`"
      pattern: "webhook"
---

<objective>
Re-run the two UAT tests that failed in 03-HUMAN-UAT.md (Test 2 and Test 3), this time with the environmental prerequisite the debug session identified as the actual root cause: a `stripe listen` forwarder actively running with a webhook secret that matches `.env.local`'s `STRIPE_WEBHOOK_SECRET`. There is NO code to fix for the environmental root cause of Test 2 — the fix is procedural. The code fixes from Plan 03-06 have already landed, so any failure this time will now surface a readable console error (webhook 400/500 logging + distinguishable action errors) instead of failing silently.

Purpose: Close the environmental/procedural half of the Phase 3 UAT gaps and confirm ROADMAP SC3 (webhook-confirmed activation) and SC4 (no duplicate customer) against live Stripe test mode.
Output: Human sign-off (no file artifacts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-checkout-flow-pricing-page/03-HUMAN-UAT.md
@.planning/phases/03-checkout-flow-pricing-page/03-04-PLAN.md
@.planning/debug/success-page-stuck-loading.md
@.planning/debug/repeat-checkout-not-short-circuiting.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 03-06 added developer-visible logging so this re-run is diagnosable: the webhook route now logs 400 signature-verification and 500 processing failures, `createCheckoutSession` catches and re-throws a distinguishable `subscriptionLookupFailed` on any Convex auth-handshake/query failure, and the pricing page `console.error`s the real error. No code fix closes Test 2's root cause — the diagnosed cause was environmental (the local `stripe listen` forwarder was not running, or its secret was out of sync with `.env.local`'s `STRIPE_WEBHOOK_SECRET`), so the webhook never wrote the Convex `subscriptions` row. This checkpoint re-runs Tests 2 and 3 with that prerequisite correctly in place.

    The full manual checklist and setup (APP_URL, forwarder, test card) documented in 03-04-PLAN.md still applies — this checkpoint re-verifies specifically the two tests that failed.

  </what-built>
  <how-to-verify>
    1. Start the dev server (`npm run dev`). In a SECOND, dedicated terminal, start the forwarder and leave it running for the entire test: `stripe listen --forward-to localhost:3000/api/webhooks/stripe`.
    2. Copy the `whsec_...` value it prints at startup into `.env.local` as `STRIPE_WEBHOOK_SECRET`, then RESTART `npm run dev` so the new secret is loaded. (Each `stripe listen` session mints its own secret — a stale value silently 400s every delivery.)
    3. (Test 2) Sign in as a test user, go to `https://localhost:3000/en/pricing`, click "Upgrade to Pro", and pay with test card `4242 4242 4242 4242` (any future expiry, any CVC). On redirect to `/en/checkout/success`, confirm it shows the loading state, then flips to "Subscription active" once the webhook fires. Confirm the `stripe listen` terminal shows `checkout.session.completed` delivered with a `200` response (and, if desired, the Stripe Dashboard → Developers → Events shows the same). It must NOT stay stuck on loading and must NOT show "active" before the webhook lands.
    4. (Test 3) As the same now-active user, revisit `/en/pricing` and click "Upgrade to Pro" again. Confirm it redirects straight to the success page with NO new `checkout.stripe.com` session (the D-08 active short-circuit). If any red error appears instead, open the browser console — Plan 03-06's logging will now print the real cause (`subscriptionLookupFailed` vs `checkoutSessionCreationFailed`); capture it in your response.
    5. In the Stripe Dashboard (test mode) → Customers, confirm only ONE Customer object exists for this email — no duplicate was created (SC4 / D-06).
  </how-to-verify>
  <resume-signal>Type "approved" once Test 2 (webhook-confirmed activation) and Test 3 (short-circuit + single customer) both pass, or paste the console/webhook error now visible thanks to Plan 03-06's logging.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                                              |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| Stripe hosted checkout → app webhook → Convex | Payment confirmation crosses from Stripe into the Phase 2 webhook, the sole writer of subscription state |

## STRIDE Threat Register

| Threat ID | Category    | Component                                                         | Disposition | Mitigation Plan                                                                                                                                                                 |
| --------- | ----------- | ----------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-07   | Repudiation | duplicate/overlapping subscription or customer on repeat checkout | mitigate    | Step 4-5 re-verifies the D-08 active short-circuit and D-06 customer reuse hold end-to-end against live Stripe, preventing duplicate customers/subscriptions (SC4).             |
| T-03-10   | Spoofing    | forged webhook delivery accepted with a mismatched secret         | accept      | The route already 400s on signature mismatch (verified by unit tests); this checkpoint's secret-sync step ensures legitimate deliveries succeed rather than weakening the gate. |

</threat_model>

<verification>
- ROADMAP SC3 (webhook-confirmed "Subscription active") and SC4 (no duplicate customer) confirmed by human sign-off with the forwarder running and secret synced.
- Full automated suite already green from Plan 03-06 and Waves 1-2.
</verification>

<success_criteria>
A human, with `stripe listen` running and `STRIPE_WEBHOOK_SECRET` synced, has completed a real test-mode payment and seen the success page confirm activation (Test 2), verified the repeat-checkout short-circuit with no new session and no duplicate customer (Test 3) — or has captured the now-readable console/webhook error for further diagnosis.
</success_criteria>

<output>
Create `.planning/phases/03-checkout-flow-pricing-page/03-07-SUMMARY.md` when done
</output>
