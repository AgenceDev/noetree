---
phase: 03-checkout-flow-pricing-page
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/[locale]/(marketing)/checkout/success/page.tsx
  - app/[locale]/(marketing)/checkout/success/SuccessStatus.tsx
autonomous: true
requirements: [PAY-02]
user_setup: []

must_haves:
  truths:
    - "After checkout, the success page identifies the current user via server-side Clerk auth() and passes clerkUserId to a reactive client component"
    - "While the subscription is not yet confirmed active, the page shows a loading state (spinner + 'Confirming your subscription…'), never an optimistic 'active'"
    - "The instant the Phase 2 webhook writes an active subscriptions row, the page re-renders to 'Subscription active' via the reactive Convex query (no polling)"
    - "If the row is still not active after ~18 seconds, the page shows reassurance copy + a manual Refresh control"
  artifacts:
    - path: "app/[locale]/(marketing)/checkout/success/page.tsx"
      provides: "RSC auth() boundary that guards and renders SuccessStatus"
      contains: "auth"
    - path: "app/[locale]/(marketing)/checkout/success/SuccessStatus.tsx"
      provides: "Reactive client confirmation with loading/timeout/confirmed states"
      contains: "convexQuery"
  key_links:
    - from: "app/[locale]/(marketing)/checkout/success/page.tsx"
      to: "SuccessStatus"
      via: "clerkUserId prop from server-side auth()"
      pattern: "SuccessStatus"
    - from: "app/[locale]/(marketing)/checkout/success/SuccessStatus.tsx"
      to: "api.subscriptions.getSubscription"
      via: "reactive convexQuery over websocket"
      pattern: "getSubscription"
---

<objective>
Implement the post-checkout confirmation page (PAY-02): a server component that resolves the current user via Clerk `auth()` and a reactive client child that watches the Convex `subscriptions` row and transitions loading → confirmed the instant the Phase 2 webhook lands, with a timeout/refresh fallback.

Purpose: Deliver the "in-app confirmation after activation" half of the phase, driving the Wave 0 SuccessStatus test to green. This page only READS Convex — it never grants access itself.
Output: `checkout/success/page.tsx` (RSC boundary) and `SuccessStatus.tsx` (reactive client component).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-checkout-flow-pricing-page/03-CONTEXT.md
@.planning/phases/03-checkout-flow-pricing-page/03-RESEARCH.md
@.planning/phases/03-checkout-flow-pricing-page/03-PATTERNS.md
@.planning/phases/03-checkout-flow-pricing-page/03-UI-SPEC.md

<interfaces>
<!-- Read-only consumption of the Convex query written in Phase 2. Do NOT modify convex/subscriptions.ts. -->

From convex/subscriptions.ts:

```typescript
// query getSubscription: args { clerkUserId: string } -> doc | null
// doc.status: "active" | "canceled" | "past_due"
```

Reactive client pattern (from app/[locale]/notes/page.tsx):
`useQuery(convexQuery(api.subscriptions.getSubscription, { clerkUserId }))` — websocket-pushed, re-renders on server row change.

Clerk server: `auth()` -> `{ userId }`. i18n internal redirect: `redirect` from `@/i18n/routing`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Success page RSC auth boundary</name>
  <files>app/[locale]/(marketing)/checkout/success/page.tsx</files>
  <read_first>
    - app/[locale]/layout.tsx (params: Promise<{ locale }> await + destructure convention; guard-then-render shape)
    - i18n/routing.ts (locale-aware `redirect` for the internal guard redirect back to /pricing)
    - .planning/phases/03-checkout-flow-pricing-page/03-RESEARCH.md (Pattern 2 — RSC boundary; D-12 rationale)
    - .planning/phases/03-checkout-flow-pricing-page/03-CONTEXT.md (D-12)
  </read_first>
  <action>
    Create `app/[locale]/(marketing)/checkout/success/page.tsx` as an async server component. Await `params` (`Promise<{ locale: string }>`) and destructure `locale`; validate `locale` against `routing.locales` and default/guard on miss (T-03-01). Call `const { userId } = await auth()` from `@clerk/nextjs/server`; if there is no `userId`, redirect to `/${locale}/pricing` using the i18n `redirect` from `@/i18n/routing` (internal route). Otherwise render `<SuccessStatus clerkUserId={userId} />`.

    Do NOT fetch the Stripe Checkout Session server-side and do NOT use the `session_id` query param as a lookup key (D-12) — Convex is the sole source of truth. The `session_id` param may be read only as an optional debugging reference, never to grant or display entitlement.

  </action>
  <verify>
    <automated>test -f "app/[locale]/(marketing)/checkout/success/page.tsx" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "checkout/success/page" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` imports `auth` from `@clerk/nextjs/server` and renders `SuccessStatus` with a `clerkUserId` prop sourced from `auth()`.
    - No import of the Stripe SDK appears in this file (grep for `stripe` returns nothing) — confirming D-12 (no server-side Session fetch).
    - The no-user guard redirects to `/${locale}/pricing` via the `@/i18n/routing` redirect.
    - TypeScript reports no errors for this file.
  </acceptance_criteria>
  <done>RSC boundary resolves the user server-side and hands clerkUserId to the reactive child; no Stripe/session_id dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SuccessStatus reactive confirmation component</name>
  <files>app/[locale]/(marketing)/checkout/success/SuccessStatus.tsx</files>
  <read_first>
    - app/[locale]/(marketing)/checkout/success/SuccessStatus.test.tsx (the RED contract this task makes green — loading/active/timeout assertions)
    - app/[locale]/notes/page.tsx (useQuery(convexQuery(...)) reactive pattern)
    - components/ui/skeleton.tsx (loading primitive to reuse, D-10)
    - components/ui/button.tsx (Refresh control)
    - .planning/phases/03-checkout-flow-pricing-page/03-UI-SPEC.md (CheckoutSuccess copy, accent spinner color, max-w-md)
    - .planning/phases/03-checkout-flow-pricing-page/03-RESEARCH.md (Pattern 2; Pitfall 3 webhook ordering)
    - .planning/phases/03-checkout-flow-pricing-page/03-CONTEXT.md (D-09, D-10, D-11)
  </read_first>
  <behavior>
    - data undefined/pending → LoadingView: skeleton/spinner + "Confirming your subscription…" (D-10), never optimistic "active".
    - data.status === "active" → ConfirmedView: "Subscription active" + body + "Go to notes" link (D-09).
    - After 18000ms with status still not "active" → TimeoutView: "Still confirming…" + reassurance body + a manual "Refresh status" control (D-11), no infinite spinner, no auto-retry.
  </behavior>
  <action>
    Create `app/[locale]/(marketing)/checkout/success/SuccessStatus.tsx` as a `"use client"` component taking `{ clerkUserId: string }`. Use `useTranslations("CheckoutSuccess")` for all copy. Run the reactive query `useQuery(convexQuery(api.subscriptions.getSubscription, { clerkUserId }))` (always a real clerkUserId here — no `"skip"`). Track a `timedOut` boolean via `useState` set true by a `useEffect` `setTimeout` of 18000ms (D-11, mid-point of the 15-20s window), clearing the timer on unmount.

    Render precedence: if `data?.status === "active"` → ConfirmedView; else if `timedOut` → TimeoutView (with a Refresh control that calls `window.location.reload()` or refetches, per D-11 manual re-check); else → LoadingView built on `components/ui/skeleton.tsx` with the accent-colored spinner (UI-SPEC). Treat any non-active status within the window as "still confirming," not an error (Pitfall 3 — Stripe webhook ordering is not guaranteed). Constrain the layout to `max-w-md`. Do NOT introduce polling / setInterval — the query is websocket-reactive (D-09).

  </action>
  <verify>
    <automated>vitest run "app/[locale]/(marketing)/checkout/success/SuccessStatus.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `SuccessStatus.test.tsx` passes all states (green): loading copy, active confirmation, and timeout copy + Refresh control.
    - The component uses `convexQuery(api.subscriptions.getSubscription, { clerkUserId })`; `grep -v '^\s*//' SuccessStatus.tsx` finds zero occurrences of `setInterval` (D-09 no polling).
    - The timeout is 18000ms (grep finds `18000`).
    - "Subscription active" text is rendered only when `data?.status === "active"` — never rendered on the undefined/pending branch (D-10 no optimistic display).
  </acceptance_criteria>
  <done>Reactive confirmation transitions loading → confirmed on webhook write, with an 18s timeout/refresh fallback; component test green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                        |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| client → success page display | The client renders a status but cannot write it; entitlement source is Convex, written only by the Phase 2 webhook |

## STRIDE Threat Register

| Threat ID | Category                           | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                       |
| --------- | ---------------------------------- | ------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-03   | Tampering / Elevation of Privilege | success page displayed "active" state       | mitigate    | The success page is strictly read-only against the Convex `subscriptions` row and never writes entitlement; a user reaching /checkout/success without paying simply never sees "active." Actual enforcement is Phase 4's server-side Convex mutations, unaffected by anything rendered here (ASVS V4) |
| T-03-06   | Spoofing                           | clerkUserId used for the confirmation query | mitigate    | clerkUserId is resolved by server-side `auth()` in the RSC boundary (Task 1) and passed as a prop — the client component cannot substitute another user's id to observe their status                                                                                                                  |
| T-03-01   | Tampering                          | `locale` param on the success route         | mitigate    | Validate `locale` against `routing.locales` before the guard redirect (ASVS V5)                                                                                                                                                                                                                       |

</threat_model>

<verification>
- `vitest run "app/[locale]/(marketing)/checkout/success/SuccessStatus.test.tsx"` green.
- `npm run test:unit` overall green.
- Manual end-to-end confirmation is covered by Plan 04 (real webhook round-trip).
</verification>

<success_criteria>
After activation, a signed-in user lands on the success page and sees a loading state that flips to "Subscription active" the moment the Phase 2 webhook writes the active row, with an 18-second reassurance/refresh fallback (PAY-02).
</success_criteria>

<output>
Create `.planning/phases/03-checkout-flow-pricing-page/03-03-SUMMARY.md` when done
</output>
