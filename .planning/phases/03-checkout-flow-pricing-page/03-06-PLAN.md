---
phase: 03-checkout-flow-pricing-page
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[locale]/(marketing)/pricing/actions.ts
  - app/[locale]/(marketing)/pricing/actions.test.ts
  - app/[locale]/(marketing)/pricing/page.tsx
  - app/api/webhooks/stripe/route.ts
  - app/api/webhooks/stripe/route.test.ts
autonomous: true
requirements: [PAY-01, PAY-02]
gap_closure: true

must_haves:
  truths:
    - "A failure in the Convex subscription lookup (Clerk `getToken({template:'convex'})` handshake or the `getSubscription` query) inside `createCheckoutSession` is caught, logged, and re-thrown as a distinguishable error — it no longer propagates uncaught or looks identical to a Stripe Checkout API failure."
    - "When checkout fails on the pricing page, the actual thrown error is written to the browser console via `console.error`, so a developer can read the real cause instead of only the static generic red `checkoutError` string."
    - "Webhook signature-verification failures (400) and downstream processing failures (500) are logged server-side with developer-visible detail, so a stopped/mismatched `stripe listen` forwarder is diagnosable locally instead of failing silently."
  artifacts:
    - path: "app/[locale]/(marketing)/pricing/actions.ts"
      provides: "Dedicated try/catch around the auth-handshake + convex.query lookup that logs and re-throws a distinguishable error"
      contains: "subscriptionLookupFailed"
    - path: "app/[locale]/(marketing)/pricing/page.tsx"
      provides: "console.error of the real thrown error in handleUpgrade's catch block"
      contains: "console.error"
    - path: "app/api/webhooks/stripe/route.ts"
      provides: "console.error logging on both the 400 signature-failure path and the 500 processing-failure path"
      contains: "console.error"
  key_links:
    - from: "app/[locale]/(marketing)/pricing/actions.ts"
      to: "app/[locale]/(marketing)/pricing/page.tsx"
      via: "thrown Error caught by handleUpgrade and logged via console.error"
      pattern: "console\\.error"
---

<objective>
Close the code-level defects diagnosed in the Phase 3 UAT (03-HUMAN-UAT.md, Test 3) and add the developer-visible logging recommended for Test 2's environmental root cause. This plan makes checkout and webhook failures diagnosable and prevents the uncaught-exception path that produced the unreadable red error the user could not capture.

Three targeted, low-risk changes across three files (no new dependencies, no new UI copy):

1. `actions.ts` — wrap the new (untested-until-UAT) Clerk→Convex auth handshake + `getSubscription` query in a dedicated try/catch so a handshake/query failure is caught, logged, and re-thrown as a distinguishable error instead of propagating uncaught into the pricing page's bare catch.
2. `page.tsx` — log the real thrown error in `handleUpgrade`'s catch block so the actual cause is readable during development.
3. `route.ts` — log the webhook signature-verification (400) and processing (500) failure paths so a misconfigured/stopped local `stripe listen` forwarder is visible instead of failing silently.

Purpose: Make the two UAT failures diagnosable and fix the genuine uncaught-exception bug. The environmental root cause of Test 2 (forwarder not running) is closed separately by the manual re-verification in Plan 03-07.
Output: Three edited source files + two edited test files, all unit tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-checkout-flow-pricing-page/03-HUMAN-UAT.md
@.planning/debug/repeat-checkout-not-short-circuiting.md
@.planning/debug/success-page-stuck-loading.md

<interfaces>
<!-- Current shape of the three files under change. Executor should edit these
     directly — no codebase exploration needed. -->

app/[locale]/(marketing)/pricing/actions.ts — createCheckoutSession(locale: string): Promise<void>

- After the signed-out guard, the auth handshake + lookup currently sit OUTSIDE any try/catch:
  const convexToken = await getToken({ template: "convex" });
  if (convexToken) convex.setAuth(convexToken);
  const existing = await convex.query(api.subscriptions.getSubscription, {});
- The short-circuit `if (existing?.status === "active") { localeRedirect(...); return; }` and the
  Stripe call (already wrapped in its own try/catch that throws new Error("checkoutSessionCreationFailed"))
  follow. Note: localeRedirect/redirect throw Next redirect-control errors by design — those MUST stay
  OUTSIDE the new try/catch so they are never swallowed.

app/[locale]/(marketing)/pricing/page.tsx — handleUpgrade() currently:
try { await createCheckoutSession(locale); }
catch { setError(true); }
finally { setIsPending(false); }

- The bare `catch {}` discards the real error; UI only ever renders the static t("checkoutError").

app/api/webhooks/stripe/route.ts — POST(req): Promise<Response>

- Signature failure path: `} catch { return new Response("Invalid signature", { status: 400 }); }`
  (currently a bare catch with no binding, no logging).
- Processing failure path: `} catch (err) { ... return new Response("Webhook processing failed", { status: 500 }); }`
  (already binds err for the ConvexError check, but never logs it).
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard and log the Convex auth-handshake + subscription lookup in actions.ts</name>
  <files>app/[locale]/(marketing)/pricing/actions.ts, app/[locale]/(marketing)/pricing/actions.test.ts</files>
  <behavior>
    - When `getToken`/`convex.setAuth`/`convex.query(getSubscription)` throws, `createCheckoutSession` catches it, logs it via `console.error`, and re-throws `new Error("subscriptionLookupFailed")` — NOT the generic "checkoutSessionCreationFailed" and NOT an uncaught propagation. `stripe.checkout.sessions.create` is never reached in this case.
    - Existing behavior is preserved: signed-out → redirectToSignIn, no Stripe call; already-active → localeRedirect to success, no Stripe call; no/inactive row → Stripe session created with correct metadata + conditional customer reuse.
    - The `localeRedirect(...)` short-circuit and the sign-in `redirectToSignIn` path continue to throw/return exactly as before (their Next redirect-control throws are NOT caught by the new lookup try/catch).
  </behavior>
  <action>
    In `actions.ts`, wrap the auth-handshake + lookup block (the `getToken({ template: "convex" })`, `convex.setAuth(convexToken)`, and `convex.query(api.subscriptions.getSubscription, {})` calls, lines ~30-32) in a dedicated try/catch. Declare `existing` before the try so the short-circuit and Stripe branches downstream still read it. In the catch, call `console.error("createCheckoutSession: subscription lookup/auth handshake failed", err)` then `throw new Error("subscriptionLookupFailed")`. Do NOT log `convexToken` or any token/secret value (see threat_model T-03-08). Keep the existing short-circuit `if (existing?.status === "active")` and the existing narrow Stripe try/catch (throwing "checkoutSessionCreationFailed") OUTSIDE this new try/catch — the two failure modes must stay distinguishable by their thrown message. This directly closes the SECONDARY root cause in repeat-checkout-not-short-circuiting.md (uncaught exception from the unguarded query feeding page.tsx's bare catch).

    In `actions.test.ts`, add a RED-first test: mock `mockAuth` to resolve a valid `userId` + `getToken` (as the existing active/new-row tests do), make `mockConvexQuery.mockRejectedValueOnce(new Error("convex auth handshake failed"))`, and assert `createCheckoutSession("en")` rejects with `/subscriptionLookupFailed/` AND `expect(mockSessionsCreate).not.toHaveBeenCalled()`. Run it and confirm it fails against the current unguarded code before implementing, then confirm green after.

  </action>
  <verify>
    <automated>npx vitest run actions.test</automated>
  </verify>
  <done>New test proves a thrown Convex query/handshake now surfaces as `Error("subscriptionLookupFailed")` with no Stripe call; all pre-existing actions.test.ts cases still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Log the real checkout error in page.tsx's catch block</name>
  <files>app/[locale]/(marketing)/pricing/page.tsx</files>
  <action>
    In `handleUpgrade`, change the bare `catch {` to `catch (err) {`, and as the first statement inside add `console.error("Checkout upgrade failed", err);` before `setError(true);`. Leave the rendered UI (`t("checkoutError")`) unchanged — no new i18n strings (the ask is diagnosability, not new copy). This directly addresses the UAT report "I see an issue but I can't log it, there is a red text under it": the developer can now read the actual thrown error (e.g. `subscriptionLookupFailed` vs `checkoutSessionCreationFailed`) in the browser console. Do not log any token/secret; `err` here is a plain Error message crossing the Server Action boundary.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -v '^\s*//' "app/[locale]/(marketing)/pricing/page.tsx" | grep -c 'console\.error'</automated>
  </verify>
  <done>tsc passes with no errors and the grep gate returns ≥1 (a non-comment `console.error` exists in handleUpgrade's catch).</done>
</task>

<task type="auto">
  <name>Task 3: Log webhook signature (400) and processing (500) failures in route.ts</name>
  <files>app/api/webhooks/stripe/route.ts, app/api/webhooks/stripe/route.test.ts</files>
  <action>
    In `route.ts`: (a) change the signature-verification `} catch {` to `} catch (err) {` and add `console.error("[stripe webhook] signature verification failed (400) — check STRIPE_WEBHOOK_SECRET matches the active `stripe listen` session", err)` before `return new Response("Invalid signature", { status: 400 })`. (b) In the existing processing `catch (err)` block, add `console.error("[stripe webhook] processing failed (500)", err)` immediately before the final `return new Response("Webhook processing failed", { status: 500 })` (leave the ConvexError→400 Unauthorized branch's return unchanged; only the 500 fall-through needs logging). Do NOT log `rawBody`, `signature`, or any secret (threat_model T-03-08). This is the DX improvement recommended in success-page-stuck-loading.md so a stopped/mismatched local forwarder is visible instead of silently 400-ing.

    In `route.test.ts`, extend the existing "returns 400 when the Stripe signature is invalid" test: add `const errorSpy = vi.spyOn(console, "error").mockImplementation(() => {})` and assert `expect(errorSpy).toHaveBeenCalled()`, then restore the spy. This guards against the logging silently regressing.

  </action>
  <verify>
    <automated>npx vitest run route.test</automated>
  </verify>
  <done>route.test.ts passes including the new assertion that the 400 signature-failure path calls console.error; existing 200/400/500 status-code tests still pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                                         |
| ------------------------------------------------- | ----------------------------------------------------------------------------------- |
| browser → Server Action (`createCheckoutSession`) | Untrusted client invokes the action; thrown error messages cross back to the client |
| Stripe → webhook route (`/api/webhooks/stripe`)   | Untrusted external POST; signature verification is the trust gate                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                                | Disposition | Mitigation Plan                                                                                                                                                                                                            |
| --------- | ---------------------- | -------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-08   | Information Disclosure | new `console.error` calls in actions.ts / page.tsx / route.ts                                            | mitigate    | Log only the caught error object/message and static context strings — never `convexToken`, `rawBody`, `stripe-signature`, `STRIPE_WEBHOOK_SECRET`, or any env secret. Task actions call this out explicitly for each file. |
| T-03-09   | Information Disclosure | distinguishable `subscriptionLookupFailed` vs `checkoutSessionCreationFailed` error thrown to the client | accept      | Both are opaque, non-sensitive identifier strings (no stack, no secret); the client UI still renders only the generic `t("checkoutError")`. The distinction aids the developer via console, not the attacker.              |

</threat_model>

<verification>
- `npx vitest run` (full unit suite, 52+ tests) stays green including the two new/extended assertions.
- `npx tsc --noEmit` clean.
- Manual code review confirms no token/secret is passed to any `console.error`.
</verification>

<success_criteria>
A thrown Convex auth-handshake/query failure in `createCheckoutSession` is caught, logged, and re-thrown as `subscriptionLookupFailed` (distinguishable from Stripe failures); the pricing page logs the real error to the console; and the webhook route logs both its 400 signature-failure and 500 processing-failure paths — all with no secret leakage and all unit tests green.
</success_criteria>

<output>
Create `.planning/phases/03-checkout-flow-pricing-page/03-06-SUMMARY.md` when done
</output>
