---
phase: 03-checkout-flow-pricing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components/ui/badge.tsx
  - .env.example
  - .env.local
  - messages/en.json
  - messages/fr.json
  - app/[locale]/(marketing)/pricing/actions.test.ts
  - app/[locale]/(marketing)/checkout/success/SuccessStatus.test.tsx
  - cypress/integration/pricing.spec.ts
  - cypress/integration/checkout-redirect.spec.ts
autonomous: false
requirements: [PLAN-01, PAY-01, PAY-02]
user_setup: []

must_haves:
  truths:
    - "The shadcn badge primitive and jsdom test environment are available"
    - "APP_URL is documented in .env.example and set locally so the Server Action can build absolute Stripe redirect URLs"
    - "D-03: Pricing and CheckoutSuccess i18n copy exists in both en and fr, sourced from messages/en.json + messages/fr.json per the next-intl locale routing convention"
    - "D-06, D-07, D-08: Four RED test files exist that fail because their target modules are not yet implemented — the actions.test.ts scaffold encodes the customer-reuse (D-06), dual clerkUserId metadata (D-07), and already-active short-circuit (D-08) contracts the Wave 2 Server Action must satisfy"
  artifacts:
    - path: "components/ui/badge.tsx"
      provides: "shadcn Badge primitive for the 'Current plan' label"
      contains: "Badge"
    - path: "messages/en.json"
      provides: "Pricing + CheckoutSuccess namespaces"
      contains: "Pricing"
    - path: "messages/fr.json"
      provides: "Pricing + CheckoutSuccess namespaces (French)"
      contains: "Pricing"
    - path: "app/[locale]/(marketing)/pricing/actions.test.ts"
      provides: "RED unit test for createCheckoutSession"
    - path: "cypress/integration/pricing.spec.ts"
      provides: "RED e2e test for pricing page render"
  key_links:
    - from: ".env.example"
      to: "Server Action success_url/cancel_url"
      via: "APP_URL env var"
      pattern: "APP_URL"
---

<objective>
Establish the shared tooling/config/test foundation every task in this phase depends on: the shadcn `badge` primitive, the jsdom Vitest environment, the new `APP_URL` env var, the `Pricing`/`CheckoutSuccess` i18n namespaces, and the four RED (failing) test scaffolds that the implementation plans in Wave 2 will drive to green.

Note: the marketing route-group shell (`app/[locale]/(marketing)/layout.tsx`) and the root-layout decomposition that makes it chrome-free are NOT in this plan — they are owned by Plan 03-05 (Wave 1), because a chrome-free `(marketing)` shell requires pulling the app shell OUT of the root layout, not just adding a nested route-group layout. This plan and 03-05 run in parallel in Wave 1 with no file overlap.

Purpose: Interface-first + test-first. Downstream plans build against contracts and pre-written failing tests, not a blank slate.
Output: Config/tooling changes, i18n copy, and 4 failing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-checkout-flow-pricing-page/03-CONTEXT.md
@.planning/phases/03-checkout-flow-pricing-page/03-RESEARCH.md
@.planning/phases/03-checkout-flow-pricing-page/03-PATTERNS.md
@.planning/phases/03-checkout-flow-pricing-page/03-UI-SPEC.md
@.planning/phases/03-checkout-flow-pricing-page/03-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify jsdom package legitimacy before install</name>
  <what-built>Nothing yet — this gate precedes the only package-manager install in this phase.</what-built>
  <how-to-verify>
    `jsdom` is required as a Vitest devDependency for the `SuccessStatus.test.tsx` component test (03-VALIDATION.md Wave 0). It is NOT covered by 03-RESEARCH.md's Package Legitimacy Audit (which concluded no npm installs and skipped the gate), so it is treated as [ASSUMED] and must be human-verified.
    1. Open https://npmjs.com/package/jsdom
    2. Confirm it is the canonical W3C-standards DOM implementation (weekly downloads in the tens of millions, repo `jsdom/jsdom`, MIT license).
    3. Confirm the version to be installed resolves from the npm registry (not a typosquat — exact name `jsdom`, no scope, no suffix).
  </how-to-verify>
  <resume-signal>Type "approved" to allow `pnpm add -D jsdom`, or describe concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install tooling + primitive, add APP_URL env, add i18n namespaces</name>
  <files>package.json, components/ui/badge.tsx, .env.example, .env.local, messages/en.json, messages/fr.json</files>
  <read_first>
    - .env.example (current STRIPE_WEBHOOK_SECRET per-environment comment convention to mirror)
    - .env.local (confirm APP_URL absent before adding; do not touch existing secret values)
    - messages/en.json (existing flat-namespace convention: Dashboard/Notes keys)
    - messages/fr.json (must mirror every en.json key)
    - components/ui/skeleton.tsx (shadcn primitive shape: data-slot + cn())
    - .planning/phases/03-checkout-flow-pricing-page/03-UI-SPEC.md (Copywriting Contract table — exact copy strings)
  </read_first>
  <action>
    1. Install jsdom: `pnpm add -D jsdom` (approved in Task 1).
    2. Install the badge primitive via the official shadcn registry: `npx shadcn@latest add badge` — produces components/ui/badge.tsx matching the components.json preset (style new-york, baseColor neutral). Do NOT hand-write it.
    3. In .env.example add a documented `APP_URL` entry: server-only (NO NEXT_PUBLIC_ prefix), one value per environment, following the same comment style as the STRIPE_WEBHOOK_SECRET block. Document that it is the app's absolute origin used to build Stripe `success_url`/`cancel_url`, local value `https://localhost:3000` (matches the `next dev --experimental-https` dev script).
    4. In .env.local set `APP_URL=https://localhost:3000` (non-secret known local value; leave all existing keys untouched).
    5. In messages/en.json add two new top-level namespaces `Pricing` and `CheckoutSuccess` with keys covering every UI-SPEC Copywriting Contract string: page heading "Simple, transparent pricing", subheading, Free price line "Free"/"Up to 20 notes", Pro price line "9€ / month"/"Unlimited notes · 100 AI credits/month", CTA "Upgrade to Pro", badge "Current plan", and for CheckoutSuccess: loading heading "Confirming your subscription…", loading body, timeout heading "Still confirming…", timeout body, timeout action "Refresh status", confirmed heading "Subscription active", confirmed body + "Go to notes" link, error "Something went wrong starting checkout. Please try again — if it keeps happening, contact support."
    6. Mirror the identical key structure in messages/fr.json with French translations. Never add a key to one locale file without the other.
  </action>
  <verify>
    <automated>node -e "const e=require('./messages/en.json'),f=require('./messages/fr.json'); if(!e.Pricing||!e.CheckoutSuccess||!f.Pricing||!f.CheckoutSuccess) process.exit(1); const k=o=>Object.keys(o).sort().join(','); if(k(e.Pricing)!==k(f.Pricing)||k(e.CheckoutSuccess)!==k(f.CheckoutSuccess)) process.exit(2)"</automated>
  </verify>
  <acceptance_criteria>
    - `components/ui/badge.tsx` exists and exports `Badge` using `cn()` from `@/lib/utils`.
    - `pnpm ls jsdom` reports jsdom present as a devDependency.
    - `.env.example` contains a line beginning `APP_URL=` with a server-only comment (no `NEXT_PUBLIC_` on it).
    - `.env.local` contains `APP_URL=https://localhost:3000`.
    - `messages/en.json` and `messages/fr.json` each contain `Pricing` and `CheckoutSuccess` namespaces with identical key sets per locale (verify command exits 0).
  </acceptance_criteria>
  <done>badge primitive, jsdom, APP_URL (example+local), and both-locale Pricing/CheckoutSuccess namespaces are all present.</done>
</task>

<task type="auto">
  <name>Task 3: Four RED test scaffolds</name>
  <files>app/[locale]/(marketing)/pricing/actions.test.ts, app/[locale]/(marketing)/checkout/success/SuccessStatus.test.tsx, cypress/integration/pricing.spec.ts, cypress/integration/checkout-redirect.spec.ts</files>
  <read_first>
    - app/api/webhooks/stripe/route.test.ts (vi.mock("stripe")/vi.mock("convex/browser") pattern to mirror in actions.test.ts)
    - cypress/integration/app.spec.ts (setupClerkTestingToken + cy.visit shape)
    - components/ui/skeleton.tsx (loading primitive referenced by SuccessStatus)
    - .planning/phases/03-checkout-flow-pricing-page/03-VALIDATION.md (Per-Task Verification Map — the exact behaviors each test asserts)
    - .planning/phases/03-checkout-flow-pricing-page/03-RESEARCH.md (Patterns 1-3 — the target module signatures the tests are written against)
  </read_first>
  <action>
    Create four RED test files whose targets do not exist yet, so each currently FAILS (this is intended — Wave 2 makes them pass). These live under the `(marketing)` route group, whose layout is created by Plan 03-05 (Wave 1); the tests do not depend on that layout existing to be written or to fail RED.
    1. `app/[locale]/(marketing)/pricing/actions.test.ts` — Vitest unit. Mirror the webhook test's mocking strategy: `vi.mock("stripe")` exposing `checkout.sessions.create`, `vi.mock("convex/browser")` exposing `query`, and a new mock of `@clerk/nextjs/server` `auth()` resolving `{ userId, redirectToSignIn }`. In beforeAll set process.env STRIPE_SECRET_KEY, STRIPE_PRO_PRICE_ID, NEXT_PUBLIC_CONVEX_URL, APP_URL, then `await import("./actions")`. Write three `it` cases against `createCheckoutSession(locale)`: (a) signed-out → `redirectToSignIn` called with a returnBackUrl, no `sessions.create` call; (b) existing subscription with status "active" → internal redirect to the success page, no `sessions.create` call (D-08); (c) no/inactive subscription → `sessions.create` called once with `mode:"subscription"`, `line_items[0].price` = STRIPE_PRO_PRICE_ID, `metadata.clerkUserId` AND `subscription_data.metadata.clerkUserId` both equal the auth() userId (D-07), and `customer` set to the existing row's stripeCustomerId only when a row exists (D-06).
    2. `app/[locale]/(marketing)/checkout/success/SuccessStatus.test.tsx` — Vitest component test. Add a `// @vitest-environment jsdom` docblock as line 1. Mock `@convex-dev/react-query` convexQuery + `@tanstack/react-query` useQuery to control returned data. Assert: data undefined → loading copy "Confirming your subscription…"; data.status "active" → "Subscription active"; and (using fake timers) after the timeout window with a non-active status → "Still confirming…" + a "Refresh status" control. Target import: `./SuccessStatus` (does not exist yet).
    3. `cypress/integration/pricing.spec.ts` — e2e. Use setupClerkTestingToken; cy.visit the `/en/pricing` URL; assert the Free and Pro plan copy render (headings + price lines). No Stripe interaction.
    4. `cypress/integration/checkout-redirect.spec.ts` — e2e. Sign in a Clerk test user, visit `/en/pricing`, click "Upgrade to Pro", assert the resulting `cy.url()` includes `checkout.stripe.com` (does NOT complete a real card payment).
    Keep each test file self-contained; do not implement the target modules here.

  </action>
  <verify>
    <automated>test -f "app/[locale]/(marketing)/pricing/actions.test.ts" && test -f "app/[locale]/(marketing)/checkout/success/SuccessStatus.test.tsx" && test -f cypress/integration/pricing.spec.ts && test -f cypress/integration/checkout-redirect.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exist at the exact paths in the VALIDATION Wave 0 list.
    - `SuccessStatus.test.tsx` line 1 is the docblock `// @vitest-environment jsdom`.
    - `vitest run "app/[locale]/(marketing)/pricing/actions.test.ts"` FAILS (RED) because `./actions` does not resolve yet — this failure is the expected pre-implementation state.
  </acceptance_criteria>
  <done>Four failing test scaffolds define the Wave 2 contracts.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                             |
| -------------------------- | ----------------------------------------------------------------------- |
| npm registry → local build | Third-party package source (jsdom, shadcn badge) crosses into the build |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                                   |
| --------- | ---------------------- | -------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-SC   | Tampering              | `pnpm add -D jsdom` supply chain | mitigate    | jsdom is [ASSUMED] (not in RESEARCH audit) → Task 1 blocking-human legitimacy checkpoint on npmjs.com before install; shadcn `badge` is copied from the official registry (no npm registry entry, no postinstall) so it carries no install-time supply-chain risk |
| T-03-CFG  | Information Disclosure | `APP_URL` env var                | accept      | APP*URL is a non-secret public origin (`https://localhost:3000` locally); deliberately server-only (no NEXT_PUBLIC* prefix) to match repo convention, but it holds no confidential value                                                                          |

</threat_model>

<verification>
- `npm run test:unit` runs (the new actions/SuccessStatus tests appear and are RED as designed).
- i18n key-parity check exits 0.
</verification>

<success_criteria>
badge + jsdom installed, APP_URL documented and set locally, both-locale Pricing/CheckoutSuccess copy present, and four RED Wave 0 test files in place.
</success_criteria>

<output>
Create `.planning/phases/03-checkout-flow-pricing-page/03-01-SUMMARY.md` when done
</output>
</content>
</invoke>
