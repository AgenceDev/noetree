---
phase: 03-checkout-flow-pricing-page
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: false
requirements: [PAY-01, PAY-02]
user_setup:
  - service: stripe
    why: "Manual test-mode checkout completion crosses into Stripe's hosted cross-origin domain, not automatable in CI"
    dashboard_config:
      - task: "Confirm no duplicate Customer object is created on repeat checkout"
        location: "Stripe Dashboard (test mode) -> Customers"

must_haves:
  truths:
    - "A real Stripe test-mode card payment completes and returns to the success page"
    - "The success page transitions to 'Subscription active' once the Phase 2 webhook writes the active row"
    - "Repeating checkout with the same signed-in user reuses the existing Stripe customer (no duplicate customer)"
  artifacts: []
  key_links:
    - from: "Stripe hosted checkout"
      to: "Convex subscriptions row"
      via: "Phase 2 webhook (checkout.session.completed)"
      pattern: "webhook"
---

<objective>
Gate the phase on the two manual-only verifications from 03-VALIDATION.md that no automated test can cover: a full real Stripe test-mode payment round-trip (proving the redirect → hosted checkout → webhook → reactive confirmation chain end to end) and duplicate-customer reuse (ROADMAP SC4). All automated coverage (unit + e2e) is already green from Waves 1-2; this plan verifies the parts that require a human at a browser and the Stripe Dashboard.

Purpose: Confirm ROADMAP success criteria 2, 3, and 4 against live Stripe test mode before `/gsd:verify-work`.
Output: Human sign-off (no file artifacts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-checkout-flow-pricing-page/03-VALIDATION.md
@.planning/phases/03-checkout-flow-pricing-page/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Waves 1-2 delivered: the pricing page + `createCheckoutSession` Server Action (redirects to Stripe Checkout with dual clerkUserId metadata and customer reuse) and the reactive success page (loading → "Subscription active"). All unit + Cypress tests are green. Remaining verification requires a real Stripe test-mode payment, which is manual-only per 03-VALIDATION.md.

    Before starting, ensure `APP_URL` is set for the environment under test: locally it is `https://localhost:3000` in `.env.local` (set in Plan 01). For staging/production, set `APP_URL` to the real origin in the hosting provider's env vars (parallel to how STRIPE_WEBHOOK_SECRET is set per environment). The Phase 2 webhook forwarder must be running locally (`stripe listen --forward-to localhost:3000/api/webhooks/stripe`).

  </what-built>
  <how-to-verify>
    1. Start the dev server (`npm run dev`) and, in a second terminal, the Stripe CLI forwarder: `stripe listen --forward-to localhost:3000/api/webhooks/stripe`.
    2. Sign in as a test user, navigate to `https://localhost:3000/en/pricing`, and click "Upgrade to Pro". Confirm the browser lands on a `checkout.stripe.com` hosted page (SC2).
    3. Complete payment with Stripe test card `4242 4242 4242 4242`, any future expiry, any CVC. Confirm you are redirected back to `/en/checkout/success`.
    4. Confirm the success page shows the loading state, then flips to "Subscription active" once the webhook fires and Convex writes the row (SC3). It must NOT show "active" before the webhook lands.
    5. Trigger "Upgrade to Pro" again as the same signed-in user. Confirm the Server Action short-circuits (redirects straight to the success page, no new Stripe Checkout) because status is already "active" (D-08).
    6. In the Stripe Dashboard (test mode) → Customers, confirm only ONE Customer object exists for this email — no duplicate was created (SC4 / D-06).
  </how-to-verify>
  <resume-signal>Type "approved" once SC2/SC3/SC4 are confirmed, or describe what failed</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                                                    |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Stripe hosted checkout → app webhook → Convex | Payment confirmation crosses from Stripe into the app's Phase 2 webhook, the sole writer of subscription state |

## STRIDE Threat Register

| Threat ID | Category               | Component                                               | Disposition | Mitigation Plan                                                                                                                                                                                  |
| --------- | ---------------------- | ------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-03-07   | Repudiation            | duplicate/overlapping subscription from repeat checkout | mitigate    | Manual step 5-6 verifies the D-08 active short-circuit and D-06 customer reuse hold end-to-end, preventing duplicate customers/subscriptions (SC4)                                               |
| T-03-03   | Elevation of Privilege | reaching /checkout/success without paying               | accept      | Verified by design in Plan 03: the success page only reads Convex; without a real webhook write it never shows "active." This plan confirms the positive path; the negative path needs no action |

</threat_model>

<verification>
- ROADMAP SC2 (redirect to Stripe Checkout), SC3 (webhook-confirmed success), SC4 (no duplicate customer) confirmed by human sign-off.
- Full automated suite (`npm run test:unit && npm run test-cypress`) green from Waves 1-2.
</verification>

<success_criteria>
A human has completed a real Stripe test-mode subscription, seen the success page confirm activation, and verified no duplicate Stripe customer is created on repeat checkout.
</success_criteria>

<output>
Create `.planning/phases/03-checkout-flow-pricing-page/03-04-SUMMARY.md` when done
</output>
