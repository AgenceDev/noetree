---
phase: 05-ai-credits-system
plan: 02
type: tdd
wave: 2
depends_on: [05-01]
files_modified:
  - convex/aiCredits.ts
  - convex/stripeWebhooks.ts
  - convex/aiCredits.test.ts
  - convex/stripeWebhooks.test.ts
autonomous: true
requirements: [PAY-05]
must_haves:
  truths:
    - "A checkout.session.completed event with mode 'payment' credits the resolved user's balance by exactly 50 and records a 'topup' creditTransactions row"
    - "Replaying the same payment-mode event (same stripeEventId) does not double-credit — second call returns alreadyProcessed and balance is unchanged"
    - "A payment-mode top-up for a user with no aiCredits row inserts a new row with balance 50"
    - "A checkout.session.completed event with mode 'subscription' (or mode absent, as in existing fixtures) still routes to upsertSubscription, unaffected"
  artifacts:
    - path: "convex/aiCredits.ts"
      provides: "addCredits internalMutation with idempotency + patch-or-insert +50"
      contains: "export const addCredits"
    - path: "convex/stripeWebhooks.ts"
      provides: "session.mode branch inside checkout.session.completed"
      contains: 'session.mode === "payment"'
    - path: "convex/stripeWebhooks.test.ts"
      provides: "payment-mode + idempotency + subscription-regression coverage"
      contains: "payment"
  key_links:
    - from: "convex/stripeWebhooks.ts checkout.session.completed"
      to: "internal.aiCredits.addCredits"
      via: "session.mode === 'payment' branch, amount hardcoded 50"
      pattern: "internal.aiCredits.addCredits"
    - from: "convex/aiCredits.ts addCredits"
      to: "processedStripeEvents (by_stripeEventId)"
      via: "idempotency check before crediting"
      pattern: "by_stripeEventId"
---

<objective>
Implement the `addCredits` internalMutation (idempotent, +50 balance, records a `"topup"` transaction) and extend the existing single-writer webhook dispatcher's `checkout.session.completed` case to branch on `session.mode === "payment"` → `addCredits`, leaving the subscription path untouched.

Purpose: This is the server-authoritative credit-granting half of PAY-05. The webhook dispatcher stays the single writer (D-14) — no new module, no new Stripe event type. Idempotency (mirroring `resetCredits`/`upsertSubscription`) is mandatory because Stripe delivers events at-least-once and can replay.

Output: `addCredits` implemented with an extended arg signature, one new branch in `stripeWebhooks.ts`, and convex-test coverage for payment-mode crediting, idempotency, and subscription-mode regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-credits-system/05-CONTEXT.md
@.planning/phases/05-ai-credits-system/05-RESEARCH.md
@.planning/phases/05-ai-credits-system/05-PATTERNS.md
@convex/_generated/ai/guidelines.md
@.planning/phases/05-ai-credits-system/05-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly, no exploration. -->

Existing addCredits stub — convex/aiCredits.ts:

- `addCredits = internalMutation({ args: { clerkUserId: v.string(), amount: v.number() }, handler: throws "Not implemented" })` — fill body AND extend args (Pitfall 2: current signature is too narrow for idempotent webhook use).

resetCredits idempotency + processedStripeEvents shape to mirror — convex/aiCredits.ts:

- `const already = await ctx.db.query("processedStripeEvents").withIndex("by_stripeEventId", q => q.eq("stripeEventId", args.stripeEventId)).unique(); if (already) return { alreadyProcessed: true };`
- patch-or-insert on aiCredits by_clerkUserId (identical branch)
- `ctx.db.insert("creditTransactions", { clerkUserId, type, amount, createdAt, stripePaymentIntentId? })`
- final `ctx.db.insert("processedStripeEvents", { stripeEventId, eventType, processedAt: Date.now() })`

creditTransactions schema (convex/schema.ts lines 61-71): `{ clerkUserId, type: union(deduction|topup|reset|refund), amount, createdAt, stripePaymentIntentId: v.optional(v.string()) }`.

Existing checkout.session.completed case to extend — convex/stripeWebhooks.ts lines 42-71:

- `const session = args.event.data.object; const clerkUserId = session.metadata?.clerkUserId;`
- currently unconditionally calls `internal.subscriptions.upsertSubscription` with stripeCustomerId/stripeSubscriptionId/status/currentPeriodEnd/cancelAtPeriodEnd.
- `invoice.paid` case (lines 113-129) is the same-file precedent for calling `internal.aiCredits.*` from the dispatcher.

Webhook test harness — convex/stripeWebhooks.test.ts:

- `seedCheckoutEvent(id, clerkUserId, stripeCustomerId, stripeSubscriptionId)` builds a fixture with NO `mode` field (its implicit `mode: undefined` must keep routing to the subscription branch — Pitfall 4).
- Auth-guard call shape: `t.action(api.stripeWebhooks.processWebhookEvent, { secret: process.env.INTERNAL_WEBHOOK_SECRET, event: {...} })`.
- The action requires the `secret` arg to equal `process.env.INTERNAL_WEBHOOK_SECRET`; existing tests set it — reuse that exact setup.

Stripe payment_intent availability (RESEARCH.md A2, LOW confidence): `session.payment_intent` may be a string, an expanded object, or absent on a raw webhook event. Read it defensively: `typeof session.payment_intent === "string" ? session.payment_intent : session.payment_intent?.id`. schema field is `v.optional` so a missing value degrades gracefully.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement addCredits internalMutation (idempotent +50)</name>
  <files>convex/aiCredits.ts, convex/aiCredits.test.ts</files>
  <read_first>
    - convex/aiCredits.ts (post-Plan-01 state — resetCredits reference, applyDeduction, imports)
    - convex/subscriptions.ts upsertSubscription (idempotency-check-first shape)
    - convex/schema.ts lines 55-77 (aiCredits + creditTransactions + processedStripeEvents)
    - .planning/phases/05-ai-credits-system/05-RESEARCH.md (Pitfall 2, Pitfall 5, "Don't Hand-Roll" idempotency row)
  </read_first>
  <behavior>
    - addCredits for a clerkUserId with balance=10 → patches balance to 60; inserts creditTransactions {type:"topup", amount:50, stripePaymentIntentId:"pi_x"}; inserts processedStripeEvents row.
    - addCredits for a clerkUserId with NO aiCredits row → inserts a new aiCredits row {balance:50, lastResetAt: now}.
    - addCredits called twice with the same stripeEventId → first credits +50, second returns { alreadyProcessed: true } and does not change balance or add a second topup transaction.
    - addCredits with an undefined stripePaymentIntentId → still credits +50, transaction row omits stripePaymentIntentId (or stores undefined) without a validator error.
  </behavior>
  <action>
    Replace the `addCredits` internalMutation in convex/aiCredits.ts. Extend `args` to `{ clerkUserId: v.string(), amount: v.number(), stripeEventId: v.string(), eventType: v.string(), stripePaymentIntentId: v.optional(v.string()) }` (Pitfall 2) and remove the `// LEAVE UNCHANGED — Phase 5` comment. Handler: (1) idempotency check against `processedStripeEvents` by `by_stripeEventId` — `if (already) return { alreadyProcessed: true };` (mirror resetCredits exactly). (2) read aiCredits by `by_clerkUserId` `.unique()`; if it exists `ctx.db.patch(row._id, { balance: row.balance + args.amount })`, else `ctx.db.insert("aiCredits", { clerkUserId: args.clerkUserId, balance: args.amount, lastResetAt: Date.now() })` (Pitfall 5 — patch throws on missing doc, insert path is mandatory). (3) `ctx.db.insert("creditTransactions", { clerkUserId: args.clerkUserId, type: "topup", amount: args.amount, createdAt: Date.now(), ...(args.stripePaymentIntentId ? { stripePaymentIntentId: args.stripePaymentIntentId } : {}) })`. (4) final `ctx.db.insert("processedStripeEvents", { stripeEventId: args.stripeEventId, eventType: args.eventType, processedAt: Date.now() })`. Return something non-`alreadyProcessed` on the success path (e.g. `{ credited: args.amount }`).

    Extend convex/aiCredits.test.ts with `describe("aiCredits.addCredits")` covering the four behaviors. Seed via `t.run`. Call `t.mutation(internal.aiCredits.addCredits, { clerkUserId: "user_x", amount: 50, stripeEventId: "evt_topup_1", eventType: "checkout.session.completed", stripePaymentIntentId: "pi_x" })`. For idempotency, call twice with the same stripeEventId and assert the second returns `{ alreadyProcessed: true }` and the balance/transaction-count is unchanged (query via `t.run`).

  </action>
  <verify>
    <automated>npx vitest run convex/aiCredits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - addCredits args include `stripeEventId`, `eventType`, and `stripePaymentIntentId: v.optional(v.string())`
    - addCredits performs the `processedStripeEvents` by_stripeEventId idempotency check before any balance write and returns `{ alreadyProcessed: true }` on replay
    - addCredits has both a patch branch and an insert branch for the aiCredits row
    - The topup transaction row uses `type: "topup"` and `amount: args.amount`
    - `npx vitest run convex/aiCredits.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>addCredits idempotently grants credits (patch-or-insert +amount), records a topup transaction with the payment intent, and never double-credits on Stripe replay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch checkout.session.completed on session.mode → addCredits</name>
  <files>convex/stripeWebhooks.ts, convex/stripeWebhooks.test.ts</files>
  <read_first>
    - convex/stripeWebhooks.ts lines 41-129 (checkout.session.completed + invoice.paid cases)
    - convex/stripeWebhooks.test.ts (seedCheckoutEvent helper, existing checkout tests, auth-guard shape)
    - .planning/phases/05-ai-credits-system/05-CONTEXT.md (D-14, D-15)
    - .planning/phases/05-ai-credits-system/05-RESEARCH.md (Pattern 3, Pitfall 4 — exact-match on "payment", anti-pattern: never derive amount from Stripe)
  </read_first>
  <behavior>
    - processWebhookEvent with a checkout.session.completed event where data.object.mode === "payment" → calls internal.aiCredits.addCredits with amount 50 and the session's metadata.clerkUserId; balance increases by 50.
    - The same payment-mode event replayed → addCredits idempotency yields no double-credit (assert via balance).
    - processWebhookEvent with the existing seedCheckoutEvent fixture (mode undefined) → still routes to upsertSubscription; existing subscription tests keep passing unchanged.
    - A checkout.session.completed event with mode === "subscription" explicitly → routes to upsertSubscription (not addCredits).
  </behavior>
  <action>
    In convex/stripeWebhooks.ts, inside the `case "checkout.session.completed":` block, at the TOP of the case (right after `const session = args.event.data.object;` and `const clerkUserId = session.metadata?.clerkUserId;`), add an exact-match branch: `if (session.mode === "payment") { return await ctx.runMutation(internal.aiCredits.addCredits, { stripeEventId: args.event.id, eventType: args.event.type, clerkUserId, amount: 50, stripePaymentIntentId: typeof session.payment_intent === "string" ? session.payment_intent : session.payment_intent?.id }); }`. Hardcode `amount: 50` (anti-pattern warning: never derive from `session.amount_total`/line items — locked value per PROJECT.md). Use exact `=== "payment"` (Pitfall 4) so mode `undefined`/`"subscription"` both fall through to the existing subscription-upsert logic, which stays byte-for-byte unchanged below the new branch.

    Extend convex/stripeWebhooks.test.ts: add a `seedTopupCheckoutEvent(id, clerkUserId, paymentIntentId)` helper (or add a `mode`/`payment_intent` param to the existing helper) that sets `data.object.mode = "payment"`, `data.object.metadata.clerkUserId`, `data.object.payment_intent`, and OMITS `subscription`/`subscriptionSnapshot`. Add tests: (a) fire the payment-mode event via `t.action(api.stripeWebhooks.processWebhookEvent, { secret: process.env.INTERNAL_WEBHOOK_SECRET, event: seedTopupCheckoutEvent(...) })`, seed a pre-existing subscription for the same clerkUserId if needed, assert the aiCredits balance is +50 (query via `t.run` / `api.aiCredits.getCredits`); (b) replay the same event and assert balance unchanged (idempotent); (c) confirm an existing subscription-mode test (mode-absent fixture) still passes — do not modify those tests. Name at least one test with "payment" and one with "idempotent" in its title (per the VALIDATION test map `-t` filters).

  </action>
  <verify>
    <automated>npx vitest run convex/stripeWebhooks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - convex/stripeWebhooks.ts checkout.session.completed case contains `session.mode === "payment"` as an exact-match branch calling `internal.aiCredits.addCredits`
    - The addCredits call passes `amount: 50` as a literal (not derived from `session.amount_total` or line items)
    - The existing subscription-upsert logic in the same case is unchanged and still reached when mode is undefined/"subscription"
    - stripeWebhooks.test.ts has a payment-mode test and an idempotency test; the pre-existing subscription tests still pass
    - `npx vitest run convex/stripeWebhooks.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>The single-writer dispatcher routes payment-mode checkout completions to addCredits (+50, idempotent) while every subscription-mode/absent-mode event still upserts the subscription unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                                      |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Stripe → webhook dispatcher → addCredits | Stripe delivers events at-least-once; a replayed/duplicated event must not double-credit.                                                        |
| webhook payload → clerkUserId resolution | The credited user is resolved from session.metadata.clerkUserId set server-side at checkout creation (Plan 03), not from arbitrary client input. |

## STRIDE Threat Register

| Threat ID | Category             | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                                                           |
| --------- | -------------------- | ----------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-03   | Tampering            | addCredits double-credit on Stripe replay | mitigate    | processedStripeEvents idempotency check keyed by stripeEventId, checked before any balance write inside addCredits (Task 1). Proven by the idempotency tests.                                                             |
| T-05-06   | Tampering            | credit amount from Stripe response        | mitigate    | amount hardcoded to 50 in the dispatcher branch (Task 2), never derived from session.amount_total/line items — a Dashboard price change cannot silently alter credits granted.                                            |
| T-05-05   | Spoofing / Tampering | forged/unsigned webhook payload           | accept      | Unchanged this phase — signature verification (stripe.webhooks.constructEvent) in app/api/webhooks/stripe/route.ts already gates every event; the INTERNAL_WEBHOOK_SECRET guard in processWebhookEvent is also unchanged. |
| T-05-SC   | Tampering            | npm/pip/cargo installs                    | accept      | No new packages installed this phase (RESEARCH.md audit N/A).                                                                                                                                                             |

</threat_model>

<verification>
- `npx vitest run convex/aiCredits.test.ts convex/stripeWebhooks.test.ts` — addCredits + payment-mode + idempotency + subscription-regression all pass.
- `npm run test:unit` — full suite green.
- Grep: `session.mode === "payment"` and `internal.aiCredits.addCredits` present in convex/stripeWebhooks.ts; `addCredits` args include `stripeEventId` in convex/aiCredits.ts.
</verification>

<success_criteria>

- ROADMAP SC4 (top-up crediting): payment-mode webhook grants +50 and records the transaction — proven by test.
- PAY-05 server half: single-writer dispatcher extended, not duplicated (D-14).
- Idempotency: duplicate delivery does not double-credit — proven by test.
- Regression: subscription-mode events unaffected — existing tests pass.
  </success_criteria>

<output>
Create `.planning/phases/05-ai-credits-system/05-02-SUMMARY.md` when done, recording the final addCredits arg signature and the hardcoded top-up amount (50) so Plan 05 verification can grep for the exact contract.
</output>
