---
phase: 05-ai-credits-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - messages/fr.json
  - app/[locale]/(app)/notes/actions.ts
  - components/CreditsExhaustedDialog.tsx
autonomous: true
requirements: [CRED-03, PAY-05]
must_haves:
  truths:
    - "A Pro user (subscription status active) invoking the top-up Server Action is redirected to a Stripe Checkout Session in payment mode for STRIPE_TOPUP_PRICE_ID"
    - "A non-Pro caller of the top-up Server Action is rejected server-side (throws) before any Stripe call — the UI-only gate is not trusted"
    - "CreditsExhaustedDialog shows the Free 'Upgrade to Pro' CTA (→ /pricing) when subscription is not active, and the Pro 'Buy 50 credits' CTA (→ top-up action) when active"
    - "New AiCredits i18n namespace and the Editor.tooltips AI-action key exist in both en.json and fr.json"
  artifacts:
    - path: "app/[locale]/(app)/notes/actions.ts"
      provides: "createTopupCheckoutSession Server Action (payment mode, Pro-only gate)"
      contains: 'mode: "payment"'
    - path: "components/CreditsExhaustedDialog.tsx"
      provides: "zero-credit blocking dialog with branching CTA"
      contains: "getSubscription"
    - path: "messages/en.json"
      provides: "AiCredits namespace + Editor.tooltips AI key"
      contains: "AiCredits"
  key_links:
    - from: "app/[locale]/(app)/notes/actions.ts"
      to: "Stripe Checkout (payment mode)"
      via: "stripe.checkout.sessions.create with STRIPE_TOPUP_PRICE_ID"
      pattern: "STRIPE_TOPUP_PRICE_ID"
    - from: "components/CreditsExhaustedDialog.tsx Pro branch"
      to: "createTopupCheckoutSession"
      via: "form action / onClick invoking the Server Action"
      pattern: "createTopupCheckoutSession"
    - from: "components/CreditsExhaustedDialog.tsx"
      to: "api.subscriptions.getSubscription"
      via: "reactive useQuery to branch the CTA"
      pattern: "getSubscription"
---

<objective>
Create the top-up purchase flow's non-toolbar pieces: the `createTopupCheckoutSession` Server Action (Stripe Checkout in `payment` mode, Pro-only server-side gate), the `CreditsExhaustedDialog` blocking dialog with a CTA that branches on subscription status (Free → Upgrade to Pro; Pro-at-zero → Buy 50 credits), and the `AiCredits` i18n namespace copy.

Purpose: Delivers CRED-03 (trigger a top-up when credits are low) and the client half of PAY-05. The dialog and action are self-contained and consumed by the toolbar in Plan 04. The server-side Pro gate is the defense-in-depth mitigation for a Free user calling the action directly (D-10, T-05-04).

Output: A new Server Action file, the `CreditsExhaustedDialog` component, and `AiCredits` + `Editor.tooltips` copy in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-credits-system/05-CONTEXT.md
@.planning/phases/05-ai-credits-system/05-UI-SPEC.md
@.planning/phases/05-ai-credits-system/05-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly, no exploration. -->

Server Action template — app/[locale]/(marketing)/pricing/actions.ts createCheckoutSession (near 1:1 template):

- `"use server"`, imports auth from "@clerk/nextjs/server", redirect from "next/navigation", Stripe, ConvexHttpClient from "convex/browser", api from "@/convex/\_generated/api".
- `const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);` at module scope.
- `const { userId, redirectToSignIn, getToken } = await auth();` → if no userId, redirectToSignIn and return.
- Authenticate the Convex call: `const convexToken = await getToken({ template: "convex" }); if (convexToken) convex.setAuth(convexToken); existing = await convex.query(api.subscriptions.getSubscription, {});` inside a try/catch that logs and re-throws a distinguishable Error (never log the token — T-03-08).
- Narrow try/catch around ONLY `stripe.checkout.sessions.create(...)`; `redirect(session.url)` is OUTSIDE any try (Pitfall 4: redirect throws internally).
- `baseUrl = process.env.APP_URL!`.

getSubscription (convex/subscriptions.ts) returns the row or null; `.status` is `"active" | "canceled" | "past_due"`; `.stripeCustomerId` is present when a row exists.

Dialog analog — components/UpgradeModal.tsx (full file): Dialog + DialogContent + DialogHeader(DialogTitle, DialogDescription) + DialogFooter(DialogClose→secondary Button "Maybe later", primary Button asChild → Link href="/pricing"). Imports Dialog primitives from "@/components/ui/dialog", Button from "@/components/ui/button", Link from "@/i18n/routing", useTranslations from "next-intl".

Reactive Convex read in a client component — pattern used app-wide (app/[locale]/(app)/notes/page.tsx): `import { convexQuery } from "@convex-dev/react-query"; import { useQuery } from "@tanstack/react-query"; const { data } = useQuery(convexQuery(api.subscriptions.getSubscription, {}));`.

Env var STRIPE_TOPUP_PRICE_ID is already present in .env.example (Phase 1) and all environments.

i18n: messages/en.json has "PlanEnforcement" (line 150) and "Editor.tooltips" (line 158, flat keys like "link"/"image"). messages/fr.json mirrors the same structure (PlanEnforcement line 150). next-intl flat-namespace convention. Existing "PlanEnforcement" en copy: title "You've reached the Free plan limit", upgradeCta "Upgrade to Pro", dismiss "Maybe later".

Copywriting Contract (05-UI-SPEC.md — exact en values, translate for fr):

- Toolbar tooltip (Editor.tooltips, new key e.g. "aiAction"): "AI action (1 credit)"
- Badge tooltip (AiCredits, ICU plural): "{n} AI credits remaining" (handle 0/1/other)
- Dialog title (both branches): "You're out of AI credits"
- Dialog body Free: "Free plans don't include AI credits. Upgrade to Pro for 100 AI credits a month, with the option to top up anytime."
- Dialog body Pro-at-0: "You've used all your AI credits for this cycle. Buy 50 more credits for 2€, or wait for your next billing cycle to reset."
- CTA Free: "Upgrade to Pro" (→ /pricing)
- CTA Pro: "Buy 50 credits" (→ top-up action)
- Dismiss (both): "Maybe later"
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add AiCredits namespace + AI-action tooltip key (en + fr)</name>
  <files>messages/en.json, messages/fr.json</files>
  <read_first>
    - messages/en.json lines 150-179 (PlanEnforcement + Editor.tooltips)
    - messages/fr.json lines 150-179 (PlanEnforcement + Editor.tooltips)
    - .planning/phases/05-ai-credits-system/05-UI-SPEC.md (Copywriting Contract table)
  </read_first>
  <action>
    In messages/en.json, add a new top-level `"AiCredits"` namespace (flat keys) with: `dialogTitle` = "You're out of AI credits", `dialogBodyFree` = "Free plans don't include AI credits. Upgrade to Pro for 100 AI credits a month, with the option to top up anytime.", `dialogBodyPro` = "You've used all your AI credits for this cycle. Buy 50 more credits for 2€, or wait for your next billing cycle to reset.", `upgradeCta` = "Upgrade to Pro", `topupCta` = "Buy 50 credits", `dismiss` = "Maybe later", and `badgeTooltip` using next-intl ICU plural: `"{count, plural, =0 {0 AI credits remaining} one {# AI credit remaining} other {# AI credits remaining}}"`. Also add a `"aiAction"` key to the existing `Editor.tooltips` object with value "AI action (1 credit)". Mirror the exact same keys in messages/fr.json with French translations consistent with the existing PlanEnforcement fr copy (e.g. upgradeCta "Passer à Pro", dismiss "Plus tard", topupCta "Acheter 50 crédits", badgeTooltip ICU plural in French, aiAction "Action IA (1 crédit)"). Keep valid JSON (commas, no trailing comma).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); JSON.parse(require('fs').readFileSync('messages/fr.json','utf8')); console.log('valid')"</automated>
  </verify>
  <acceptance_criteria>
    - messages/en.json and messages/fr.json each contain a top-level "AiCredits" object with keys dialogTitle, dialogBodyFree, dialogBodyPro, upgradeCta, topupCta, dismiss, badgeTooltip
    - Both files' Editor.tooltips objects contain an "aiAction" key
    - badgeTooltip value is an ICU plural string handling =0, one, other with `{count}`
    - Both files parse as valid JSON (verify command prints "valid")
  </acceptance_criteria>
  <done>AiCredits copy and the AI-action tooltip exist in both locales as valid JSON, matching the UI-SPEC Copywriting Contract.</done>
</task>

<task type="auto">
  <name>Task 2: createTopupCheckoutSession Server Action (payment mode, Pro-only gate)</name>
  <files>app/[locale]/(app)/notes/actions.ts</files>
  <read_first>
    - app/[locale]/(marketing)/pricing/actions.ts (full file — the 1:1 structural template)
    - .planning/phases/05-ai-credits-system/05-CONTEXT.md (D-10, D-12, D-13, D-15)
    - .planning/phases/05-ai-credits-system/05-PATTERNS.md ("New Server Action" section — deltas list)
    - .planning/phases/05-ai-credits-system/05-RESEARCH.md (Code Examples — top-up Server Action)
  </read_first>
  <action>
    Create app/[locale]/(app)/notes/actions.ts as a new `"use server"` module exporting `async function createTopupCheckoutSession(): Promise<void>`. Follow createCheckoutSession's structure with these deltas: (1) `const { userId, redirectToSignIn, getToken } = await auth();` — if no userId, `redirectToSignIn()` and return. (2) Build a ConvexHttpClient, set auth with `getToken({ template: "convex" })`, query `api.subscriptions.getSubscription, {}` inside a try/catch that logs (never the token) and re-throws `new Error("subscriptionLookupFailed")`. (3) D-10 DEFENSE-IN-DEPTH: `if (existing?.status !== "active") throw new Error("TOPUP_REQUIRES_PRO");` — this is the inverse of the subscription flow's short-circuit and MUST run server-side before any Stripe call (T-05-04). (4) `const baseUrl = process.env.APP_URL!;` then `stripe.checkout.sessions.create({ mode: "payment", ...(existing?.stripeCustomerId ? { customer: existing.stripeCustomerId } : {}), line_items: [{ price: process.env.STRIPE_TOPUP_PRICE_ID!, quantity: 1 }], metadata: { clerkUserId: userId }, success_url: `${baseUrl}/notes`, cancel_url: `${baseUrl}/notes` })` wrapped in a narrow try/catch that re-throws `new Error("checkoutSessionCreationFailed")`. NO `subscription_data` key (payment mode has none — D-15 uses only `session.metadata.clerkUserId`). NO `/checkout/success` (D-13 — redirect back to /notes; the badge updates reactively once the webhook credits). (5) `if (session.url) redirect(session.url);` OUTSIDE any try block (Pitfall 4). Reuse the module-scope `const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);`. clerkUserId is sourced ONLY from `auth()` (T-05-01), never a function argument.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - app/[locale]/(app)/notes/actions.ts begins with `"use server"` and exports `createTopupCheckoutSession`
    - Contains `mode: "payment"` and `process.env.STRIPE_TOPUP_PRICE_ID`
    - Contains a server-side `existing?.status !== "active"` (or equivalent) gate that throws before `stripe.checkout.sessions.create`
    - Does NOT contain `subscription_data` and does NOT reference `/checkout/success`
    - `redirect(session.url)` is not inside a try/catch block
    - clerkUserId in metadata comes from `auth()` userId, not a parameter
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>The top-up Server Action creates a payment-mode Stripe Checkout Session for the top-up price, refusing non-Pro callers server-side, and redirects back into the app.</done>
</task>

<task type="auto">
  <name>Task 3: CreditsExhaustedDialog with branching CTA</name>
  <files>components/CreditsExhaustedDialog.tsx</files>
  <read_first>
    - components/UpgradeModal.tsx (full file — clone this structure, D-08)
    - app/[locale]/(app)/notes/actions.ts (createTopupCheckoutSession signature — created in Task 2)
    - .planning/phases/05-ai-credits-system/05-CONTEXT.md (D-08, D-10, D-11)
    - .planning/phases/05-ai-credits-system/05-UI-SPEC.md (Color — primary CTA stays default Button variant both branches; Copywriting Contract)
    - .planning/phases/05-ai-credits-system/05-PATTERNS.md ("CreditsExhaustedDialog" section)
  </read_first>
  <action>
    Create components/CreditsExhaustedDialog.tsx as a `"use client"` component cloning UpgradeModal.tsx's Dialog structure. Props: `{ open: boolean; onOpenChange: (open: boolean) => void }`. Use `const t = useTranslations("AiCredits");`. Determine Pro status reactively: `const { data: subscription } = useQuery(convexQuery(api.subscriptions.getSubscription, {}));` (import convexQuery from "@convex-dev/react-query", useQuery from "@tanstack/react-query", api from "@/convex/_generated/api"). `const isPro = subscription?.status === "active";` (D-11, same active-only check as Phase 4). Render `DialogTitle` = `t("dialogTitle")` (both branches). `DialogDescription` = `isPro ? t("dialogBodyPro") : t("dialogBodyFree")`. Footer: a `DialogClose asChild` secondary Button with `t("dismiss")`, plus the branching primary CTA (default Button variant in BOTH branches per UI-SPEC Color): when `!isPro`, render `<Button asChild><Link href="/pricing">{t("upgradeCta")}</Link></Button>` (Link from "@/i18n/routing", identical to UpgradeModal). When `isPro`, render a top-up trigger that invokes `createTopupCheckoutSession` — use a `<form action={createTopupCheckoutSession}>` wrapping `<Button type="submit">{t("topupCta")}</Button>` (server action form submission; import the action from "@/app/[locale]/(app)/notes/actions" via the appropriate alias). Do NOT add a font-size className to any text (UI-SPEC Typography — use component defaults). Do NOT tint anything with --destructive (zero balance is a neutral blocked state, not an error).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - components/CreditsExhaustedDialog.tsx is a "use client" component with props `{ open, onOpenChange }`
    - Uses `useTranslations("AiCredits")` and branches the description on `subscription?.status === "active"`
    - Free branch renders a Link to "/pricing" with `t("upgradeCta")`; Pro branch invokes `createTopupCheckoutSession` with `t("topupCta")`
    - Both primary CTAs use the default Button variant; the dismiss button uses the secondary variant
    - No `--destructive` styling and no font-size className overrides on dialog text
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>The blocking dialog renders the correct copy and CTA for Free vs Pro-at-zero users, wired to /pricing or the top-up Server Action respectively.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                                                            |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| client → createTopupCheckoutSession Server Action | A Free user could call the action directly (bypassing the UI that only shows the button to Pro users). |
| Server Action → Stripe                            | clerkUserId written into session.metadata must come from the authenticated session, not client input.  |

## STRIDE Threat Register

| Threat ID | Category                      | Component                                | Disposition | Mitigation Plan                                                                                                                                                                                               |
| --------- | ----------------------------- | ---------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-04   | Elevation of Privilege        | createTopupCheckoutSession Pro-only gate | mitigate    | The Server Action independently queries getSubscription and throws `TOPUP_REQUIRES_PRO` when status !== "active" BEFORE any Stripe call (Task 2), never trusting that the UI only shows the CTA to Pro users. |
| T-05-01   | Elevation of Privilege / IDOR | clerkUserId in checkout metadata         | mitigate    | clerkUserId sourced only from `auth()` userId server-side (Task 2), never a Server Action parameter (D-15).                                                                                                   |
| T-05-07   | Information Disclosure        | error/token logging in the Server Action | mitigate    | The subscription-lookup catch logs the error but never the Convex token (mirrors pricing/actions.ts T-03-08).                                                                                                 |
| T-05-SC   | Tampering                     | npm/pip/cargo installs                   | accept      | No new packages installed this phase.                                                                                                                                                                         |

</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` — Server Action + dialog type-check.
- `node -e "JSON.parse(...)"` — both message files are valid JSON.
- Grep: `mode: "payment"`, `TOPUP_REQUIRES_PRO` (or `status !== "active"` gate) in actions.ts; `getSubscription` + `createTopupCheckoutSession` in CreditsExhaustedDialog.tsx; `AiCredits` in both message files.
</verification>

<success_criteria>

- CRED-03: a Pro user at zero balance can trigger a top-up purchase from the dialog.
- PAY-05 client half: payment-mode Checkout Session created via Server Action (D-12).
- D-10/D-11: Free users see Upgrade-to-Pro; Pro-at-zero see Buy-50-credits; the Pro gate holds server-side even if the UI is bypassed.
- UI-SPEC copy + color/typography constraints honored.
  </success_criteria>

<output>
Create `.planning/phases/05-ai-credits-system/05-03-SUMMARY.md` when done, recording the Server Action export name/path (`createTopupCheckoutSession` in app/[locale]/(app)/notes/actions.ts), the dialog component name/props, and the AiCredits i18n key names so Plan 04 wires them exactly.
</output>
