---
phase: 05-ai-credits-system
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-03]
files_modified:
  - components/editor/EditorToolbar.tsx
autonomous: true
requirements: [CRED-02, CRED-04]
must_haves:
  truths:
    - "The editor toolbar shows a Sparkles AI-action button and a credits balance badge next to it, visible for both Free and Pro users (never hidden)"
    - "The balance badge reactively shows the authenticated user's remaining credits (0 when there is no aiCredits row) via getMyCredits"
    - "Clicking the AI button calls runAiAction; on success the badge decrements reactively; on INSUFFICIENT_CREDITS the CreditsExhaustedDialog opens"
  artifacts:
    - path: "components/editor/EditorToolbar.tsx"
      provides: "AiActionButton + CreditsBalanceBadge + runAiAction wiring + dialog mount"
      contains: "runAiAction"
  key_links:
    - from: "components/editor/EditorToolbar.tsx AiActionButton"
      to: "api.aiCredits.runAiAction"
      via: "useConvexMutation + useMutation onClick"
      pattern: "runAiAction"
    - from: "components/editor/EditorToolbar.tsx onError"
      to: "CreditsExhaustedDialog"
      via: "ConvexError.data === 'INSUFFICIENT_CREDITS' opens the dialog"
      pattern: "INSUFFICIENT_CREDITS"
    - from: "components/editor/EditorToolbar.tsx CreditsBalanceBadge"
      to: "api.aiCredits.getMyCredits"
      via: "reactive useQuery"
      pattern: "getMyCredits"
---

<objective>
Add the AI-action toolbar button (`Sparkles` icon), the reactive credits balance badge, and the wiring that calls `runAiAction` on click — decrementing the badge on success and opening `CreditsExhaustedDialog` on `INSUFFICIENT_CREDITS` — into the editor toolbar.

Purpose: This is the user-facing entry point that satisfies CRED-02 (balance visible near the AI feature, ROADMAP SC2) and the client trigger for CRED-04. It consumes the Plan 01 contract (`runAiAction`, `getMyCredits`, `INSUFFICIENT_CREDITS`) and the Plan 03 dialog + `AiCredits` copy. The button is visible for both tiers and never hidden (D-09).

Output: An updated `EditorToolbar.tsx` with a new toolbar group (AI button + badge), the mutation wiring, and a mounted `CreditsExhaustedDialog`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-credits-system/05-CONTEXT.md
@.planning/phases/05-ai-credits-system/05-UI-SPEC.md
@.planning/phases/05-ai-credits-system/05-PATTERNS.md
@.planning/phases/05-ai-credits-system/05-01-SUMMARY.md
@.planning/phases/05-ai-credits-system/05-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly, no exploration. -->

EditorToolbar.tsx (this plan edits it) — key existing structure:

- `"use client"`; `const t = useTranslations("Editor");`; `const { editor } = useEditorContext();`; guard `if (!editor || !editor.isEditable) return null;`.
- Existing local `useState` dialogs (linkDialogOpen etc.) — mirror this for the credits dialog open state.
- Toolbar rows are inside `<TooltipProvider delayDuration={1000}><div className="flex flex-wrap gap-1">...groups separated by `<Separator orientation="vertical" className="h-auto!" />`...</div></TooltipProvider>`.
- The Link/Image buttons (lines 142-153) use the local `ToolbarButton` component (defined lines 247-277): props `{ onClick, active?, disabled?, tooltip, icon }`. Reuse it for the AI button.
- Tooltip/TooltipTrigger/TooltipContent already imported (lines 6-11). `Button` imported from "../ui/button". `Separator` imported. Lucide icons imported from "lucide-react" (line 4) — ADD `Sparkles`.
- `Badge` is NOT yet imported — add `import { Badge } from "../ui/badge";`.

runAiAction contract (Plan 01, convex/aiCredits.ts): `mutation({ args: {} })` — call with `{}`. On insufficient balance it throws `ConvexError` with `err.data === "INSUFFICIENT_CREDITS"`.

getMyCredits contract (Plan 01): `query({ args: {} })` returns the aiCredits row (`{ balance, ... }`) or `null`. Treat `null` as balance 0 (D-09).

Client mutation + error-handling pattern to mirror — hooks/useNoteMutations.ts createNote (lines 55-91):

- `const mutate = useConvexMutation(api.aiCredits.runAiAction);` (from "@convex-dev/react-query")
- `const run = useMutation({ mutationFn: mutate, onError: (err) => { if (err instanceof ConvexError && err.data === "INSUFFICIENT_CREDITS") setCreditsDialogOpen(true); } });` (useMutation from "@tanstack/react-query", ConvexError from "convex/values")
- trigger with `run.mutate({})`.

Reactive read pattern — `import { convexQuery } from "@convex-dev/react-query"; import { useQuery } from "@tanstack/react-query"; const { data: credits } = useQuery(convexQuery(api.aiCredits.getMyCredits, {})); const balance = credits?.balance ?? 0;`.

CreditsExhaustedDialog (Plan 03, components/CreditsExhaustedDialog.tsx): props `{ open, onOpenChange }`; queries subscription itself and branches the CTA — the toolbar only controls open state.

i18n keys (Plan 03): `Editor.tooltips.aiAction` = "AI action (1 credit)"; `AiCredits.badgeTooltip` ICU plural on `{count}`.

UI-SPEC constraints: Sparkles icon (h-4 w-4); new toolbar group AFTER the Link/Image group, preceded by a `<Separator orientation="vertical" className="h-auto!" />`; badge is `Badge variant="secondary"` wrapped in Tooltip, placed immediately right of the AI button with the existing `gap-1` convention; badge is ALWAYS neutral (no red-at-zero); no font-size override on the badge; the AI button uses the neutral ghost/secondary ToolbarButton variant (never primary-colored).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AiActionButton + CreditsBalanceBadge toolbar group</name>
  <files>components/editor/EditorToolbar.tsx</files>
  <read_first>
    - components/editor/EditorToolbar.tsx (full file — imports, toolbar JSX, ToolbarButton definition)
    - app/[locale]/(app)/notes/page.tsx lines 29-30, 453-456 (convexQuery + useQuery reactive-read pattern)
    - .planning/phases/05-ai-credits-system/05-UI-SPEC.md (Design System new components, Spacing, Color, Typography)
  </read_first>
  <action>
    In components/editor/EditorToolbar.tsx: add `Sparkles` to the lucide-react import (line 4) and add `import { Badge } from "../ui/badge";`. Add `import { convexQuery } from "@convex-dev/react-query";`, `import { useQuery } from "@tanstack/react-query";`, and `import { api } from "@/convex/_generated/api";`. Add a second translations hook `const tCredits = useTranslations("AiCredits");` alongside the existing `const t = useTranslations("Editor");`.

    Inside the component (before the early-return guard is fine — hooks must run unconditionally), read the balance: `const { data: credits } = useQuery(convexQuery(api.aiCredits.getMyCredits, {}));` and `const balance = credits?.balance ?? 0;` (D-09: null → 0).

    In the JSX, AFTER the existing Link/Image `ToolbarButton`s and BEFORE the Link Dialog, add a `<Separator orientation="vertical" className="h-auto!" />` followed by a new group containing: (1) a `ToolbarButton` with `icon={<Sparkles className="h-4 w-4" />}`, `tooltip={t("tooltips.aiAction")}`, and an `onClick` placeholder handler (wired in Task 2 — for now `onClick={() => {}}`). (2) Immediately to its right, the balance badge wrapped in a Tooltip: `<Tooltip><TooltipTrigger asChild><Badge variant="secondary">{balance}</Badge></TooltipTrigger><TooltipContent side="bottom">{tCredits("badgeTooltip", { count: balance })}</TooltipContent></Tooltip>`. Keep the button and badge in the existing `gap-1` flow (do not add custom spacing classes). Do NOT color-code the badge by balance level (no red-at-zero — always `variant="secondary"`). Do NOT add a font-size className to the Badge.

  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - EditorToolbar.tsx imports `Sparkles` from lucide-react and `Badge` from "../ui/badge"
    - Contains `useQuery(convexQuery(api.aiCredits.getMyCredits, {}))` and `const balance = credits?.balance ?? 0`
    - A new toolbar group renders a Sparkles ToolbarButton and a `Badge variant="secondary"` showing `balance`, wrapped in a Tooltip, after the Link/Image group and preceded by a Separator
    - Badge tooltip uses `tCredits("badgeTooltip", { count: balance })`; button tooltip uses `t("tooltips.aiAction")`
    - Badge has no balance-conditional color and no font-size className
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>The toolbar shows a Sparkles AI button and a neutral reactive balance badge next to it, visible for all users.</done>
</task>

<task type="auto">
  <name>Task 2: Wire runAiAction + mount CreditsExhaustedDialog</name>
  <files>components/editor/EditorToolbar.tsx</files>
  <read_first>
    - components/editor/EditorToolbar.tsx (post-Task-1 state)
    - hooks/useNoteMutations.ts lines 1-14, 55-91 (useConvexMutation + useMutation + ConvexError onError pattern)
    - components/CreditsExhaustedDialog.tsx (props contract from Plan 03)
    - .planning/phases/05-ai-credits-system/05-CONTEXT.md (D-04, D-09, D-11)
  </read_first>
  <action>
    In components/editor/EditorToolbar.tsx: add imports `import { useConvexMutation } from "@convex-dev/react-query";`, `import { useMutation } from "@tanstack/react-query";`, `import { ConvexError } from "convex/values";`, and `import CreditsExhaustedDialog from "@/components/CreditsExhaustedDialog";`. Add local state `const [creditsDialogOpen, setCreditsDialogOpen] = useState(false);` (mirror the existing linkDialogOpen state).

    Create the mutation: `const runAiActionMutate = useConvexMutation(api.aiCredits.runAiAction);` and `const runAiAction = useMutation({ mutationFn: runAiActionMutate, onError: (err) => { if (err instanceof ConvexError && err.data === "INSUFFICIENT_CREDITS") setCreditsDialogOpen(true); } });`. Define `const handleAiAction = () => runAiAction.mutate({});` and set the Sparkles `ToolbarButton`'s `onClick={handleAiAction}` (replacing the Task 1 placeholder). The badge updates reactively via its own getMyCredits useQuery once the mutation commits — no manual refetch (D-13-adjacent). Do NOT hide or disable the button at zero balance (D-09) — the click still fires the mutation, which throws INSUFFICIENT_CREDITS and opens the dialog.

    Mount the dialog once, inside the toolbar's returned JSX (alongside the existing Link/Image `<Dialog>`s): `<CreditsExhaustedDialog open={creditsDialogOpen} onOpenChange={setCreditsDialogOpen} />`. The dialog resolves Free-vs-Pro branching itself (Plan 03) — the toolbar only supplies open state.

  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - EditorToolbar.tsx imports `useConvexMutation`, `useMutation`, `ConvexError`, and `CreditsExhaustedDialog`
    - Contains `useConvexMutation(api.aiCredits.runAiAction)` wrapped in `useMutation` with an `onError` checking `err instanceof ConvexError && err.data === "INSUFFICIENT_CREDITS"` → `setCreditsDialogOpen(true)`
    - The Sparkles ToolbarButton `onClick` triggers `runAiAction.mutate({})`
    - The Sparkles button is neither hidden nor disabled based on balance
    - `<CreditsExhaustedDialog open={creditsDialogOpen} onOpenChange={setCreditsDialogOpen} />` is rendered
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Clicking the AI button deducts a credit (badge decrements reactively) or, at zero balance, opens the branching CreditsExhaustedDialog.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                                                            |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| toolbar client → api.aiCredits.runAiAction | The client triggers the deduction; authority and balance checks are enforced server-side (Plan 01), not by the button's enabled state. |

## STRIDE Threat Register

| Threat ID | Category  | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                                                                                   |
| --------- | --------- | ------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-02   | Tampering | client-side balance display / button gating | mitigate    | The client never decides whether the action is allowed — it always calls runAiAction, which enforces the balance check server-side and throws INSUFFICIENT_CREDITS (Plan 01). The badge is display-only; a tampered badge cannot grant an action. |
| T-05-01   | IDOR      | getMyCredits / runAiAction calls            | mitigate    | Both are called with `{}` (no clerkUserId); identity is derived server-side (Plan 01). The toolbar cannot request another user's balance or deduct from another user.                                                                             |
| T-05-SC   | Tampering | npm/pip/cargo installs                      | accept      | No new packages installed this phase.                                                                                                                                                                                                             |

</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` — toolbar type-checks against the Plan 01/03 contracts.
- `npm run test:unit` — no regressions (UI wiring has no unit tests; server logic covered by Plans 01/02). UI behavior is verified manually in Plan 05.
- Grep: `runAiAction`, `getMyCredits`, `INSUFFICIENT_CREDITS`, `CreditsExhaustedDialog`, `Sparkles` present in EditorToolbar.tsx.
</verification>

<success_criteria>

- CRED-02 (SC2): balance visible next to the AI entry point in the editor toolbar (not only in Settings).
- CRED-04 client trigger: clicking the button invokes the server-side deduction.
- ROADMAP SC3: zero-balance click opens the blocking dialog instead of performing an action.
- D-09: button visible and active for both Free and Pro users.
  </success_criteria>

<output>
Create `.planning/phases/05-ai-credits-system/05-04-SUMMARY.md` when done, noting the toolbar group placement and confirming the button/badge are always visible so Plan 05's human verification steps match.
</output>
