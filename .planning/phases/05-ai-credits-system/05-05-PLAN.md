---
phase: 05-ai-credits-system
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified: []
autonomous: false
requirements: [CRED-02, CRED-03, CRED-04, PAY-05]
must_haves:
  truths:
    - "clerkUserId is never accepted as a client argument in runAiAction, getMyCredits, or createTopupCheckoutSession (IDOR-safe)"
    - "createTopupCheckoutSession refuses non-Pro callers server-side before any Stripe call"
    - "The full unit suite is green (deduction, concurrency, addCredits, webhook payment/idempotency/regression)"
    - "A human confirms the end-to-end UX: badge visible both tiers, zero-credit dialog branches by plan, Pro action decrements, top-up purchase credits +50"
  artifacts: []
  key_links:
    - from: "verification"
      to: "ROADMAP Phase 5 SC1-SC5"
      via: "automated grep/suite + human UAT sign-off"
      pattern: "INSUFFICIENT_CREDITS"
---

<objective>
Verify the phase against its security threat model (automated grep + full unit suite) and against ROADMAP SC1-SC5 via human UAT of the toolbar, blocking dialog, and end-to-end Stripe top-up.

Purpose: Phase gate. The unit tests already prove the server-side logic (Plans 01/02); this plan proves (a) the IDOR/Pro-gate mitigations are actually in the source (T-05-01, T-05-04) and (b) the UI flows a human cannot unit-test behave correctly. No production code is modified here.

Output: A passing automated verification and a human sign-off recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-credits-system/05-VALIDATION.md
@.planning/phases/05-ai-credits-system/05-01-SUMMARY.md
@.planning/phases/05-ai-credits-system/05-02-SUMMARY.md
@.planning/phases/05-ai-credits-system/05-03-SUMMARY.md
@.planning/phases/05-ai-credits-system/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated security + full-suite verification</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - convex/aiCredits.ts (runAiAction / getMyCredits arg signatures)
    - app/[locale]/(app)/notes/actions.ts (createTopupCheckoutSession Pro gate)
    - .planning/phases/05-ai-credits-system/05-VALIDATION.md (Per-Task Verification Map rows 05-04-01, 05-04-02)
  </read_first>
  <action>
    Run the automated verification gate (no code changes). (1) T-05-01 IDOR check: confirm `runAiAction` and `getMyCredits` in convex/aiCredits.ts are declared with `args: {}` (no `clerkUserId` argument), and that `createTopupCheckoutSession` in app/[locale]/(app)/notes/actions.ts takes no parameters and sources clerkUserId from `auth()`. Use grep to assert there is NO `clerkUserId:` inside the `args:` object of runAiAction/getMyCredits and NO `clerkUserId` parameter on the Server Action signature. (2) T-05-04 Pro-gate check: grep app/[locale]/(app)/notes/actions.ts for a `status !== "active"` (or `status === "active"` guard) that throws before `stripe.checkout.sessions.create`. (3) Run the full unit suite `npm run test:unit` and confirm it is green (deductCredit success/insufficient/concurrency, addCredits success/insert/idempotency, webhook payment/idempotent/subscription-regression, plus all pre-existing tests). (4) Run `npx tsc --noEmit -p tsconfig.json` and confirm 0 errors. Record each check's result in the SUMMARY. If any check fails, STOP and report which requirement/threat is unmet — do not proceed to Task 2.
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - runAiAction and getMyCredits use `args: {}` (grep confirms no client-supplied clerkUserId)
    - createTopupCheckoutSession has a server-side non-active-status throw before the Stripe call
    - `npm run test:unit` exits 0 (full suite green)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>All automated security assertions pass and the full unit + type-check suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The AI credits system end-to-end: a Sparkles toolbar button + neutral balance badge in the editor (visible for all users), a zero-credit blocking dialog that branches its CTA on plan, atomic server-side deduction, and a payment-mode Stripe top-up that credits +50 via the existing webhook dispatcher.
  </what-built>
  <how-to-verify>
    Prerequisite: run the app (`next dev --experimental-https`) and, for the top-up test, `stripe listen --forward-to https://localhost:3000/api/webhooks/stripe --skip-verify` with STRIPE_WEBHOOK_SECRET synced (per STATE.md Phase 3 note).

    1. As a FREE user (no active subscription): open a note in the editor. CONFIRM the Sparkles AI button and a balance badge showing "0" are visible in the toolbar. Click the AI button. CONFIRM the CreditsExhaustedDialog opens with the Free copy ("Free plans don't include AI credits...") and an "Upgrade to Pro" button that navigates to /pricing.
    2. As a PRO user with balance 0 (spend down or seed balance=0): click the AI button. CONFIRM the dialog opens with the Pro copy ("You've used all your AI credits...") and a "Buy 50 credits" button.
    3. As a PRO user with balance > 0: click the AI button. CONFIRM no dialog appears and the badge decrements by 1 in place (no page refresh).
    4. Top-up (PAY-05 SC4): from the Pro zero-balance dialog, click "Buy 50 credits". CONFIRM redirect to Stripe Checkout in payment mode (one-time, ~2€). Complete the test-mode payment. CONFIRM you land back in /notes and, once the webhook processes, the badge increases by 50. CONFIRM a "topup" transaction was recorded (via Convex dashboard or `stripe listen` log showing checkout.session.completed handled).
    5. Reset (SC5, already Phase 2): no action needed — confirm only that this phase did not alter resetCredits.

  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe which check failed and how.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                                        |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| all phase entry points | Final confirmation that every client→server boundary in the phase derives identity server-side and enforces authority server-side. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                  |
| --------- | ---------------------- | ------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------ |
| T-05-01   | IDOR                   | runAiAction / getMyCredits / top-up action | mitigate    | Verified by grep in Task 1: all use identity-derived clerkUserId, none accept it as an argument. |
| T-05-04   | Elevation of Privilege | top-up Pro gate                            | mitigate    | Verified by grep in Task 1: server-side status check throws before the Stripe call.              |
| T-05-02   | Tampering              | TOCTOU deduction                           | mitigate    | Verified by the concurrency test in the full suite (Task 1).                                     |
| T-05-03   | Tampering              | webhook double-credit                      | mitigate    | Verified by the idempotency test in the full suite (Task 1).                                     |
| T-05-SC   | Tampering              | npm/pip/cargo installs                     | accept      | No packages installed across the entire phase.                                                   |

</threat_model>

<verification>
- `npm run test:unit` green; `npx tsc --noEmit` 0 errors.
- Grep confirms IDOR-safe signatures and the Pro gate.
- Human UAT sign-off on ROADMAP SC1-SC5.
</verification>

<success_criteria>

- All five ROADMAP Phase 5 success criteria confirmed (SC1 atomic/TOCTOU, SC2 visible balance, SC3 zero-credit block + top-up prompt, SC4 payment-mode top-up credits +50, SC5 unchanged auto-reset).
- Security threat model dispositions all verified in source or by test.
  </success_criteria>

<output>
Create `.planning/phases/05-ai-credits-system/05-05-SUMMARY.md` when done, recording the automated check results and the human UAT outcome (approved / issues). This is the phase gate before `/gsd:verify-work`.
</output>
