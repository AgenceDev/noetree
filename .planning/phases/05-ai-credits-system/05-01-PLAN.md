---
phase: 05-ai-credits-system
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/aiCredits.ts
  - convex/aiCredits.test.ts
autonomous: true
requirements: [CRED-04, CRED-02]
must_haves:
  truths:
    - "runAiAction deducts exactly 1 credit from the authenticated user's balance and records a 'deduction' creditTransactions row"
    - "Two concurrent runAiAction/deductCredit calls at balance=1 never both succeed (TOCTOU-safe)"
    - "A user at balance 0 (or with no aiCredits row) calling runAiAction is rejected with ConvexError whose data === 'INSUFFICIENT_CREDITS' and balance is not mutated"
    - "getMyCredits returns the authenticated user's aiCredits row derived from identity, never from a client-supplied clerkUserId"
    - "The refund path exists in runAiAction (implemented, not reached this phase) and creditTransactions accepts type 'refund'"
  artifacts:
    - path: "convex/schema.ts"
      provides: "refund literal on creditTransactions.type union"
      contains: 'v.literal("refund")'
    - path: "convex/aiCredits.ts"
      provides: "deductCredit real body + runAiAction public mutation + getMyCredits identity-derived query"
      contains: "export const runAiAction"
    - path: "convex/aiCredits.test.ts"
      provides: "convex-test coverage for deduction, insufficient-balance, and concurrency"
      contains: "INSUFFICIENT_CREDITS"
  key_links:
    - from: "convex/aiCredits.ts runAiAction"
      to: "aiCredits table (by_clerkUserId)"
      via: "identity-derived read + patch in a single mutation handler"
      pattern: "getUserIdentity"
    - from: "convex/aiCredits.ts runAiAction insufficient branch"
      to: "client onError"
      via: 'throw new ConvexError("INSUFFICIENT_CREDITS")'
      pattern: "INSUFFICIENT_CREDITS"
---

<objective>
Fill in the real body of the `deductCredit` internalMutation, add a public identity-derived `runAiAction` mutation (deduct 1 credit → run placeholder action → refund on failure, all in one transactional handler), add a public identity-derived `getMyCredits` query for the balance badge, and add the `"refund"` literal to the `creditTransactions.type` schema union.

Purpose: This is the atomic, TOCTOU-safe core of the phase (ROADMAP SC1). It establishes the `INSUFFICIENT_CREDITS` ConvexError contract the toolbar UI keys its blocking dialog off of (Plan 04) and the identity-derived balance query the badge reads (CRED-02). All concurrency safety comes from Convex's single-handler serializable transaction model — no locking primitive, no version field.

Output: `deductCredit` implemented, new `runAiAction` + `getMyCredits`, schema `"refund"` literal, and convex-test coverage proving deduction / insufficient-balance / concurrency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-credits-system/05-CONTEXT.md
@.planning/phases/05-ai-credits-system/05-RESEARCH.md
@.planning/phases/05-ai-credits-system/05-PATTERNS.md
@convex/_generated/ai/guidelines.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly, no exploration. -->

Existing convex/aiCredits.ts (this plan edits it):

- `getCredits = query({ args: { clerkUserId: v.string() }, ... })` — takes a CLIENT-SUPPLIED clerkUserId. Leave it UNCHANGED (existing Phase 2 tests call `api.aiCredits.getCredits` with `{ clerkUserId }`). Do NOT use it from any client — add the new `getMyCredits` for that.
- `deductCredit = internalMutation({ args: { clerkUserId: v.string(), amount: v.number() }, handler: throws "Not implemented" })` — fill this body.
- `addCredits` — LEAVE UNCHANGED this plan (Plan 02 owns it).
- `resetCredits` — fully implemented reference; DO NOT modify. Mirror its patch-or-insert branch and its `processedStripeEvents` idempotency shape.
- Module constant `const MONTHLY_CREDIT_QUOTA = 100` exists at top.

resetCredits patch-or-insert shape to mirror (convex/aiCredits.ts lines 53-76):

- `.query("aiCredits").withIndex("by_clerkUserId", q => q.eq("clerkUserId", clerkUserId)).unique()`
- `if (existingCredits) ctx.db.patch(existingCredits._id, {...}) else ctx.db.insert("aiCredits", {...})`
- then `ctx.db.insert("creditTransactions", { clerkUserId, type, amount, createdAt: Date.now() })`

Identity-derivation pattern to mirror — convex/subscriptions.ts getSubscription:

- `const identity = await ctx.auth.getUserIdentity(); if (!identity) return null;` (query) / throw (mutation)
- `identity.subject` is the BARE clerkUserId (`user_xxx`) — the same value stored in aiCredits.clerkUserId. NEVER accept clerkUserId as a function argument (guidelines.md IDOR rule).

Distinguishable client-visible error — convex/notes.ts line 559:

- `throw new ConvexError("NOTE_LIMIT_REACHED")` — a plain Error's message is redacted before reaching the client; ConvexError.data survives. Use the identical pattern with `"INSUFFICIENT_CREDITS"`.

Test harness — convex/aiCredits.test.ts (lines 1-19): `convexTest(schema, modules)`, `modules = import.meta.glob("./**/*.ts")`, `SUBSCRIPTION_SEED` constant, `t.run(async ctx => ctx.db.insert(...))` for seeding, `t.mutation(internal.aiCredits.X, {...})`, `t.query(api.aiCredits.X, {...})`, `t.withIdentity({ subject, tokenIdentifier })` for identity-derived calls.

Import line at convex/aiCredits.ts top is currently `import { query, internalMutation } from "./_generated/server";` and `import { v } from "convex/values";` — you must add `mutation` to the server import and `ConvexError` to the values import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add "refund" schema literal + implement deductCredit body</name>
  <files>convex/schema.ts, convex/aiCredits.ts, convex/aiCredits.test.ts</files>
  <read_first>
    - convex/schema.ts lines 61-71 (creditTransactions.type union to extend)
    - convex/aiCredits.ts (full file — deductCredit stub, resetCredits reference, imports)
    - convex/aiCredits.test.ts lines 1-40 (harness, seed constants, existing describe block)
    - convex/_generated/ai/guidelines.md (mutation transaction guarantees; patch throws on missing doc; v.* validators mandatory)
    - .planning/phases/05-ai-credits-system/05-PATTERNS.md ("deductCredit" and "Distinguishable client-visible errors" sections)
  </read_first>
  <behavior>
    - deductCredit with clerkUserId owning balance=5, amount=1 → patches balance to 4, inserts a creditTransactions row {type:"deduction", amount:-1}; returns { success: true }.
    - deductCredit with balance=1, amount=1 → patches balance to 0.
    - deductCredit with balance=0 → throws ConvexError with data "INSUFFICIENT_CREDITS"; no patch, no transaction row.
    - deductCredit with no aiCredits row for that clerkUserId → treated as balance 0 → throws ConvexError "INSUFFICIENT_CREDITS".
    - Two deductCredit calls issued via Promise.all against the same t instance at balance=1 → exactly one resolves { success: true }, the other rejects; final balance is 0 (never -1).
    - creditTransactions accepts a row with type "refund" (schema validation passes).
  </behavior>
  <action>
    In convex/schema.ts, add `v.literal("refund")` as a fourth member of the `creditTransactions.type` `v.union(...)` (after `v.literal("reset")`, D-07). No index change.

    In convex/aiCredits.ts: update the imports — change `import { query, internalMutation } from "./_generated/server";` to also import `mutation`, and change `import { v } from "convex/values";` to `import { v, ConvexError } from "convex/values";`.

    Add a module-level plain helper `async function applyDeduction(ctx, clerkUserId: string, amount: number)` (typed with `MutationCtx` from `./_generated/server`). It reads the aiCredits row via `by_clerkUserId` `.unique()`, computes `balance = credits?.balance ?? 0` (D-09: no row = 0), and if `balance < amount` throws `new ConvexError("INSUFFICIENT_CREDITS")`. Otherwise, if `credits` exists `ctx.db.patch(credits._id, { balance: balance - amount })`, else `ctx.db.insert("aiCredits", { clerkUserId, balance: balance - amount, lastResetAt: Date.now() })` (defensive-only branch, unreachable when balance<amount already threw). Then `ctx.db.insert("creditTransactions", { clerkUserId, type: "deduction", amount: -amount, createdAt: Date.now() })`. Read AND write happen inside this one helper called from one mutation handler — never split across `ctx.runMutation` (Pitfall 1: reopens the TOCTOU window).

    Replace the `deductCredit` internalMutation handler (keep its existing `args: { clerkUserId: v.string(), amount: v.number() }` and remove the `// LEAVE UNCHANGED — Phase 5` comment) so its handler body is `return await applyDeduction(ctx, args.clerkUserId, args.amount);` returning `{ success: true }` on success.

    Extend convex/aiCredits.test.ts: add `describe("aiCredits.deductCredit", ...)` with the six behavior cases above. Seed an aiCredits row with `t.run(async ctx => ctx.db.insert("aiCredits", { clerkUserId: "user_x", balance: 5, lastResetAt: Date.now() }))`. Call `t.mutation(internal.aiCredits.deductCredit, { clerkUserId: "user_x", amount: 1 })`. Assert insufficient case with `await expect(...).rejects.toThrow("INSUFFICIENT_CREDITS")`. For the concurrency case use `await Promise.allSettled([t.mutation(...), t.mutation(...)])` at balance=1 and assert exactly one fulfilled + final balance is 0 (query the row via `t.run`). For the refund-literal case, `t.run(async ctx => ctx.db.insert("creditTransactions", { clerkUserId: "user_x", type: "refund", amount: 1, createdAt: Date.now() }))` and assert it does not throw.

  </action>
  <verify>
    <automated>npx vitest run convex/aiCredits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - convex/schema.ts creditTransactions.type union contains `v.literal("refund")`
    - convex/aiCredits.ts imports `ConvexError` from "convex/values" and `mutation` from "./_generated/server"
    - convex/aiCredits.ts contains `async function applyDeduction` and `deductCredit`'s handler delegates to it
    - deductCredit throws `new ConvexError("INSUFFICIENT_CREDITS")` when balance < amount; no `ctx.db.patch`/`insert` runs on that path
    - The concurrency test uses `Promise.allSettled` (or `Promise.all`) against the same `t` instance, not two sequential awaited calls
    - `npx vitest run convex/aiCredits.test.ts` exits 0 with all tests passing (including the pre-existing resetCredits/getCredits tests)
  </acceptance_criteria>
  <done>Schema accepts "refund"; deductCredit atomically decrements and records a deduction, throws a distinguishable ConvexError on insufficient balance, and is proven TOCTOU-safe by the concurrency test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public runAiAction mutation + getMyCredits query</name>
  <files>convex/aiCredits.ts, convex/aiCredits.test.ts</files>
  <read_first>
    - convex/aiCredits.ts (post-Task-1 state — applyDeduction helper, imports)
    - convex/subscriptions.ts getSubscription (identity-derivation with args:{} to mirror)
    - .planning/phases/05-ai-credits-system/05-CONTEXT.md (D-04, D-05, D-06, D-09)
    - .planning/phases/05-ai-credits-system/05-RESEARCH.md (Pattern 1, Pattern 2, Open Question 1 — the public wrapper naming/identity derivation)
  </read_first>
  <behavior>
    - runAiAction called with an identity whose subject owns balance=3 → returns { success: true } and balance becomes 2, with one "deduction" transaction recorded.
    - runAiAction called with identity subject owning balance=0 → throws ConvexError data "INSUFFICIENT_CREDITS"; balance unchanged; no transaction.
    - runAiAction called with an identity that has no aiCredits row → throws ConvexError "INSUFFICIENT_CREDITS" (Free user / pre-first-reset, D-09).
    - runAiAction called with no authenticated identity → throws (unauthenticated).
    - getMyCredits with an identity whose subject owns balance=7 → returns the row with balance 7.
    - getMyCredits with an identity that has no row → returns null.
    - getMyCredits with no identity → returns null.
  </behavior>
  <action>
    Add a public `runAiAction = mutation({ args: {}, handler: async ctx => {...} })` to convex/aiCredits.ts. In the handler: `const identity = await ctx.auth.getUserIdentity(); if (!identity) throw new ConvexError("UNAUTHENTICATED");`. Derive `clerkUserId = identity.subject` (NEVER a client argument — D-05 IDOR mitigation). Call `await applyDeduction(ctx, clerkUserId, 1)` (the deduct step; propagates the INSUFFICIENT_CREDITS ConvexError to the client). Then run the placeholder action inline: `const actionSucceeded = true;` (D-01 — no LLM call, always succeeds). Then the refund path (D-04/D-06, implemented but unreachable this phase): `if (!actionSucceeded) { read the row, ctx.db.patch(row._id, { balance: row.balance + 1 }), ctx.db.insert("creditTransactions", { clerkUserId, type: "refund", amount: 1, createdAt: Date.now() }); }`. Add a comment noting the refund branch is intentionally unreachable while the action is a placeholder (D-06). Return `{ success: actionSucceeded }`. Do NOT call `ctx.runMutation(internal.aiCredits.deductCredit)` — inline via `applyDeduction` so deduct + action + refund share one transaction (Pitfall 1).

    Add a public `getMyCredits = query({ args: {}, handler: async ctx => {...} })`: derive identity like getSubscription — `if (!identity) return null;` then query aiCredits `.withIndex("by_clerkUserId", q => q.eq("clerkUserId", identity.subject)).unique()` and return it. This is the IDOR-safe read the badge uses (CRED-02); it takes NO clerkUserId argument.

    Extend convex/aiCredits.test.ts: add `describe("aiCredits.runAiAction")` and `describe("aiCredits.getMyCredits")`. Drive both through `t.withIdentity({ subject: "user_x", tokenIdentifier: "https://clerk.dev|user_x" }).mutation(api.aiCredits.runAiAction, {})` / `.query(api.aiCredits.getMyCredits, {})`. Seed the aiCredits row keyed on the BARE subject (`clerkUserId: "user_x"`). Assert the insufficient/no-row cases with `.rejects.toThrow("INSUFFICIENT_CREDITS")`, and the no-identity getMyCredits case returns null (call without `withIdentity`).

  </action>
  <verify>
    <automated>npx vitest run convex/aiCredits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - convex/aiCredits.ts contains `export const runAiAction = mutation({ args: {},` and `export const getMyCredits = query({ args: {},`
    - runAiAction derives `identity.subject`, calls `applyDeduction(ctx, ..., 1)`, and contains a refund branch inserting a `type: "refund"` transaction
    - runAiAction does NOT contain `ctx.runMutation`
    - getMyCredits takes no clerkUserId arg and returns null when `getUserIdentity()` is null
    - Tests drive runAiAction/getMyCredits via `t.withIdentity({ subject, tokenIdentifier })` with subject matching the seeded aiCredits.clerkUserId
    - `npx vitest run convex/aiCredits.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>The toolbar has a public, identity-derived mutation (runAiAction) that deducts a credit TOCTOU-safely and a public identity-derived query (getMyCredits) for the badge; both reject/return-null cleanly without any client-supplied identifier.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                           |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| client → Convex runAiAction/getMyCredits | Untrusted client invokes the deduction mutation and balance query; must not be able to act on another user's credits. |
| concurrent clients → aiCredits row       | Two in-flight deductions against the same balance must serialize.                                                     |

## STRIDE Threat Register

| Threat ID      | Category                      | Component                                         | Disposition | Mitigation Plan                                                                                                                                                                                                                |
| -------------- | ----------------------------- | ------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-05-01        | Elevation of Privilege / IDOR | runAiAction, getMyCredits                         | mitigate    | Derive clerkUserId exclusively from `ctx.auth.getUserIdentity().subject`; both functions take `args: {}` — no client-supplied clerkUserId (Task 2). Mirrors 03-REVIEW.md CR-01.                                                |
| T-05-02        | Tampering / Repudiation       | deductCredit balance decrement (TOCTOU)           | mitigate    | Read + branch + patch inside a single mutation handler (applyDeduction), never split across ctx.runMutation. Convex serializes concurrent same-document mutations. Proven by the Promise.allSettled concurrency test (Task 1). |
| T-05-IDOR-READ | Information Disclosure        | existing getCredits (client-supplied clerkUserId) | accept      | Pre-existing Phase 2 query; low-value (a credit count). Client-facing reads use the new getMyCredits instead; getCredits is retained only for internal/test callers. Not refactored to avoid breaking existing Phase 2 tests.  |
| T-05-SC        | Tampering                     | npm/pip/cargo installs                            | accept      | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit: N/A). No install tasks exist.                                                                                                                      |

</threat_model>

<verification>
- `npx vitest run convex/aiCredits.test.ts` — all deduction/concurrency/insufficient/getMyCredits tests pass; pre-existing resetCredits/getCredits tests still pass.
- `npm run test:unit` — full suite green (no regressions from the schema literal or import changes).
- Grep: `runAiAction`, `getMyCredits`, `applyDeduction`, `INSUFFICIENT_CREDITS` present in convex/aiCredits.ts; `v.literal("refund")` present in convex/schema.ts.
</verification>

<success_criteria>

- ROADMAP SC1 (atomic deduction, TOCTOU-safe): proven by the concurrency test.
- ROADMAP SC3 (0-credit blocked): runAiAction throws INSUFFICIENT_CREDITS at balance 0 / no row.
- CRED-04: credits deducted server-side inside the mutation, never client-side.
- CRED-02 groundwork: getMyCredits identity-derived balance read exists for the badge.
- D-07: schema accepts "refund".
  </success_criteria>

<output>
Create `.planning/phases/05-ai-credits-system/05-01-SUMMARY.md` when done, recording the exact public mutation name (`runAiAction`), query name (`getMyCredits`), and the error string (`INSUFFICIENT_CREDITS`) so Plan 04 (toolbar) consumes the identical contract.
</output>
