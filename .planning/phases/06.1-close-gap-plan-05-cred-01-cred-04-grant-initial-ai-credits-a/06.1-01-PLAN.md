---
phase: 06.1-close-gap-plan-05-cred-01-cred-04-grant-initial-ai-credits-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/aiCredits.ts
  - convex/aiCredits.test.ts
  - convex/stripeWebhooks.ts
  - convex/stripeWebhooks.test.ts
autonomous: true
requirements: [PLAN-05, CRED-01, CRED-04]

must_haves:
  truths:
    - "A subscription-mode checkout.session.completed grants the resolved clerkUserId exactly 100 AI credits (balance is 100, not null/0)"
    - "The credit grant is NOT silently skipped by a processedStripeEvents key collision with upsertSubscription's row for the same raw stripeEventId"
    - "Replaying the same subscription-mode checkout event does not double-grant (balance stays 100, exactly one 'reset' transaction)"
    - "A re-subscription for a user with a leftover balance resets to exactly 100, not additive (42 -> 100, never 142) — D-01"
    - "The subscriptions row is still created on checkout (no regression to the existing upsertSubscription behavior)"
    - "D-02: no backfill script in this phase — pre-existing 0-balance Pro subscribers self-heal at their next monthly renewal via the existing, unmodified resetCredits/invoice.paid path; this plan is fix-forward only, changing behavior for checkouts completed after it ships"
  artifacts:
    - path: "convex/aiCredits.ts"
      provides: "grantInitialCredits internalMutation (reset-to-100 semantics, collision-safe idempotency key)"
      contains: "export const grantInitialCredits"
    - path: "convex/stripeWebhooks.ts"
      provides: "subscription-branch call to internal.aiCredits.grantInitialCredits alongside upsertSubscription"
      contains: "internal.aiCredits.grantInitialCredits"
    - path: "convex/aiCredits.test.ts"
      provides: "unit tests for grantInitialCredits including the raw-key-collision regression test"
      contains: "grantInitialCredits"
    - path: "convex/stripeWebhooks.test.ts"
      provides: "subscription-branch grant tests + inverted line-176 assertion + replay landmine test"
      contains: "grantInitialCredits"
  key_links:
    - from: "convex/stripeWebhooks.ts"
      to: "internal.aiCredits.grantInitialCredits"
      via: "ctx.runMutation in the checkout.session.completed subscription branch"
      pattern: "ctx\\.runMutation\\(\\s*internal\\.aiCredits\\.grantInitialCredits"
    - from: "convex/aiCredits.ts grantInitialCredits"
      to: "processedStripeEvents"
      via: "insert with a suffixed idempotency key that does not collide with the raw stripeEventId"
      pattern: "credits-grant"
---

<objective>
Close the v1.0 milestone-audit BLOCKER: a brand-new Pro subscriber currently receives zero AI credits at checkout because the `checkout.session.completed` subscription branch only calls `upsertSubscription` — the only credit-granting path (`invoice.paid` / `billing_reason === "subscription_cycle"`) explicitly excludes the initial `subscription_create` invoice. This plan grants the initial 100-credit quota at checkout so a paying Pro user gets the plan benefit immediately instead of hitting `INSUFFICIENT_CREDITS` on their first AI action.

Purpose: Restore PLAN-05 / CRED-01 / CRED-04 to fully-true. Quota is defined and reset on renewal today; this plan makes it granted at the moment the subscription is created.
Output: A new `grantInitialCredits` internal mutation, wired into the existing subscription-mode webhook branch, with unit + dispatcher tests — including the explicit regression test for the idempotency-key collision landmine flagged in CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-close-gap-plan-05-cred-01-cred-04-grant-initial-ai-credits-a/06.1-CONTEXT.md
@.planning/phases/06.1-close-gap-plan-05-cred-01-cred-04-grant-initial-ai-credits-a/06.1-PATTERNS.md
@convex/_generated/ai/guidelines.md

<interfaces>
<!-- Contracts the executor needs. Extracted from current source. Use directly — no exploration needed. -->

Existing `resetCredits` (convex/aiCredits.ts:161-222) — closest analog for the NEW mutation.
It uses INDIRECT clerkUserId resolution (subscriptions join via stripeSubscriptionId) because
invoice.paid events lack clerkUserId. The new grant does NOT need that join — clerkUserId is
already in scope at the webhook call site. Copy resetCredits' "reset-to-100" balance semantics,
NOT addCredits' additive semantics.

Existing `addCredits` signature (convex/aiCredits.ts:224-231) — closest analog for the NEW mutation's
ARG SHAPE (direct clerkUserId string arg):
args: { clerkUserId: v.string(), amount: v.number(), stripeEventId: v.string(),
eventType: v.string(), stripePaymentIntentId: v.optional(v.string()) }

Idempotency guard pattern (identical in resetCredits, addCredits, upsertSubscription):
const already = await ctx.db.query("processedStripeEvents")
.withIndex("by_stripeEventId", q => q.eq("stripeEventId", <KEY>)).unique();
if (already) return { alreadyProcessed: true };
// ...state write...
await ctx.db.insert("processedStripeEvents", { stripeEventId: <KEY>, eventType, processedAt: Date.now() });

LANDMINE (CONTEXT.md + PATTERNS.md): upsertSubscription (convex/subscriptions.ts:80-84) ALREADY inserts a
processedStripeEvents row keyed by the RAW stripeEventId for this same checkout.session.completed event.
If the new mutation reuses the raw stripeEventId as its idempotency key, its `already` check finds
upsertSubscription's row and silently returns { alreadyProcessed: true } WITHOUT granting — webhook still
returns 200, subscription still activates, only credits stay missing. The new mutation MUST use a
non-colliding key: `${args.stripeEventId}:credits-grant` (schema-valid v.string(), no migration needed —
by_stripeEventId is a non-unique index at convex/schema.ts:74-78).

creditTransactions.type union (convex/schema.ts:63-68): only "deduction" | "topup" | "reset" | "refund"
exist. The grant reuses "reset" (per CONTEXT.md Phase 5 D-07 pointer) — NO new literal, NO schema change.

MONTHLY_CREDIT_QUOTA = 100 (convex/aiCredits.ts:10) — the locked grant amount.

Current subscription branch (convex/stripeWebhooks.ts:88-114) `return`s directly from upsertSubscription's
result. clerkUserId is resolved once at line 44: `const clerkUserId = session.metadata?.clerkUserId;`.
The subscription branch today has NO local clerkUserId guard — upsertSubscription (convex/subscriptions.ts:47-52)
handles a missing clerkUserId by returning { anomaly: "missing clerkUserId" }. The existing test at
convex/stripeWebhooks.test.ts:91-115 (null metadata, no `mode` field) asserts that returned anomaly.

route.ts (app/api/webhooks/stripe/route.ts:62-93) IGNORES the action's return payload — it returns 200 on
any normal resolution, 400 only on ConvexError "Unauthorized", 500 only on throw. So the subscription
branch's return shape is unconstrained as long as it does not throw; existing tests assert via
getSubscription/getCredits queries, not the return value.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add grantInitialCredits internal mutation with collision-safe idempotency</name>
  <files>convex/aiCredits.ts, convex/aiCredits.test.ts</files>
  <read_first>
    - convex/aiCredits.ts (current state — model the new mutation on resetCredits lines 161-222 for reset-to-100 balance semantics + addCredits lines 224-280 for the direct-clerkUserId arg shape)
    - convex/aiCredits.test.ts (current state — copy the convexTest harness, describe/it structure, and the resetCredits idempotency-replay test at lines 69-99 as the pattern for the new tests)
    - convex/subscriptions.ts (lines 21-88 — read-only; this is upsertSubscription, the sibling mutation that inserts the RAW-stripeEventId processedStripeEvents row the new mutation must not collide with)
    - convex/schema.ts (lines 55-78 — confirm aiCredits fields {clerkUserId, balance, lastResetAt}, creditTransactions.type union, and the non-unique by_stripeEventId index)
    - convex/_generated/ai/guidelines.md (Convex mutation/validator/testing rules — new mutation must include arg validators; tests use import.meta.glob module map)
  </read_first>
  <behavior>
    - Test: grantInitialCredits inserts a new aiCredits row with balance exactly 100 when none exists for the given clerkUserId (direct arg), and sets lastResetAt to a number.
    - Test: grantInitialCredits patches an EXISTING aiCredits row to exactly 100 when the user already has a leftover balance of 42 — result is 100 (reset, NOT 142 additive), and exactly one aiCredits row remains (patch path, not duplicate insert). [D-01]
    - Test: grantInitialCredits inserts exactly one creditTransactions row of type "reset" with amount 100.
    - Test: grantInitialCredits is idempotent by its OWN suffixed key — calling it twice with the same stripeEventId returns { alreadyProcessed: true } on the second call, balance stays 100, and exactly one "reset" transaction exists.
    - Test (LANDMINE regression — the critical one): pre-insert a processedStripeEvents row keyed by the RAW stripeEventId (simulating upsertSubscription having already run for the same checkout event), THEN call grantInitialCredits with that same stripeEventId — it MUST still grant (balance becomes 100), proving its idempotency key does not collide with upsertSubscription's raw-key row.
  </behavior>
  <action>
    Add a new `internalMutation` named `grantInitialCredits` to convex/aiCredits.ts (place it directly after `resetCredits`, before `addCredits`; no new imports needed — internalMutation, v, MONTHLY_CREDIT_QUOTA are already in scope). Args (validators required): `clerkUserId: v.string()`, `stripeEventId: v.string()`, `eventType: v.string()`. Model the arg shape on `addCredits` (direct clerkUserId, no subscriptions-table join — clerkUserId arrives directly from the caller, unlike resetCredits' indirect stripeSubscriptionId lookup).

    Handler logic, in order:
    1. Compute the idempotency key as the exact string `` `${args.stripeEventId}:credits-grant` `` and store it in a const. This suffixed key is MANDATORY — it is the fix for the landmine. Do NOT use `args.stripeEventId` bare as the key.
    2. Idempotency guard: query `processedStripeEvents` `by_stripeEventId` for the suffixed key; if a row exists, `return { alreadyProcessed: true }`.
    3. Balance write with RESET (not additive) semantics per D-01: look up the aiCredits row `by_clerkUserId`; if it exists, `ctx.db.patch` it to `{ balance: MONTHLY_CREDIT_QUOTA, lastResetAt: Date.now() }`; else `ctx.db.insert` `{ clerkUserId, balance: MONTHLY_CREDIT_QUOTA, lastResetAt: Date.now() }`. Never use `existing.balance + amount`.
    4. Insert one `creditTransactions` row `{ clerkUserId, type: "reset", amount: MONTHLY_CREDIT_QUOTA, createdAt: Date.now() }`. Reuse the existing "reset" literal (D-07) — do NOT add a schema literal.
    5. Insert the `processedStripeEvents` marker keyed by the SAME suffixed key: `{ stripeEventId: <suffixed key>, eventType: args.eventType, processedAt: Date.now() }`.
    6. `return { granted: MONTHLY_CREDIT_QUOTA }`.

    All five reads/writes happen inside this single mutation handler (never split across ctx.runMutation) so the check-and-mark is atomic (Phase 2 D-14 pattern). Follow the anomaly-not-throw convention only if you add any guard — but note clerkUserId here is a required v.string(), so the guard against a missing clerkUserId lives at the CALL SITE (Task 2), not here.

    Then add the five tests described in <behavior> to convex/aiCredits.test.ts. Add them as a new `describe("aiCredits.grantInitialCredits", ...)` block. Reuse the existing module-map/convexTest setup at the top of the file. For the landmine-regression test, seed the raw-key row via `t.run(async ctx => ctx.db.insert("processedStripeEvents", { stripeEventId: "<rawId>", eventType: "checkout.session.completed", processedAt: Date.now() }))` before invoking the mutation with `stripeEventId: "<rawId>"`, then assert `getCredits` returns balance 100.

  </action>
  <verify>
    <automated>rtk vitest run convex/aiCredits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `convex/aiCredits.ts` contains `export const grantInitialCredits = internalMutation({` with args validators `clerkUserId: v.string()`, `stripeEventId: v.string()`, `eventType: v.string()`.
    - Source: the handler references the literal substring `:credits-grant` and uses that suffixed value for BOTH the `processedStripeEvents` query filter AND the insert (grep both the `.withIndex(... credits-grant ...)` check and the `ctx.db.insert("processedStripeEvents", ...)` use the suffixed key, never bare `args.stripeEventId`).
    - Source: the balance write uses `balance: MONTHLY_CREDIT_QUOTA` (both patch and insert branches); the string `existingCredits.balance +` or `+ args.amount` does NOT appear in grantInitialCredits.
    - Source: the creditTransactions insert uses `type: "reset"` (no new schema literal added to convex/schema.ts).
    - Behavior: `rtk vitest run convex/aiCredits.test.ts` passes, including the new `grantInitialCredits` describe block (all five tests), with the landmine-regression test proving a pre-existing raw-key processedStripeEvents row does NOT block the grant.
    - Behavior: the re-subscription test asserts final balance is exactly 100 (not 142) starting from a seeded balance of 42.
  </acceptance_criteria>
  <done>grantInitialCredits exists as an internalMutation with a non-colliding `:credits-grant` idempotency key, reset-to-100 semantics, and passing unit tests including the collision-regression test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire grantInitialCredits into the checkout.session.completed subscription branch</name>
  <files>convex/stripeWebhooks.ts, convex/stripeWebhooks.test.ts</files>
  <read_first>
    - convex/stripeWebhooks.ts (current state — the subscription branch is lines 73-114; clerkUserId resolved at line 44; payment-mode sibling guard+call is lines 46-71 and is the structural template for the anomaly guard)
    - convex/stripeWebhooks.test.ts (current state — MUST update the test at lines 176-210; reuse seedCheckoutEvent lines 14-34; the payment-mode replay test lines 149-174 is the template for the subscription replay test)
    - convex/aiCredits.ts (grantInitialCredits from Task 1 — confirm its exact arg names before wiring the ctx.runMutation call)
    - app/api/webhooks/stripe/route.ts (lines 62-93 — confirm the return payload is ignored so the branch's return shape is free to stay as upsertSubscription's result)
  </read_first>
  <behavior>
    - Test (invert existing lines 176-210): a subscription-mode checkout.session.completed now GRANTS 100 credits. Update the assertion `expect(credits).toBeNull()` to `expect(credits?.balance).toBe(100)`. Keep the subscription-row assertions (sub not null, stripeSubscriptionId correct). Update the test name to reflect that it routes to grantInitialCredits (reset semantics), NOT addCredits/topup — and add an assertion that the resulting creditTransactions row is type "reset", and that NO "topup" transaction was created.
    - Test (LANDMINE regression at dispatcher level): dispatch the SAME subscription-mode checkout event (via seedCheckoutEvent) through processWebhookEvent TWICE. Assert: getCredits balance is 100 after the first dispatch (proving the grant fired despite upsertSubscription's raw-key row), stays 100 after the replay (no double-grant), the subscriptions row exists exactly once, and exactly one "reset" creditTransactions row exists.
    - Test (D-01 re-subscription at dispatcher level): seed an aiCredits row with balance 42 for the user, then dispatch a subscription-mode checkout event — assert final balance is exactly 100 (reset, not 142).
    - Test (regression, must stay green): the existing null-metadata anomaly test at lines 91-115 still returns { anomaly: "missing clerkUserId" } and no credits/subscription rows are created.
  </behavior>
  <action>
    In convex/stripeWebhooks.ts, modify ONLY the subscription branch of the `checkout.session.completed` case (lines 73-114). Do not touch the payment-mode branch, other event cases, or mapStripeSubscriptionStatus.

    1. After the existing unexpected-mode guard (line 82-87) and before the `stripeCustomerId`/`stripeSubscriptionId` computation, add a missing-clerkUserId anomaly guard mirroring the payment-mode branch (lines 54-59): `if (!clerkUserId) { console.error(\`checkout.session.completed (subscription) anomaly: missing clerkUserId for stripeEventId ${args.event.id}\`); return { anomaly: "missing clerkUserId" }; }`. This is required because grantInitialCredits' `clerkUserId` is a required `v.string()` — an unguarded call with `undefined` would throw an ArgumentValidationError and surface as a 500 to Stripe (endless retries). This guard also preserves the existing null-metadata test's `{ anomaly: "missing clerkUserId" }` result (it currently comes from upsertSubscription; now it short-circuits earlier — same returned value).
    2. Change the existing `return await ctx.runMutation(internal.subscriptions.upsertSubscription, {...})` so it instead assigns the result to a const (e.g. `const upsertResult = await ctx.runMutation(...)`), keeping every existing argument unchanged.
    3. Immediately after, add a second sequential call: `await ctx.runMutation(internal.aiCredits.grantInitialCredits, { stripeEventId: args.event.id, eventType: args.event.type, clerkUserId });`. Order: grant AFTER upsert (subscription row exists first); the distinct idempotency key makes order irrelevant to correctness. clerkUserId is already in scope from line 44 — do not re-derive it.
    4. `return upsertResult;` — preserves the branch's existing return shape (route.ts ignores the payload; existing tests assert via getSubscription/getCredits queries, so this stays backward-compatible).

    Then update convex/stripeWebhooks.test.ts:
    - Rewrite the lines 176-210 test per <behavior> (invert the credits assertion to balance 100, keep subscription assertions, assert type:"reset" and no "topup" row, update the test name).
    - Add the subscription-mode replay landmine test (model on the payment-mode replay test at lines 149-174, using seedCheckoutEvent and a single fixed event object dispatched twice).
    - Add the D-01 re-subscription test (seed aiCredits balance 42 via t.run, dispatch seedCheckoutEvent, assert balance 100).
    - Leave the null-metadata anomaly test (lines 91-115) unchanged; confirm it still passes.

  </action>
  <verify>
    <automated>rtk vitest run convex/stripeWebhooks.test.ts convex/aiCredits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `convex/stripeWebhooks.ts` subscription branch contains `ctx.runMutation(internal.aiCredits.grantInitialCredits, {` with args `stripeEventId: args.event.id`, `eventType: args.event.type`, `clerkUserId`.
    - Source: the subscription branch contains a `if (!clerkUserId)` guard that returns `{ anomaly: "missing clerkUserId" }` before the grant/upsert calls.
    - Source: the branch still calls `internal.subscriptions.upsertSubscription` with all its original arguments (no argument removed); it does NOT call `internal.aiCredits.addCredits` (grant path is grantInitialCredits, not the additive top-up mutation).
    - Behavior: `rtk vitest run convex/stripeWebhooks.test.ts convex/aiCredits.test.ts` passes with zero failures across both files.
    - Behavior: the updated line-176 test asserts `expect(credits?.balance).toBe(100)` (was `toBeNull()`), and asserts the transaction type is "reset" with no "topup" row.
    - Behavior: the new dispatcher replay test proves balance is 100 after one dispatch and still 100 after replaying the identical event (no double-grant, one subscriptions row, one "reset" transaction).
    - Behavior: the existing null-metadata test still returns `{ anomaly: "missing clerkUserId" }`.
  </acceptance_criteria>
  <done>A subscription-mode checkout grants 100 credits via grantInitialCredits alongside upsertSubscription, is replay-safe, resets (not stacks) on re-subscription, and the full webhook + credits test suites are green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                          | Description                                                                                                                                           |
| ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Stripe → Next.js webhook route (app/api/webhooks/stripe/route.ts) | Untrusted external POST; already gated by Stripe signature verification (constructEvent) — UNCHANGED by this plan.                                    |
| Webhook route → Convex action (processWebhookEvent)               | Gated by INTERNAL_WEBHOOK_SECRET shared-secret check — UNCHANGED by this plan.                                                                        |
| Convex action → grantInitialCredits internalMutation              | Internal-only; clerkUserId sourced from Stripe `session.metadata.clerkUserId` (server-set at checkout creation, Phase 2 D-06), never client-supplied. |

## STRIDE Threat Register

| Threat ID | Category                                           | Component                                                                         | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                |
| --------- | -------------------------------------------------- | --------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-06.1-01 | Denial (availability / business-logic correctness) | grantInitialCredits idempotency key vs upsertSubscription's raw-stripeEventId row | mitigate    | Use a distinct `${stripeEventId}:credits-grant` idempotency key so the grant is never silently no-op'd by the sibling write's processedStripeEvents row. Enforced by the raw-key-collision regression test in both aiCredits.test.ts and stripeWebhooks.test.ts (Tasks 1 & 2). |
| T-06.1-02 | Repudiation / replay                               | Duplicate/replayed webhook delivery double-granting credits                       | mitigate    | Atomic check-and-mark on processedStripeEvents keyed by the grant's own suffixed key; dispatcher replay test asserts balance stays 100 and exactly one "reset" transaction.                                                                                                    |
| T-06.1-03 | Elevation of privilege (IDOR)                      | grantInitialCredits credit grant                                                  | mitigate    | Registered as `internalMutation` (not `mutation`) — unreachable from the public client SDK; clerkUserId is derived from Stripe-set session metadata, never accepted from a browser. Consistent with the identity-derived pattern enforced across Phases 3-6.                   |
| T-06.1-04 | Tampering (business-logic gaming)                  | Cancel-before-renewal then resubscribe to stack credits                           | mitigate    | Reset-to-100 semantics (D-01), not additive — the re-subscription test asserts 42 → 100, never 142.                                                                                                                                                                            |
| T-06.1-05 | Spoofing                                           | Forged webhook payload reaching the grant                                         | accept      | Out of new scope — existing Stripe signature verification (route.ts:19-36) and INTERNAL_WEBHOOK_SECRET gate (stripeWebhooks.ts:33-38) are unchanged and not bypassed by this plan; the new mutation runs strictly downstream of both gates.                                    |
| T-06.1-SC | Tampering                                          | Supply chain (package installs)                                                   | accept      | No npm/pnpm/pip/cargo installs in this plan — no new dependencies added; no package-legitimacy checkpoint required.                                                                                                                                                            |

</threat_model>

<verification>
- `rtk vitest run convex/aiCredits.test.ts convex/stripeWebhooks.test.ts` — both files green, including the new grantInitialCredits describe block and the two collision/replay regression tests.
- Full suite sanity: `rtk vitest run` remains green (no regression to the 90+ existing tests).
- Source grep: `grantInitialCredits` appears in convex/aiCredits.ts (definition) and convex/stripeWebhooks.ts (call site); `:credits-grant` appears only in grantInitialCredits' idempotency key.
</verification>

<success_criteria>

- A subscription-mode `checkout.session.completed` grants the resolved clerkUserId exactly 100 AI credits (PLAN-05 / CRED-01 initial-grant gap closed; CRED-04 no longer starved).
- The grant is collision-safe (does not silently no-op against upsertSubscription's raw-key processedStripeEvents row) and replay-safe (no double-grant).
- Re-subscription resets the balance to exactly 100 (D-01, non-additive).
- The existing subscription-creation and anomaly-path behaviors are unchanged (no regression).
- Both credit and webhook test suites pass.
  </success_criteria>

<output>
Create `.planning/phases/06.1-close-gap-plan-05-cred-01-cred-04-grant-initial-ai-credits-a/06.1-01-SUMMARY.md` when done.
</output>
