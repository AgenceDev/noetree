# Phase 06.1: Close gap: PLAN-05/CRED-01/CRED-04 — grant initial AI credits at Pro checkout - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-12
**Phase:** 06.1-Close gap: PLAN-05/CRED-01/CRED-04 — grant initial AI credits at Pro checkout
**Areas discussed:** Re-subscription grant behavior, Backfill for already-affected subscribers

---

## Re-subscription Grant Behavior

| Option         | Description                                                                                                                                                                                                                | Selected |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Reset to 100   | Matches existing monthly-renewal reset semantics exactly (resetCredits already does balance = 100, not balance += 100). Simple, consistent, no risk of unbounded balance stacking from repeated cancel/resubscribe cycles. | ✓        |
| Add 100 on top | Preserves any leftover balance from before cancellation. More generous but inconsistent with renewal-reset behavior, and opens a minor gaming vector.                                                                      |          |
| You decide     | Let Claude pick during planning based on what's simplest to implement safely.                                                                                                                                              |          |

**User's choice:** Reset to 100 (Recommended)
**Notes:** No follow-up questions — user confirmed this was settled and moved to next area.

---

## Backfill for Already-Affected Subscribers

| Option                  | Description                                                                                                                                                                                                                                 | Selected |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Fix-forward only        | No backfill script this phase. New checkouts get credits correctly going forward; anyone already affected self-heals at their next monthly renewal. Appropriate since Stripe is still in test-mode — no real paying customers affected yet. | ✓        |
| One-off backfill script | Write a one-time internal Convex mutation/script to grant 100 credits to any active subscription with no credits row or a stale zero balance. Adds scope for a problem that self-heals within a month anyway.                               |          |

**User's choice:** Fix-forward only (Recommended)
**Notes:** None further.

---

## Claude's Discretion

- Exact idempotency-key strategy for the new credit-grant mutation call (must not collide with `upsertSubscription`'s `processedStripeEvents` row for the same `stripeEventId` — a landmine discovered during context-gathering and documented in CONTEXT.md `<decisions>`).
- Exact mutation shape: new dedicated mutation vs. adapting `resetCredits`'s call signature.
- Exact test coverage approach (unit test on webhook dispatcher subscription branch, duplicate-delivery/idempotency test).
- Whether the grant transaction uses `creditTransactions.type: "reset"` or a new type — left to planner, no strong preference expressed.

## Deferred Ideas

None — discussion stayed within the single-gap phase scope.
